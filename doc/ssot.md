# WP Perf Shield SSOT

This file is the single source of truth for packaging and release conventions.

## Plugin Identity

- Plugin folder: `wp-perf-shield`
- Main plugin file: `wp-perf-shield.php`
- WordPress plugin name: `WP Perf Shield`
- Author: `MENJ`
- Author URI: `https://github.com/menj`
- Minimum WordPress version: `5.8`
- Minimum PHP version: `7.4`

## Documentation Layout

The root must keep only the WordPress.org-compatible `readme.txt`.

Project documentation belongs in:

```text
doc/readme.md
doc/upgrading.md
doc/changelog.md
doc/ssot.md
```

The operator-facing manual smoke-test checklist lives in `doc/upgrading.md` under the "Manual Smoke-Test Checklist" heading. Run it on staging before publishing each release.

The operator-facing post-remediation cache invalidation checklist lives in `doc/upgrading.md` under the "Post-Remediation Cache Invalidation Checklist" heading. Run it on production after every remediation pass that removes confirmed malware artefacts. The checklist exists because cleanup at the WordPress layer does not flush every cache layer (object cache, page cache plugin, Plesk WP Toolkit cache, PHP OPcache, Nginx reverse proxy, CDN edge cache, browser cache, service worker cache) that may have stored a copy of the infected page. Without a deliberate cache invalidation pass, a fully-cleaned site can continue serving cached malicious HTML to visitors for hours.

## Indicator Data

Versioned malware indicator data lives in:

```text
includes/class-wps-indicators.php
```

`WPS_Indicators::INDICATOR_VERSION` should be bumped whenever the lists change. Hash lists, suffix patterns, and regex catalogues that are tightly coupled to specific scanner or hardening logic remain in their owning class until a parity-test harness exists.

## Auto-Delete Policy

Automatic deletion is reserved for confirmed malware evidence: exact malware
family folders, known malicious filenames or hashes, confirmed webshell/dropper
signatures, or multi-signal payloads that meet the scanner's own critical
threshold. Broad substrings, weak heuristics, harmless uploads `index.php`
silence stubs, active-theme critical files, and below-threshold payload scores
must stay review-only. Files that need modification rather than deletion must
be backed up with a timestamped non-overwriting backup before any write.

## Policy Denylist vs Malware Blocklist (1.4.62)

Two lists of plugin slugs now exist and must never be merged.

The **malware blocklist** (`WPS_Blocker::get_blocked_slugs()`, plus patterns and
hashes) is for confirmed malware. Everything it matches is logged as matching a
known malicious pattern, may auto-delete under the Auto-Delete Policy above, and
adds the uploader's address to the hostile-IP auto-block list.

The **site-policy denylist** (`WPS_Blocker::get_policy_banned_slugs()`) is for
clean, ordinary plugins an operator has chosen not to run — shipped defaults
`wp-file-manager` and `filebird`, plus operator additions from Settings. It
reuses the same five enforcement choke points (activation-link removal,
activation block, active-list filter, active-list scrub, upload prefilter) but
with its own event types (`policy_*`), its own wording ("banned by site
policy", never "malware"), and no hostile-IP consequence.

The rule, which is the reason the lists are separate: **never record a malware
attribution against a plugin that is not malware.** The event log is
tamper-evident and permanent; a false "known malicious pattern" entry against a
clean plugin is a lie the log then carries forever, and it undermines the one
thing this plugin's record is for. A policy refusal is a policy refusal, logged
as such. The `filebird` ban in particular has no security rationale — it is an
operator preference — and the wording must not imply otherwise.

Policy events sit at `warning` severity, not the `blocked -> high` band, and
every `policy_*` type is registered explicitly in `WPS_Utils::event_severity()`
against its emitting call site. The denylist has an on/off switch
(`policy_ban_enabled`, default on) so a control that has no legitimate off state
is not created.

## Event Chain Append Invariant (1.4.63, CRIT-005)

`WPS_Event_Log::record()` serialises its append and reads the chain head from
the **table** (the newest chained row's `curr_hash`), never from the cached
`wps_event_chain` option. Two invariants that must not be undone:

- The append runs inside an exclusion (advisory `GET_LOCK`, or a transaction
  with the tail row `FOR UPDATE` as fallback). The lock is released, and an
  incomplete append rolled back, in a `finally`. Never advance the anchor
  without its event row; never insert an event that cannot attach to the
  committed head.
- The head is authoritative from the table, not the option. The option is a
  cache; treating it as the source of truth is what allowed a fork to
  propagate. Any future change that reintroduces "read head from the anchor
  option" reopens CRIT-005.

The fix is preventive. Repairing an already-forked historical chain
(re-anchoring) is separate, unbuilt work. Verification: `harness/crit005-logic.php`
covers the logic in any environment; `harness/crit005-concurrency.sh` covers
real-database concurrency and must be run on staging before a production deploy.

### INDICATOR_VERSION discipline vs the phase release checklist

The Phase 2 brief's release checklist lists `INDICATOR_VERSION` under "update
all". That instruction is generic. `INDICATOR_VERSION` moves only when the
indicator lists change (the rule above under Indicator Data). CRIT-005 touches
the event store, not indicators, so 1.4.63 leaves `INDICATOR_VERSION` untouched.
Where the generic checklist and this rule disagree for a non-indicator change,
this rule wins, and the decision is recorded here rather than bumping a version
that tracks something that did not change.

## Tooling

Pre-release validation is automated in:

```text
tools/verify-package.ps1
```

It runs the parser fallback on every PHP and admin JS file, asserts version-marker consistency across the four declared release files, checks the required directory layout, and refuses any stray markdown at the package root. Run it from the repo root before building the release ZIP.

## Asset Layout

Admin UI assets are enqueued from:

```text
assets/css/admin.css
assets/js/admin.js
```

Large inline admin CSS or JavaScript blocks should not be added back to `includes/class-admin.php`.

## Admin Tab Layout

The admin tabs follow the incident-response workflow, grouped into an action band, an observability band, and a configuration band (regrouped in 1.3.66; the `logs` tab was added in 1.3.73):

```text
Overview -> Forensics -> Remediation -> Hardening -> Diagnostics -> Events -> Logs -> Settings
```

`$tab_group_starts` marks the first tab of the observability band (`diagnostics`) and the configuration band (`settings`).

Backward-compatible aliases should continue to resolve:

```text
dashboard -> overview
analytics -> diagnostics
log -> events
```

Operational telemetry such as hostile IP blocks belongs in Diagnostics. The Logs tab is read-only server access/error-log inspection (campaign-indicator scan and the automated-login signature) and must never expose a path parameter that escapes the code-defined, allow-listed log roots. Settings should stay focused on custom indicators, behavior toggles, and danger-zone maintenance actions.

The Diagnostics tab owns the Source Trace panel. Source Trace should correlate retained security events, active hostile IP blocks, cached scan findings, cached Forensics artifacts, and trace-focused SSH grep commands without running new heavy scans on normal page load. If the cached Forensics report is missing, the panel should say so and still render event/IP evidence.

Database option rows in Source Trace must be described carefully. WordPress `wp_options` has no native creation timestamp, so option anomalies should be shown at Forensics report time unless a scan, cleanup, or event log row captured a more precise time.

Hardening tab AJAX feedback should use compact status badges rather than raw inline text next to action buttons. Successful one-way Apply actions may hide the original button until the page reloads and the persisted state is rendered.

Removing `.htaccess` hardening rules must delete the whole WordPress marker block, not leave an empty marker pair. Status checks should verify expected rule lines, not only marker headers.

Each hardening action must have only one status target ID. Avoid standalone hidden status elements that duplicate the generated `*-msg` badge ID, because inline `display:none` can hide AJAX success feedback.

## Log Location

New event logs are written inside the plugin folder:

```text
logs/events.php
```

The legacy log path is migration-only:

```text
wp-content/wps-security/events.php
```

## Upload Pathway Protection

Blocked malware upload events should preserve enough context to trace the pathway:

- request route / URI
- request method
- AJAX action
- REST route
- referrer
- current WordPress user and roles
- upload, plugin-install, and manage-options capability state

ZIP uploads are restricted by default to trusted administrator WordPress upload routes. Unsafe front-end, REST, custom `admin-ajax.php`, file-manager, or unauthenticated ZIP upload pathways should be blocked and logged as `upload_path_blocked`.

All ZIP uploads, including trusted administrator routes, should still be inspected for known malware content before acceptance. The ZIP content guard should check entry names, known suffix patterns, built-in/custom PHP hashes, and high-confidence campaign payload markers.

WP Perf Shield release ZIPs are a special false-positive case because the plugin source legitimately contains malware indicators. A self-package exemption must stay narrow: only a valid `wp-perf-shield/` package with the real main plugin file may ignore indicator strings in known WP Perf Shield PHP files, and unknown extra PHP files inside that package must still be inspected.

## Confirmed ClickFix Indicators

Built-in indicators should cover the currently confirmed plugin disguise families:

```text
wp-perf-analytics
session-manager
native-render-toolkit
total-render-profiler
total-render-toolkit
pro-font-optimizer
site-speed-insights
advanced-asset-insights
page-seo-toolkit
starter-image-guard
```

When a new sample is confirmed, update the blocker slugs, suffix patterns, hashes, scanner signatures, database option cleanup allowlists, forensics trace terms, hardening folder blocks, and both WordPress and project documentation.

### On-chain (EtherHiding) and obfuscated-loader detection

Recent variants no longer rely only on a plain C2. Two detection layers cover the evolution:

- **EtherHiding.** `WPS_Indicators::etherhiding_indicators()` lists the public Binance Smart Chain and Polygon JSON-RPC hosts plus the `eth_call` / `eth_getStorageAt` read primitives. `check_clickfix_c2_in_options()` flags a decoded `wp_<10hex>_cfg` payload that fetches its next stage from an on-chain contract. The rembat.my sample read its stage from a Polygon contract (`0x08207B087F61d7e95E441E15fd6d40BEfd6eD308`, selector `0x38bcdc1c`) and set dismissal cookies `_cf_verified` and `_wp_perf_ok`.
- **Self-decoding loader.** `check_obfuscated_loader()` detects the XOR-plus-base64 loader by structure (a base64 blob, a `charCodeAt(...)^key` loop, a TextDecoder/fromCharCode pass, and a `new Function()`/`eval()` sink), then base64-decodes and brute-forces the single-byte XOR to recover the payload, key, contract, and chain hosts. Structure-based matching is required because literal signatures keyed to one sample's variable names are defeated by per-variant name randomisation and XOR-key rotation (keys 84, 113, 114, 179 seen to date).

## Confirmed RAT Placement

The confirmed second-stage RAT/backdoor family may also hide outside normal plugin folders. Built-in checks should cover:

```text
wp-content/languages/wp-locale-handler.php
wp-content/mu-plugins/session-manager.php
wp-content/cache/object-cache-handler.php
```

Language-directory checks must allow legitimate WordPress `.l10n.php` translation files while still flagging arbitrary PHP backdoors such as `wp-locale-handler.php`.

## wp-config.php Cleanup

`wp-config.php` cleanup must stay targeted and reversible:

- detect only known removable executable payloads, suspicious local/remote includes, option-loader restorers, and campaign markers
- create a backup before writing
- use atomic temp-file replacement
- refuse to write if the cleaned result fails sanity checks for `DB_NAME`, `DB_USER`, `ABSPATH`, and `wp-settings.php`
- update the stored clean baseline only after a successful cleanup
- log what was removed

A plain hash mismatch is not enough to clean automatically. If no removable pattern is found, the operator must review the file manually or reset the baseline only after confirming the edit was intentional.

## Release Version Updates

For every packaged release, update:

- `wp-perf-shield.php` plugin header `Version`
- `WPS_VERSION`
- `readme.txt` `Stable tag`
- `doc/readme.md` current version
- `doc/upgrading.md`
- `doc/changelog.md`

## ZIP Packaging

The release ZIP must contain a single root folder:

```text
wp-perf-shield/
```

All entry paths should use forward slashes.

## Security Audit History

The plugin underwent its first formal security audit at version 1.3.57 against the WordPress theme/plugin security audit and remediation YAML protocol.

### 1.3.57 audit summary (2026-05-07)

| Metric | Count |
|---|---|
| Confirmed findings | 6 |
| Implemented fixes | 5 |
| Proposed fixes | 1 |
| False positives | 4 |
| Residual risks | 1 |

#### Confirmed findings

| ID | Category | Severity | Status | Component |
|---|---|---|---|---|
| WPSEC-001 | privilege-escalation | medium | fixed | `WPS_Remediation_Controller::delete_plugin_folder()` containment narrowed from `WP_CONTENT_DIR` to `WP_PLUGIN_DIR` |
| WPSEC-002 | sensitive-file-exposure | low | fixed | `tools/index.php` placeholder + `tools/.htaccess` Deny from all added |
| WPSEC-003 | access-control (defense-in-depth) | informational | fixed | `delete_file()` protected basename list extended (.htaccess, .user.ini, wp-cron.php, wp-trackback.php, wp-comments-post.php) |
| WPSEC-004 | ssrf (defense-in-depth) | informational | fixed | All 4 runtime `wp_remote_get` call sites switched to `wp_safe_remote_get` |
| WPSEC-005 | ssrf (defense-in-depth) | informational | fixed | `$wp_version` validated against `^\d+\.\d+(\.\d+)?$` before URL interpolation in `clean_login` and `clean_cron` |
| WPSEC-006 | misconfiguration | informational | proposed (clarified) | `tools/update-indicators.php` STUB clarified with explicit early-exit guard against the example.com placeholder |

#### False positives verified

- **Three non-prepared `$wpdb` queries** at `class-forensics.php:833`, `class-remediation-controller.php:611`, and `class-remediation-controller.php:625` — all constant SQL with no user-input interpolation.
- **278 admin-page output points** — every dynamic value flows through an appropriate escape function (`esc_html`, `esc_attr`, `esc_url`, `esc_textarea`, `wp_kses`, `sanitize_html_class`, or `wp_json_encode`). Apparent unescaped echoes resolve to ternary-of-constants, helper-function-internal escaping, or static literals.
- **All 26 AJAX handlers** verified to invoke `WPS_Utils::guard_request()` with appropriate capability before any state change.
- **Settings sanitisation chain** (`sanitize_slug_list` via `sanitize_title()`, `sanitize_hash_list` via strict hex regex) verified end-to-end.

#### Residual risks

- **`delete_file` ABSPATH-wide trust for `manage_options` admins.** Architectural choice consistent with WordPress's `manage_options` threat model. A compromised admin session can delete any non-protected file inside `ABSPATH` via this handler. Mitigated by the extended protected basename list (WPSEC-003) but not eliminated. Removing the handler entirely is not desirable because malware deletion is the plugin's primary purpose.

### Audit history table

| Version | Date | Type | Confirmed | Fixed | Proposed | False positives |
|---|---|---|---|---|---|---|
| 1.3.57 | 2026-05-07 | Formal protocol audit | 6 | 5 | 1 | 4 |
| 1.3.58 | 2026-05-07 | Incremental IoC + detection-capability addition (post-audit) | 0 (no new vulnerabilities) | n/a | n/a | n/a |
| 1.3.59 | 2026-05-07 | Incremental IoC catalogue update (post-audit) | 0 (no new vulnerabilities) | n/a | n/a | n/a |
| 1.3.60 | 2026-05-07 | Documentation-only public-disclosure redaction (post-audit) | 0 (no new vulnerabilities) | n/a | n/a | n/a |
| 1.3.61 | 2026-05-08 | UI affordance: inline "Delete this path" button on Overview findings (post-audit) | 0 (no new vulnerabilities; reuses existing `wps_delete_file` handler with all 1.3.57 safety constraints intact) | n/a | n/a | n/a |
| 1.3.62 | 2026-05-08 | Selective uptake of third-party static review of 1.3.59: child-theme functions.php cleanup, log-path docs correction, admin JS escape-at-call-site hardening, terminology slip-back correction; three review recommendations explicitly NOT acted on (per-path nonce binding, per-finding delete_scope plumbing, tools/update-indicators.php removal) because they contradict prior 1.3.57 audit decisions | 4 valid findings actioned; 3 review recommendations declined with documented rationale | 4 fixed | 0 deferred under residual-risk acceptance | 5 contested-or-already-mitigated |
| 1.3.63 | 2026-05-08 | UI polish only: admin header pill restyling, severity-aware issue alert (post-audit) | 0 (no new vulnerabilities; CSS-only and PHP-template-only change, no new handlers or capabilities) | n/a | n/a | n/a |
| 1.3.64 | 2026-05-08 | CSS cascade-fix: 1.3.63's severity-aware issue-alert design did not actually render because the existing `.wps-app > h1 span !important` rule overrode it. Rescoped to `.wps-app > h1 .wps-version-pill` so the issue-alert rules take effect. Tagline updated. (post-audit) | 0 (no new vulnerabilities; CSS-only correction of a 1.3.63 visual regression) | n/a | n/a | n/a |
| 1.3.65 | 2026-05-08 | Bug-fix: the inline "Delete this path" button added in 1.3.61 has not actually worked since it shipped. The onclick attribute was malformed because `wp_json_encode()` output was interpolated directly into a double-quoted attribute, so the browser parsed the attribute as truncated and never installed the click handler. Fix matches the working Forensics-tab pattern: build the onclick string PHP-side, pass through `esc_attr()`. Process gap recorded: release-checklist adds end-to-end render-and-parse verification for admin-UI changes. (post-audit) | 0 (no new vulnerabilities; UI-rendering bug-fix for a feature that had never functioned) | n/a | n/a | n/a |
| 1.3.66 | 2026-05-08 | UX release: tab order regrouped into action / observability / config bands with a CSS-inset separator marking band boundaries; hover state added to non-active tabs (pre-1.3.66 tabs read as flat labels); active tab gets a 2px teal inset bottom accent; "Latest signals" sub-section removed from Diagnostics (duplicated metrics shown elsewhere in the tab + visually echoed Events tab). First release to apply the 1.3.65 process-gap fix: rendered output verified by static-render harness before build. (post-audit) | 0 (no new vulnerabilities; CSS + PHP-template-only UX changes, no new handlers or capabilities) | n/a | n/a | n/a |
| 1.3.67 | 2026-05-08 | Feature release implementing the surgical-edit "Clean injection" buttons deferred since 1.3.61. Two new AJAX handlers (`wps_clean_wfwaf_include`, `wps_clean_user_ini_prepend`) operate on hard-coded single-file targets (ABSPATH/wordfence-waf.php and ABSPATH/.user.ini) with backup-on-edit, atomic write, pre-check, mid-line guard, and post-write verification. Recognises bare-include and file_exists-wrapper malware idioms; refuses freeform mid-line code and mismatched-path wrappers. .htaccess findings deliberately not given a Clean button (operator-edited, unpredictable interaction with surgical line-removal). Verification: 27/27 cleaning-logic cases pass, 11/11 button-render cases pass. Second release to apply the static-render verification discipline. (post-audit) | 0 (no new vulnerabilities; new handlers operate within tighter path boundaries than `wps_delete_file`'s WPSEC-001 residual risk; same `manage_options` trust requirement) | n/a | n/a | n/a |

### Public-disclosure decision (1.3.60)

A protocol-compliance audit at 1.3.60 found that all five victim-site domain names were named explicitly across the four public-facing documentation files. The protocol's `public-disclosure-guidance` section advises against publishing customer data and private production paths in public docs. The named domains are the operator's own sites, so the strict customer-data definition does not apply, but the explicit listing publishes a connection between the operator's network and a malware compromise.

The decision recorded here is to redact the public-facing documentation files (positional references replace explicit domain names) while keeping the domain literals in code where they are functionally required (`WPS_Indicators::known_rat_victim_sites()` for XOR-seed lookup) and in inline source comments adjacent to per-victim hash entries (technical annotations operators see when reading the code, not public-facing prose). The forensic insights, timing context, and detection capability are preserved across the redaction; only the identification of the specific operator's network is removed from public surfaces.

This decision applies prospectively to all future releases. Public documentation files refer to the catalogued victim sites by position only. Code files retain the literal domain values and the explanatory comment block that documents why they are present.

### Post-1.3.57 incremental updates

Versions 1.3.58 and 1.3.59 are not formal protocol re-audits; they are incremental IoC catalogue and detection-capability updates that ship between formal audit cycles. The protocol's `release-checklist` requirements (version markers, changelog entry, upgrading entry, readme update, ssot update, regression tests where applicable) apply to these incremental releases. The protocol's full vulnerability-checks methodology is not re-run on every incremental release  it is re-run when the codebase changes substantively enough to warrant it, or on a calendar cadence the operator chooses.

**1.3.58** added two changes. First, specific catch for a new ClickFix variant `auto-content-profiler-0b8d.php` that used a previously-unseen variable-concatenation evasion technique. Slug, hash, suffix pattern, DB option key, and class-name signature all added to the per-prefix detection layer. Second, a new generic shape-based detector `WPS_Scanner::check_clickfix_shape_heuristic()` that recognises any fake-plugin-folder ClickFix variant by structural traits rather than per-prefix string matching: single-file shape, file size 5-15 KB, `developer.wordpress.org/plugins/` Plugin URI, hex-suffix class name, `wp_<10hex>_cfg` option literal, late-priority `add_action`. Score 5-6 = high severity auto_delete, score 4 = high review-only, score 3 = medium review-only. The shape heuristic raises the operator's cost: rotating prefixes no longer evades detection because the heuristic does not depend on the prefix at all. 12/12 behavioural test assertions passed across both per-prefix and heuristic layers.

**1.3.59** added a single hash entry. New wp-locale-handler RAT family build captured at `.well-known/pki-validation/cert-check.php` with XOR seed for the fifth victim site. MD5 `c053446a3916beb41df3e3428c085a3c` and SHA-256 `0c1d67c3d5036b5ced4f761d05326b8a3e98946ca89fb5fc0bd276c0b095cff0` added to the blocked hashes list. The captured file was already caught by existing detection layers (the 1.3.45+ `.well-known/` PHP scanner plus the family content signatures); the hash addition is defence-in-depth, not the primary catch. After 1.3.59 the catalogue is symmetric: every victim site has both the parent-location wp-locale-handler.php and the sibling-location cert-check.php hashes catalogued.

**1.3.60** is documentation-only. No code changes. Public-facing documentation surfaces (`readme.txt`, `doc/changelog.md`, `doc/upgrading.md`, `doc/ssot.md`) had their explicit victim-site domain references redacted to positional descriptors ("first/second/third/fourth/fifth victim site in the captured corpus"). Domains remain in the IoC catalogue source code where they are functionally required for XOR-seed lookup, and in inline comments adjacent to per-victim hashes where they serve as technical annotations. Decision recorded in the "Public-disclosure decision (1.3.60)" subsection above.

**1.3.61** is a UI affordance, not a detection change. Closes a real UX gap on the Overview tab: findings with the shape `Hidden development directory under wp-content/` and `Hidden directory under wp-content/ (no benign explanation)` now render an inline "Delete this path" button when the path is contained within `WP_PLUGIN_DIR`. The scanner gates button rendering by populating a new `delete_path` field on findings only when the path passes the safe-boundary check. The button calls the existing `wps_delete_file` AJAX handler  no new handler, no new capability, no widening of path acceptance. The 1.3.57 audit's residual-risk acceptance for `wps_delete_file` (ABSPATH-wide trust under `manage_options`) is unchanged; the new `delete_path` field is a UI gating mechanism, not a security boundary. Findings outside `WP_PLUGIN_DIR` (ABSPATH-root files like `.wp-config-cache.php`, `.user.ini` directives, mu-plugins backdoors, surgical-edit cases like the `wordfence-waf.php` auto_prepend hijack) continue to render instruction-only text because their remediation requires either editing a legitimate file or admin-level filesystem access. 8/8 behavioural test assertions passed: `.vscode/` and `.helpers/` inside plugin folders get `delete_path` populated at the correct severities (medium, high), and `.idea/` outside `WP_PLUGIN_DIR` is still flagged but with empty `delete_path` so no button renders for it. Surgical-edit button for `wordfence-waf.php` and similar findings is deferred to 1.3.62 pending operator review.

**1.3.62** is a selective uptake of findings from a third-party static code review of 1.3.59. The review was provided to the operator as a structured YAML report claiming nine items, framed as a release-blocker recommendation. Independent verification of each finding against the actual codebase (line numbers, code patterns, prior audit decisions documented in this SSOT) found mixed quality: four findings were genuinely valid bugs or worthwhile hardening, three findings contradicted documented 1.3.57 audit decisions, and two findings were mischaracterised in severity. This release acts on the four valid findings only and explicitly declines the three audit-contradicting recommendations, with the rationale recorded in `doc/changelog.md` 1.3.62 entry. The four valid findings are: (1) `clean_functions()` previously inspected only the parent theme's `functions.php` and missed credential-harvester injections in child themes  fixed to walk both parent and child; (2) the readme's log-location text said `wp-content/uploads/wps-logs/` but the actual `WPS_LOG_DIR` constant resolves to `wp-content/plugins/wp-perf-shield/logs/`  documentation corrected to match code; (3) admin JavaScript interpolated raw `r.data.message` and `r.data.error` into HTML at four call sites where the server-supplied content includes user-influenced fragments (attachment titles, option keys, file basenames)  escapes added at the vulnerable sites and the per-finding success replacement converted to a jQuery element constructor; (4) selective correction of "operator-target site" terminology slip-backs in 1.3.59 entries while leaving historical text and code-symbol references intact. The three declined recommendations  per-path nonce binding for `delete_file()`, per-finding `delete_scope` plumbing, and removal of `tools/update-indicators.php` from the release ZIP  each contradicted explicit 1.3.57 audit decisions (residual-risk acceptance, scope-implicit architecture, WPSEC-006 stub guard) and reopening any of them warrants a deliberate audit cycle rather than a third-party-review-driven point fix.

**1.3.63** is UI polish only. No detection changes. No security boundary changes. No scanner changes. The release closes a single visual-design issue raised against the admin header pills: pre-1.3.63 the version pill (informational) and the issue-count alert (actionable) shared near-identical pill shapes and sizes, plus the alert's `#c00` red rendered as muted lavender on JPEG-compressed screenshots when sitting against the magenta-tinted right portion of the gradient header. The redesign keeps both pills in their original position (the right side of the header has the "Focused malware defense." tagline so the alert cannot move there without colliding) but differentiates them visually: the version pill becomes a smaller rounded-corner rectangle in muted gray, and the issue alert becomes a fully rounded pill with severity-aware colour (red for critical, orange for high, amber for medium, blue for low), bolder weight, larger padding, drop shadow for depth on the gradient, and a subtle pulsing dot that respects `prefers-reduced-motion: reduce`. The severity displayed is the highest present in the cached findings array; findings without a severity field fall back to medium-amber rather than red so the UI does not over-claim. CSS-only and PHP-template-only change. The 1.3.57 audit's residual-risk acceptances stand and the 1.3.61 inline-delete-button machinery is unaffected.

**1.3.64** is a CSS correction to 1.3.63 plus a tagline change. Post-1.3.63 inspection of the cascade  prompted by an unrelated tagline question  found that 1.3.63's severity-aware issue-alert design did not actually render. The pre-existing rule `.wps-app > h1 span` with `!important` declarations on `background`, `color`, `font-size`, `font-weight`, and `padding` continued to match both header pills (both are `<span>` children of the `<h1>`) and silently overrode the new `.wps-issue-alert` and `.wps-sev-*` rules at the bottom of the stylesheet. The 1.3.63 build deployed visually identically to 1.3.62 even though its changelog claimed otherwise. 1.3.64 fixes this by rescoping the broad selector from `.wps-app > h1 span` to `.wps-app > h1 .wps-version-pill` so the version pill keeps its existing white-gradient teal-text appearance and the issue alert's severity-aware colour rules take effect uncontested. Two cleanup items ride along: the dead attribute selector `.wps-app > h1 span[style*="background:#c00"]` (which used to override the OLD inline-styled issue pill's red with translucent pink, producing the lavender appearance visible in pre-1.3.63 screenshots, but became inert in 1.3.63 when inline styles were removed) is dropped, and a redundant `.wps-version-pill` rule that 1.3.63 added at the bottom of the file is dropped because the rescoped existing rule is now the single source of truth for version-pill styling. The release also changes the header tagline from "Focused malware defense." to "Forensic WordPress malware defence."  specifies WordPress as the threat surface, signals depth of analysis via "Forensic", and uses British spelling matching the rest of the project's editorial register. CSS-only release; no PHP changes, no scanner changes, no detection changes. The 1.3.63 documentation is left intact as a record of design intent rather than retroactively edited; the 1.3.64 documentation records the actual delivery. This two-release "intent then delivery" pattern is the correct way to handle a buggy release once the build has shipped.

**1.3.65** is a bug-fix release for the inline "Delete this path" button on Overview findings. The button was added in 1.3.61 and described as working in that release's changelog, but operator inspection on 8 May 2026 surfaced that clicking it did nothing  no AJAX call, no error, no feedback. Independent inspection of the rendered HTML found the cause: the 1.3.61 PHP template interpolated `wp_json_encode()` output directly into a double-quoted onclick attribute. The JSON-encoded values contain double-quote string delimiters, which closed the outer onclick attribute prematurely. The browser parsed the rendered onclick as `wpsForensicAct(this, 'wps_delete_file', { path: ` (truncated at the first inner double-quote) and treated everything after as separate, malformed HTML. The click handler was never installed. The button has been silently inert across four releases (1.3.61, 1.3.62, 1.3.63, 1.3.64) because none of the intervening releases re-rendered the affected component or verified the resulting HTML parsed as intended. The fix matches the working pattern used by the Forensics-tab `forensic_action_button()` helper since 1.3.x: build the onclick JS expression as a single PHP string, then pass it through `esc_attr()` before placing it into the rendered HTML. `esc_attr()` converts inner `"` to `&quot;`, which the browser HTML-decodes back to `"` when populating the JS engine's argument list. End-to-end test simulating the full pipeline (PHP template render → browser HTML-attribute extraction → JS engine argument parse) passes 4/4 across the operator-reported `seo-by-rank-math/vendor/donatj/phpuseragentparser/.helpers` path and three additional cases covering apostrophe and ampersand edge cases. This is the second consecutive release shipping a feature that did not work as documented (the 1.3.63 → 1.3.64 sequence had the same shape: design intent recorded, feature shipped, regression caught after operator inspection, fix in the next release). Two consecutive instances of this pattern is not coincidence  it is the absence of an end-to-end verification step in the release-checklist. For releases that modify rendered admin UI from this point forward, the release-checklist adds three new steps: (1) render the affected component to a static HTML string in a PHP test harness, (2) extract the rendered attribute or text in question and confirm it parses as the intended structure, (3) for interactive elements with onclick or similar handlers, confirm the JS-engine-visible argument list matches the intended values after the browser performs its HTML-decoding pass. This will not catch every class of regression but it would have caught both 1.3.61's onclick bug and 1.3.63's CSS specificity bug.

**1.3.66** is a UX release answering three operator concerns raised in a single feedback round: (1) the tab order interleaved observability tabs (Diagnostics, Events) among action tabs (Overview, Forensics, Remediation, Hardening) for no functional reason; (2) the tabs were styled too flat to read as clickable controls; (3) the Diagnostics tab's "Latest signals" sub-section visually echoed the Events tab without distinct value. The fixes ship together because they touch overlapping surfaces and were raised together. Tab regrouping: new order groups by purpose (action band, observability band, configuration band) and the first tab of each band after the leading band gets a `wps-tab-group-start` CSS class via a new `$tab_group_starts` array in `class-admin.php`. The renderer adds the class to qualifying tabs and `admin.css` turns it into a 14px left margin and a 1px inset left edge using the existing `--wps-line` variable, marking band boundaries without consuming additional vertical space. Hover state: non-active tabs gain a `:hover` and `:focus-visible` treatment with a subtle background tint and a colour shift from muted gray to primary ink, with 150ms transitions; the `:not(.nav-tab-active)` guard prevents competition with the active state. Active accent: the active tab gains a 2px teal inset bottom accent at `#14565d` (matching the brand teal used in the version pill text colour); inset shadows do not clip with the rounded outer container. A combined `.nav-tab-active.wps-tab-group-start` selector handles the case where the active tab is also a band-start tab so all three shadow components layer correctly. Latest signals removal: the four event-derived metrics (last attack signal, last clearance, unique attacker IPs, current blocked IP attempts) duplicated information already shown by other Diagnostics sections and the operator reported the card visually echoed the Events tab; the card is removed and the parent grid simplifies to a single full-width container so "Security posture" spans the row. `$analytics` is still computed because other Diagnostics sections consume its sub-keys. This is the first release to apply the 1.3.65 process-gap fix: rendered output was verified by static-render harness across all seven `active_tab` values with six assertions before the build was packaged, and the CSS cascade was walked manually with specificity calculations for each new selector. Both the static-render and cascade-walk steps are now standing release-checklist items for any release that modifies admin UI.

**1.3.67** is a feature release implementing the surgical-edit "Clean injection" buttons that have been deferred since 1.3.61 across multiple roadmap entries. This is the largest functional change since 1.3.61's inline-delete-button addition. Two new AJAX handlers (`wps_clean_wfwaf_include` and `wps_clean_user_ini_prepend`) close the two non-payload-file gaps in the auto_prepend_file hijack chain: a malicious `include` line added to wordfence-waf.php at ABSPATH (the wrapper hijack), and a malicious `auto_prepend_file = <path>` directive added to .user.ini at ABSPATH. Both files are legitimate and have other content, so deletion is wrong; surgical line-removal is the correct remediation. The third piece of the chain (the payload PHP file at the include target) is already auto-deleted by the existing scanner+remediation flow when it sits at ABSPATH-root with a known toolkit filename. .htaccess-based variants of the directive case are deliberately NOT covered because operator-edited .htaccess files contain heterogeneous rewrite rules whose interaction with surgical line-removal is unpredictable. The cleaning logic is extracted into pure functions (`clean_wfwaf_compute()` and `clean_user_ini_compute()`) and was tested in isolation against 27 input cases covering typical wrapped form, bare include, idempotent re-run, brace-wrapped form, mismatched-paths refusal, freeform mid-line refusal, multi-include sequence, CRLF line endings, uppercase keywords, regex-metachar paths, already-clean file (wordfence-waf.php), plus standard quoted form, bare unquoted form, idempotent, uppercase directive, empty file (.user.ini). All 27 passed. The button rendering was tested through the static-render-then-parse pipeline introduced in the 1.3.65 process-gap fix: 8 input cases including paths with apostrophes, ampersands, and hidden-dot directories all produced well-formed onclick attributes that parsed back to the correct JS-engine argument list, and 3 suppression cases (remediated, unknown strategy, missing strategy) correctly produced empty render output. This is the second release to apply the static-render verification step. Each Clean handler implements six layers of safety: hard-coded single-file path (no path parameter from the client; materially narrower than the 1.3.57 residual-risk WPSEC-001 boundary on `wps_delete_file`), backup-on-edit (original copied to `<file>.wps-backup-<timestamp>` before any modification), atomic write (matching the established `clean_login`/`clean_functions`/`clean_cron` pattern), pre-check (stale findings produce "already clean" success without rewriting), mid-line guard (conservative refusal of unrecognised mixed-content lines), and post-write verification (re-read after rename, run the same regex against the new content, error and point at the backup if the offending content is still present). The 1.3.57 audit's residual-risk acceptances stand; both new handlers run under `manage_options` trust like `wps_delete_file`, but operate within tighter path boundaries.

**1.3.68 through 1.3.77** are recorded per-version in `doc/changelog.md`; in summary: Total Database Optimizer variant detection and a C2-in-options recovery check (1.3.68/1.3.69); directory-aware "Delete this path" remediation (1.3.70); the temporal drop-in integrity guard (1.3.71); the static campaign token and a second C2 host (1.3.72); the read-only server-log reader and Logs tab (1.3.73); writable-directory PHP detection (1.3.74); the research-driven trio of EtherHiding (BSC) detection, example.com plugin metadata-camouflage detection, and the automated-login access-log signature (1.3.75); the opt-in report-only Content-Security-Policy (1.3.76); and a `strtoupper()` fix so the three wp-config hardening toggles match the uppercase allowlist (1.3.77).

**1.3.78** is a detection release driven by a live sample from an infected Malaysian site (rembat.my). The captured injection is the same ClickFix family, evolved in two ways that defeated the existing literal signatures: variable names were randomised (defeating `s.charCodeAt(i)^k` / `new TextDecoder().decode(r)`), and the cleartext indicators (`_wp_perf_ok`, `eth_call`, the contract, the RPC hosts) sit inside the base64 blob where a raw substring scan cannot see them. The sample also moved EtherHiding from Binance Smart Chain to Polygon, reading its next stage from contract `0x08207B087F61d7e95E441E15fd6d40BEfd6eD308` (selector `0x38bcdc1c`) across six Polygon RPC nodes, after a desktop-only gate and an anti-debugger timing check. Two fixes: the six Polygon RPC hosts were added to `etherhiding_indicators()` and `eth_call` is now matched regardless of quote style (indicator version `1.3.78-1`); and a new pipeline check `check_obfuscated_loader()` detects the loader by structure (atob/base64_decode blob + `charCodeAt(...)^key` loop + TextDecoder/fromCharCode pass + `new Function()`/`eval()` sink), then base64-decodes and XOR-brute-forces the blob to recover the payload, key, contract, and chain hosts, treating a recovered campaign indicator as proof (critical) and structure-without-recovery as high for manual review. It does not auto-delete, because the block may be appended to a legitimate file. The cheap structural regex runs per file; the costly brute runs only on files that already match the full structure. Verified by brace-walk and by simulation against a synthetic dropper carrying the real blob with randomised names (gate fired, key 179 and all indicators/contract/hosts recovered, and a benign `base64_decode()`-only file did not trip the gate). `php -l` was not run in the build environment and must be run on the changed files before publishing.

**1.3.79 (two parallel branches), 1.3.80, and 1.3.81 (reconciliation).** The `1.3.79` label was used on two parallel branches for different detection work. One branch added structural admin-session-cookie-stealer detection (`check_admin_cookie_stealer()`, closing the renamed-`.wp-config-cache.php` gap and consuming the previously-dead `cookie_exfil_c2_domains()` accessor) and theme-modifying loader-plugin detection (`check_theme_loader_plugin()` for the `Plugin-<6-10 hex>` / theme-`css.js` family), plus catalogue entries `wp_1f20bc3f7f_cfg`, `wp_8447aa87d2_cfg`, `Cache Solutions`, and `WP Solutions`. The other branch added the RC4-obfuscated JS-injector / theme-`css.js` dropper detection (`check_obfuscated_js_injector()` plus the two indicator accessors) and then shipped `1.3.80`, a safety-stabilization release that removed the broad `addons`/`scr` plugin-folder substring deletion, introduced the two-tier uploads-PHP policy and active-theme critical-file protection, replaced prefix-`strpos` self/parent path checks with boundary-safe containment, made sub-critical css.js findings report `high` without auto-delete, scoped the core-checksum cache by WordPress version and locale, restricted the log reader, tightened the indicator-update CLI guard to `PHP_SAPI`, and added pre-write backups before `clean_login()` / `clean_functions()` / `clean_cron()`. Because the `1.3.80` package contained only the JS-injector lineage, the version numbers had collided. `1.3.81` reconciles the two: it takes the `1.3.80` safety build as the base and ports the cookie-stealer and theme-loader methods and the four catalogue entries onto it, without reintroducing any of the code `1.3.80` had deliberately changed. `WPS_Indicators::INDICATOR_VERSION` is `1.3.81-1` (the catalogue changed). `check_theme_loader_plugin()` and `check_obfuscated_js_injector()` may both fire on one `Plugin-<hex>` sample; this is intended defence in depth. Verified by `php -l` on all changed files, depth-0 brace balance, and a method-and-registration check (both ported methods declared once and registered once in `run()`, the JS-injector registration retained), and by real-sample simulation: the four structural detection methods were run (reflection harness, WordPress stubbed) against the confirmed forensic samples and all five were detected at auto-delete severity (the three single-file variants via the 6/6-trait shape heuristic; `Plugin-7e4eb3ff` via both `check_theme_loader_plugin()` and `check_obfuscated_js_injector()`; `.wp-config-cache.php` via `check_admin_cookie_stealer()` with the C2 host matched against `cookie_exfil_c2_domains()`), while the legitimate `.l10n.php` translation files and `advanced-cache.php` / `object-cache.php` drop-ins in the same dump were not flagged. A second sample batch was validated the same way: the three single-file variants (`pro-cache-scanner-6d52`, `site-speed-insights-d6e7`, `total-database-optimizer-9a95`) matched the 6/6-trait shape heuristic, and the two original injector-family captures (`Plugin-b45b652c`, `Plugin-45e0930c`) were each caught by both `check_theme_loader_plugin()` and `check_obfuscated_js_injector()`, confirming that the two independently-developed detectors cross-cover the whole `Plugin-<hex>` family. A third batch revalidated the broader single-file ClickFix prefix family (`advanced-asset-insights-ec06`, `auto-content-profiler-0b8d`, `site-speed-insights-d6e7`, `starter-image-guard-e9a2`, `total-render-toolkit-adae`): all five matched the shape heuristic at 6/6 traits regardless of slug prefix. A final batch covered the progenitor `wp-perf-analytics` family: the three renamed variants (`pro-font-optimizer-c88b`, `total-render-profiler-3753`, `native-render-toolkit-9401`) matched the shape heuristic at 6/6, while the two original captures (`wp-perf-analytics`, `wp-perf-analytics-d2e9`) scored 3/6 on the heuristic (the backstop is tuned for the later renamed shape) but were caught at critical/auto-delete by `check_php_signatures()` and blocked by `WPS_Blocker::is_blocked()` — confirming the layered design catches the progenitor by slug and signature even where the heuristic under-scores it.

**1.3.82 (persistence-layer structural detection).** Closed a renamed-copy gap on the access/persistence side, mirroring the structural approach used for the ClickFix loaders. A live `rest-api-compat` kit (an obfuscated MU plugin at `mu-plugins/rest-api-compat.php` plus `db.php` and `advanced-cache.php` re-droppers, renamed off the `session-manager.php` / `_sm_ac_v5` lineage with marker `_ac_63b990b9`) was missed by 1.3.81 except for a review-only publisher nudge. `check_wp_content_dropins()` now flags, independent of the session-manager identifiers, any audited drop-in that combines a decode call, a payload write, and a `mu-plugins` write target (critical/auto-delete); `check_mu_plugins()` now flags a non-whitelisted MU plugin matching the rolling-XOR deobfuscation-guard shape with a high cue count (critical/auto-delete). Auto-delete is gated on the `mu-plugins` write target and the rolling-XOR guard respectively, keeping the branches off legitimate cache/database drop-ins and MU plugins. Verified by `php -l`, a malware simulation (all three kit files caught at critical/auto-delete), and a false-positive simulation (legitimate drop-ins/MU plugins and two tricky fixtures left untouched by the new branches). `INDICATOR_VERSION` unchanged at `1.3.81-1` (no indicator-list edits).

**1.3.83 (externalized-payload detection and first non-ClickFix family).** Added `check_external_payload_loader()` for a new ClickFix delivery shape (a thin, obfuscated loader that reads and decodes a bundled non-PHP payload blob, e.g. page-image-scanner with `storage/state.pkg`), gated on three co-occurring cues (read+decode loader, opaque bundled blob, obfuscation tell) to keep it off legitimate compressed-data plugins. Also added `check_doorway_backdoor_kit()` and `doorway_kit_cue_score()`, the first detection of a family outside the `wp-perf-analytics` ClickFix campaign: the annealing/resweep doorway-spam + backdoor panel kit, identified by `core/` structural tells (misspelled `backdor` file counted double, hex-suffixed `panel_`/`filemanager_` files, the ipManager/linkManager/settingsManager module set) and removed with a protected-path guard that refuses to delete a real WordPress directory. Verified by `php -l`, a malware simulation (page-image-scanner and the annealing kit with its bundled resweep copy both flagged critical/auto-delete), and a false-positive simulation (a caching plugin with a `core/` subdir, a GeoIP plugin that gzinflates a bundled `.dat`, and a `core/modules/` framework left untouched). `INDICATOR_VERSION` unchanged at `1.3.81-1`. The annealing/resweep family shares no code or infrastructure with the ClickFix campaign; co-residence is suggestive but not proof of a shared operator.

**1.3.84 (Forensics batch-delete bugfix).** The base64-option delete handler (`delete_unknown_b64_option`) gates deletion on the option appearing in the cached `wps_forensics_report`, but on success it discarded the entire report with `delete_option`, invalidating every sibling finding still rendered on the page; the first deletion in a batch worked and all subsequent ones failed with "not in the current forensics report". The handler now splices only the deleted option out of `unknown_base64_options` and writes the report back with `update_option`, leaving the auto_prepend and cron findings intact. Verified by driving the real handler through a three-option report (all three sequential deletions succeed, report persists and shrinks by one entry each time, both the report-membership and core-option safety gates still hold). Server-side only; `INDICATOR_VERSION` unchanged at `1.3.81-1`.

**1.3.85 (confirmed base64 options as first-class confirmed malware).** The `unknown_base64_option` findings (values decoding to PHP source or the ClickFix outer-JS shape) were the only confirmed-malware class not auto-cleaned and only deletable one at a time, and were surfaced redundantly as non-actionable scan criticals that pointed to the Forensics tab. They are now treated like any other confirmed artefact: (a) auto-cleaned in the scan pass when the existing `auto_delete_enabled` toggle is on (default), with each live option re-validated via the new public `WPS_Forensics::confirm_base64_option_verdict()` immediately before deletion so a stale report cannot drive a delete; (b) a new bulk `delete_all_unknown_b64_options` handler + a "Delete all" button on the Forensics card for manual mode, reusing the single-handler's report-membership and core-option gates and the 1.3.84 report-splice contract; (c) one delete path, with the scan-results action text corrected from the Forensics-tab dead end. Auto-delete is gated behind the existing toggle, not unconditional. Verified by `php -l` (4 files), a node parse of admin.js, a six-case re-validation test (clickfix_js/php_payload classify; benign/core-safe/short/missing return empty), and a bulk-handler simulation (three deleted, injected `siteurl` skipped and intact, siblings preserved, report spliced; empty report errors). `INDICATOR_VERSION` unchanged at `1.3.81-1`.

**1.3.86 (known-bad DB options + malicious cron auto-clean; list unified).** Closed the consistency gap surfaced by the manual-button audit: the highest-confidence DB malware (catalogued known-bad wp_options keys) and malicious cron hooks were detect-only, removable only via the manual "Delete malicious DB options" button, while the lower-confidence heuristic base64 options already auto-cleaned (1.3.85). check_malicious_db_options now (a) reads WPS_Indicators::malware_option_keys() instead of a stale private inline copy that had drifted and was missing several catalogued _cfg variants, giving detection and deletion one source of truth; (b) deletes confirmed known-bad options in the scan pass when the existing auto_delete_enabled toggle is on, detect-only otherwise; (c) sweeps the cron array for the campaign's malicious hook substrings (same list as the manual handler), unscheduling matches when the toggle is on and flagging them otherwise. The manual button remains the off-toggle fallback; the wp-config/wp-login/functions/wp-cron repair buttons are deliberately left manual (they clean/replace essential and core files auto-delete refuses). Verified by php -l and a reflection simulation (toggle ON: three options incl. a previously-missed variant deleted, malicious cron hook unscheduled, core cron and siteurl untouched; toggle OFF: detect-only, nothing removed). INDICATOR_VERSION unchanged at 1.3.81-1 (class-wps-indicators.php lists not edited).

**1.3.87 (PHP-inventory drift tripwire  first anticipatory detector).** In response to novel strains slipping past shape/family-keyed detection, added the first family- and payload-agnostic detector: check_php_inventory_drift() baselines the PHP files in uploads and mu-plugins (realpath => sha256, autoload off, walk bounded at 100000 visited files, >2 MiB skipped) and flags any PHP file that appeared (critical) or changed (high) since the baseline, regardless of content. It establishes the baseline on first scan and flags nothing then (it cannot know which pre-existing files are clean; content detectors cover those, this covers what arrives afterwards). Watches uploads + mu-plugins specifically (high-signal, low-churn, and lacking temporal coverage) and deliberately NOT plugins/themes (legit update churn  that is the forthcoming checksum-integrity measure's job). Anomalies are review-only, never auto-deleted. A public establish_php_inventory_baseline() + a rebaseline_php_inventory AJAX handler + a "Reset PHP-inventory baseline" button on the Hardening tab (mirrors the drop-in baseline reset) re-capture the clean set after cleanup. Verified by php -l (3 files), a node parse of admin.js, and a reflection lifecycle sim (establish 2-file baseline silent; drop evil.php + modify loader.php -> NEW/critical + CHANGED/high, untouched file silent; re-baseline -> clean; non-PHP never tracked). INDICATOR_VERSION unchanged at 1.3.81-1. NEXT: plugin/theme checksum integrity (network-dependent, its own pass).

**1.3.88 (sample ultra-layout-scanner-15c8 validated; placeholder backstop extended).** New forensic sample: another externalized-payload fake plugin, cosmetically renamed from the page-image-scanner shape - 567-byte stub with a split-string require of app/class-init.php, which reads storage/config.bin, runs a chr(ord(...)) substitution loop and gzinflates it; opaque .bin blob; author "Net IO" (same shell author as catalogued wp_3093c104e2_cfg); developer.wordpress.org/plugins placeholder URI. Caught critical/auto-delete by the 1.3.83 check_external_payload_loader on all three cues with NO change required, confirming the technique-level detector generalises across renames. Separately closed the 1.3.83-noted gap: check_plugin_metadata_camouflage now also matches the developer.wordpress.org/plugins placeholder (previously example.* only), FP-guarded by requiring the developer. host so a legitimate wordpress.org/plugins/<slug>/ link is never flagged - a backstop for any future variant that drops one of the three externalized-payload cues. Verified by php -l and a harness run (sample flagged by both detectors; yoast real URI and wordpress.org/plugins dir link both clean). The sample has a per-instance hex slug and no wp_options persistence, so nothing added to the indicator catalogue; INDICATOR_VERSION unchanged at 1.3.81-1.

**1.3.89 (plugin integrity vs wordpress.org checksums  second anticipatory measure).** check_plugin_integrity() verifies each installed repo plugin against the official plugin-checksums API (downloads.wordpress.org/plugin-checksums/<slug>/<version>.json), flagging PHP files that are MODIFIED (hash matches no official value  injection into a real plugin) or EXTRA (PHP absent from the distribution  planted file), both critical and review-only (a tampered repo file should be reinstalled, not deleted). Complements the 1.3.87 new-file tripwire by catching in-place modifications, and needs no clean baseline since the reference is the official distribution. Handles the API's per-file hash whitelist (match-any, for ZIPs rebuilt at different SVN revisions), prefers sha256 with md5 fallback, verifies only executable files (skips the readme/text soft changes wp-cli ignores by default), skips premium/custom plugins with no .org checksums, caches checksums a week and caps network fetches per scan. Built with the network fetch isolated; the parse and compare core is unit-tested (single/array/md5 hashes, malformed entries, clean pass, tampered=MODIFIED, planted=EXTRA, whitelist match-any, non-PHP soft changes and non-PHP extras ignored, header version read). The live .org fetch could not be exercised in the harness (no outbound access) and needs confirmation on a real install. Themes deliberately out of scope (no .org theme checksum API; would need zip-download-and-hash). INDICATOR_VERSION unchanged at 1.3.81-1.

**1.3.90 (premium-plugin integrity blind-spot awareness + checksum-cache fix).** Prompted by the June 2026 ShapedPlugin supply-chain compromise (Wordfence): backdoored Pro plugins delivered via the vendor's EDD channel, free .org builds clean. Because checksum integrity (plugin 1.3.89, core 1.3.47/48) compares against the wordpress.org distribution, it is structurally blind to premium plugins with no public baseline. check_plugin_integrity() now collects the plugins it cannot verify (definitive 404 from the checksums endpoint = not a repo plugin) and emits ONE low-severity awareness finding listing them, with a note that a vendor-channel supply-chain compromise would not be caught by integrity checking. Low severity is deliberate  the scan emails only on 'critical', so this adds no inbox noise (gate confirmed at class-scanner.php run()). Also fixed a latent bug in get_plugin_checksums: a transient failure (is_wp_error, or 403/429/5xx) was negative-cached as 'none' for a week, suppressing verification after a brief outage and able to mislabel a verifiable plugin as premium; now only 404/410 caches 'none', transient failures return uncached and retry next scan. The ShapedPlugin loader and install-persistent.php exfil stage are deliberately NOT detected from the news write-up (would invite FPs; needs the real sample); the persistence half (dropped webshell, second-stage plugin on disk, hidden admins) is already covered, and file-level scanning is not fooled by the admin-plugin-list hiding. Verified by php -l and a stubbed-network harness (clean repo not listed; 404 premium listed once + negative-cached; transient failure not listed + left retryable); modified/extra-PHP critical paths unchanged from 1.3.89. INDICATOR_VERSION unchanged at 1.3.81-1.

**1.3.91 (doorway-kit re-drop awareness; samples pacifiers_e83dd2 + donation_c9385e).** Two fresh webroot samples reported as "keep appearing in root": both are the same annealing/resweep doorway+backdoor kit as 1.3.83 (core/ with backdor_<hex>.php, panel_<hex>.php, filemanager_<hex>.php, ipManager/linkManager/settingsManager, bundled markrogoyski ipv4-subnet-calculator, words_*.text doorway corpus, random-word entry stubs that include core/core.php), re-dropped under different random folder names with re-randomised hex filenames. Empirically re-confirmed that check_doorway_backdoor_kit() already detects and auto-deletes the kit at ABSPATH root (path_is_inside guard does not protect a malicious subfolder of the webroot), so detection is not the gap  the kit returns because an external re-dropper re-plants it. Made the detector recurrence-aware: core.php is byte-identical across drops (and embeds neither the folder name nor the hex suffixes), so its sha256 is a stable fingerprint; the detector now records the fingerprint + the set of distinct folder names in a new option wps_doorway_kit_history (autoload off, pruned to 20 fingerprints x 50 names), and when the same kit reappears under a new name it escalates the finding to 'RE-DROPPED Nx - active re-dropper present' and switches guidance from delete-the-directory to hunt-the-dropper (cron/system cron, mu-plugins, wp-config/auto_prepend, co-resident shell). Counting is by distinct name so a lingering single instance (auto-delete off) does not inflate it; auto-delete behaviour is unchanged. Verified by php -l and a 3-scan harness (first sighting normal; same-name no inflation; renamed folder = RE-DROPPED 2x with hunt guidance). Known follow-up: check_redrop still scans wp-content only, not ABSPATH root. INDICATOR_VERSION unchanged at 1.3.81-1.

**1.3.92 (in-plugin log forensics; honest SSH panel).** Prompted by the question of why log queries were presented as SSH-only. The plugin already greps every readable log in PHP via WPS_Log_Reader (discover() + IOC match) in the Logs tab, so "SSH required" overstated it, and the attack-window file find never needed a shell at all (pure mtime walk). Added WPS_Forensics::recent_modified_executables() (walks plugins/mu-plugins/uploads, PHP exts only, newest-first, bounded to 100/50k) surfaced as a new Forensics card replacing the SSH find (find removed from build_ssh_commands). build_ssh_commands now sources the wp_<hex>_cfg alternation for the render-hijacker grep from WPS_Indicators::malware_option_keys() (was hardcoded at 8 keys, catalogue has 14+, i.e. drifted  same 1.3.86 lesson). Panel retitled 'fallback for logs PHP can't read' and reworded to point at the Logs tab first and name the genuine SSH-only cases (root-owned /var/log, open_basedir, disabled shell). The plugin never shells out  matching is in PHP. Verified by php -l (both files) and a harness (recent scan newest-first + non-PHP excluded; cfg keys wp_3093c104e2_cfg/wp_8447aa87d2_cfg now present; filesystem find removed; 9 SSH commands). INDICATOR_VERSION unchanged at 1.3.81-1.

**1.3.93 (re-dropper hunt; samples trioxide_c0b61d + moulted_fdd902).** Two more doorway-kit drops, confirmed the same kit as pacifiers/donation by identical core.php fingerprint (219cc579); the 1.3.91 recurrence-awareness counts them as re-drops 3 and 4 and escalates to RE-DROPPED Nx (verified in harness by seeding the two prior names). Triage of the kit's own code shows it does NOT self-replant (file_put_contents writes are kit-internal state: config/log.txt + the doorway link/img/url JSON caches; curl fetches doorway content)  so the re-dropper is external and still live after 1.3.91. The scanner already covers the WordPress-level persistence vectors (check_cron_tampering, check_mu_plugins, check_wp_config, check_auto_prepend_hijack, check_wp_content_dropins, check_dropper_cache, check_dropper_persistence) but nothing for the off-WordPress ones (system crontab, a dropper outside the install), which PHP cannot read (no shell, open_basedir). Added a Forensics 'Re-dropper hunt' card (build_redropper_hunt_commands) with six read-only SSH commands: user+system cron listing, cron-runs-PHP/curl/wget filter, account-wide search for the kit + its reused stub files (canaryspillsdinky.php / unmadesuerscorker.php, stable across all four drops), kit-shaped-directory find, grep for PHP embedding the kit markers (the re-dropper), and account-wide recently-modified PHP. No detection-list change; INDICATOR_VERSION unchanged at 1.3.81-1. Verified by php -l (both files) and command-generation check. Note: the kit is caught + auto-deleted + escalated already  this release only adds the hunt aid for the still-live external dropper.

**1.3.94 - quarantine-first remediation.** Removal is reversible: `WPS_Quarantine` (new `includes/class-quarantine.php`) moves confirmed threats to a hardened `wp-content/wps-quarantine/` store, neutralises every file with a `.wpsq` suffix, and keeps a manifest per entry; `auto_remediate()` quarantines first (setting `quarantine_enabled`, default on) and falls back to hard delete. This also fixed root-kit removal: `delete_directory()` refuses paths outside `WP_CONTENT_DIR`, so ABSPATH-root kits flagged `auto_delete` were never actually removed - quarantine carries its own guards and handles them. Walkers skip the store; `purge_expired()` (30-day retention) runs on the hourly scan; Forensics gains an always-visible Quarantine card (restore / purge / empty) backed by three new AJAX handlers. `INDICATOR_VERSION` unchanged (`1.3.81-1`).

**1.3.95 - the design system is the single source of colour.** Every colour in the admin routes through the `--wps-*` tokens on `.wps-app`; PHP renderers choose classes, never colours, and inline styles are restricted to sizing and JS-toggled state (24 in source, all enumerated in the release verification). Scheme selection is the `appearance` option rendered as `data-wps-scheme` on the wrapper - `auto` defers to `prefers-color-scheme`. Event handling follows the same discipline: no inline onclick, no inline scripts; actions travel as `data-wps-*` attributes into delegated listeners in admin.js. Constants by intent: the 1.3.63 hero gradient, the brand pill, `color:#fff` on coloured surfaces. `INDICATOR_VERSION` unchanged (`1.3.81-1`).

**1.3.96 - tokenisation must be value-preserving.** 1.3.95 changed rendered colours while introducing tokens; 1.3.96 restores the 1.3.94 palette exactly (legacy CSS byte-identical; component layer re-pinned with one token per distinct original value, verified 56/56 against the 1.3.94 PHP). Standing rule: introducing or refactoring tokens must never change a rendered value; palette changes are a separate, explicitly approved decision. Appearance defaults to `light`; `auto` and `dark` are opt-in. `INDICATOR_VERSION` unchanged (`1.3.81-1`).
**1.3.97 - detection and removal gap-closing.** Six items, no new files, all verified by php -l (21 includes + bootstrap) + node --check + render-smoke (8 tabs, 0 notices) + a 28-check fixture harness. (1) One executable-extension predicate, `is_php_executable()` over `PHP_INVENTORY_EXTS` (php/phtml/php3-7/phps/pht/phar), now backs all sixteen content walkers plus the re-drop pre-filter and the external-payload-loader classifier; the ZIP check and the two inventory sites are deliberately untouched. Standing rule reaffirmed: walkers and the inventory constant share one definition of "a PHP file". (2) New `check_htaccess_redirects()` (registered in the aggregation array) reads .htaccess at ABSPATH/wp-content/uploads, parses RewriteCond/RewriteRule groups, and flags redirects to an external host gated on HTTP_REFERER or HTTP_USER_AGENT (search-engine referers / crawler UAs called out as the doorway signature). Review-only by design - hotlink rules share the grammar - so no `auto_delete` and no .htaccess editing. (3) `check_php_signatures` now walks `wp-content/themes` (all themes, active or not) after the plugin walk, delete target scoped to the specific theme folder; complements `check_theme_for_backdoor` (active-theme heuristics only, not full signatures). (4) `quarantine_option()` snapshots a wp_options row (name, base64(serialize(value)), autoload) into the store as `kind=option` before deletion; `restore()` writes it back via `update_option` with `allowed_classes=false`. Wired into all six deletion paths (2 scanner auto, 4 controller manual); `quarantine_enabled()` made public for the controller. (5) `create_timestamped_backup()` reroutes pre-clean backups into the store via `quarantine(..., $move=false)` copy mode (original stays for cleaned bytes; snapshot neutralised; restore refuses-if-occupied). Fallback to copy-beside only when quarantine is unavailable or disabled. Two inline backup blocks (wordfence-waf.php, .user.ini) folded onto the same path; `basename($backup)` display replaced by `backup_location_label()`. (6) `check_redrop()` adds a shallow `DirectoryIterator` pass over ABSPATH top-level - the 1.3.91/1.3.83 follow-up, root-level kits were invisible to the wp-content-only walk - sharing one match closure with the wp-content walk. Folded in while quarantine was open: the manifest now carries `sha256` (file kind), `stored_by` (acting user / cron / system), and the detection `reason`. `INDICATOR_VERSION` unchanged at `1.3.81-1`.


**1.3.98 - Tier-2 detection.** Four items, no new files, verified by php -l (21 includes + bootstrap) + node --check + render-smoke (8/8, 0 notices) + the 1.3.97 harness as regression (28/28) + a new 20-check fixture harness. (1) Verify-then-report in `auto_remediate()`: after a claimed success, `clearstatcache` + `file_exists` re-check; a persisting target gets `remediation_unverified`, re-dropper hunt guidance, an `auto_delete_unverified` event, and is excluded from `deleted_paths` so children are not skipped as parent-handled. The race branch is asserted structurally in the harness (needs a live re-dropper to provoke). Both `action` append sites hardened with null-coalescing. (2) `check_c2_references()`: unified sweep of PHP-executables under WP_CONTENT_DIR (recursive) + ABSPATH top level against `WPS_Indicators::all_c2_domains()` - a pure aggregator over cookie_exfil/tds/clickfix_download lists (victim-site list excluded by design; the family lists remain the single source of truth, so the aggregator does not move INDICATOR_VERSION). Review-only: blocklists legitimately contain these domains. (3) `check_functions_redirect_injection()`: every theme's functions.php (dormant included, per the 1.3.97 lesson); flags a redirect primitive to an external host (header/wp_redirect/JS location/meta refresh, or a runtime-decoded target incl. the `'Location: ' . base64_decode(...)` concatenation shape) ONLY when paired with a cloaking/obfuscation tell - primitive alone never fires. Review-only: functions.php is surgical-clean territory. (4) `run()` refactored from one array_merge to an ordered label=>callable list (generated mechanically from the merge, order preserved) driven through `SCAN_TIME_BUDGET_SECONDS` (45): exhausted budget skips remaining checks, names them in a `scan_budget_exhausted` finding, logs, and marks `wps_last_scan` partial; a started check is never interrupted. `INDICATOR_VERSION` unchanged at `1.3.81-1`.

**1.3.99 - the event subsystem (Hardening I).** One new file, `includes/class-event-log.php` (package 44 entries); verified by php -l (22 includes + bootstrap) + node --check + smoke 8/8 (doubling as the no-wpdb fallback proof) + 1.3.97/1.3.98 harnesses as regressions (28/28, 20/20) + a new 35-check suite over an in-memory wpdb. `WPS_Event_Log`: table `{$prefix}wps_events` with the full unified 24-field schema (unpopulated fields NULL - the shape never changes under later phases); per-record prev_hash/curr_hash (sha256 over a FIXED canonical field order - never reorder CONTENT_FIELDS) + HMAC-SHA256 under a per-install key; chain head + count + genesis anchored in the `wps_event_chain` option AND a PHP-guarded randomised-name mirror in WPS_LOG_DIR; key mirrored the same way, either copy restores the other. Rotation at 10000 rows re-anchors genesis so the surviving window verifies; rotation is the only deletion path (no clear on the store; clearing the FILE log records an `event_log_cleared` event). Legacy file log imported once as pre-chain rows (hmac='', counted separately, never claimed verified). `WPS_Logger::log_event()` is the shim: unchanged signature, forwards every call into the store, keeps the file copy as fallback. Events tab renders store-first with a verification banner + User column; file-log rendering is the fallback. HONEST LIMIT (in class header + changelog): tamper-evident, not tamper-proof - DB write + key + both anchors rebuilds the chain; the guaranteed catch is direct UPDATE/DELETE against the table. **Roadmap Decision 4 RESOLVED: custom table over append-only file** - phase-1 incident grouping is query-shaped, and a table survives filesystem resets. `INDICATOR_VERSION` unchanged at `1.3.81-1`.

**1.4.0 - Hardening II (audit high-priority list complete).** No new files (44 entries); verified by php -l (22 + bootstrap) + node --check + smoke 8/8 (structural counts identical to the 1.3.96 baseline) + 28/28 + 20/20 + 35/35 regressions + a new 24-check suite. (1) `WPS_Utils::enforce_cooldown()` wired after guard_request in 22 destructive handlers - 5 heavy @30s (regenerate_salts, delete_user, delete_plugin_folder, quarantine_empty, delete_all_unknown_b64_options), 17 standard @10s; per-user transient buckets keyed by action name; refusals audited as rate_limited (risk 20). Reversible config toggles (wpconfig_constant, htaccess_rule, rebaseline_wpconfig) deliberately excluded - a cooldown there punishes legitimate rapid toggling. (2) `WPS_Event_Log::audit()` - the stored_by/reason pattern generalised: object, original/new (or quarantine-snapshot pointer), reason, risk; wired into salts (60), delete_user (120), delete_plugin_folder (80), the four option-delete paths (40, now CAPTURING the snapshot id 1.3.97 discarded), export (10), rate refusals (20). WPS_Logger::file_event() extracted so audit() keeps the file belt without double-writing the store through the log_event forward. Quarantine actions deliberately NOT re-audited at the controller - manifest + shim context already covers them; doubling would duplicate rows. (3) Self-baseline: `check_self_integrity()` registered FIRST in the scan list (a tampered guard cannot be trusted to report on anything else); TOFU at version change; php/js/css under WPS_DIR, logs/ excluded (mutable by design, chain's job); modified/missing/unexpected = review-only criticals + one self_integrity_failed audit (risk 100). Distinct from 1.3.89 (other plugins vs wordpress.org). (4) `WPS_Event_Log::sign()` - sha256 of the exact payload + HMAC under the chain key; export_diagnostics embeds the block computed over the bundle WITHOUT the signature key. **PROCESS RULE (from a live near-miss): never re-execute historical transcript commands wholesale to restore tooling - extract the heredoc payloads only.** A restored command's embedded CSS edit modified the byte-identical legacy region in the working tree; caught by `diff` against the shipped 1.3.99 and restored byte-identical before packaging. Harness loss from environment resets is expected; the recovery path is transcript extraction of file payloads, never command replay. `INDICATOR_VERSION` unchanged at `1.3.81-1`.

**1.4.1 - doorway-kit resilience (from live sample analysis).** Sequencing note: this displaced EDR Phase 1, which moves to 1.4.2. A live kit sample (`proximity_af837f`, kit self-version 1.10.7) was analysed statically. **KEY FINDING: not a new family** - `check_doorway_backdoor_kit` (1.3.83, annealing/resweep) already scored it 5/5 against a threshold of 3 with all four tells and auto-delete armed. An initial read of the sample suggested a new family; testing the existing detector against it disproved that. The rule this reinforces: test the CURRENT detectors against a new sample before writing new ones. Three real gaps surfaced instead, all confirmed by experiment against the sample: (1) **folder-name fragility** - `check_doorway_backdoor_kit` only scored directories literally named `core`, so `mv core sys` dropped detection to ZERO. Now any directory is a candidate and the cue score decides; `core` remains a +1 corroborating tell. node_modules/vendor/dotfiles skipped. (2) **unobfuscated web shells invisible** - a 226 KiB Tiny File Manager inside the kit drew 0 hits from `check_generic_webshell_patterns` because it has no eval/base64/obfuscation. New `check_file_manager_shell()` matches a CLUSTER (4+) of `WPS_Indicators::file_manager_shell_markers()` (FM_ROOT_PATH/FM_SELF_URL/FM_USE_AUTH/FM_READONLY/FM_EXCLUDE_ITEMS/FM_UPLOAD_EXTENSION/FM_SESSION_ID/FM_Zipper), 20 KiB-4 MiB size window, severity by LOCATION (uploads/cache/mu-plugins or kit-co-located = critical, else high), review-only always. (3) **cloak config unused** - new `check_doorway_cloak_config()` finds the kit by configuration, which survives renaming: JSON carrying 4+ of `doorway_cloak_config_keys()` (panel_kee/black_ip_array/black_userag_array/black_org_array/black_country_array/white_list_only_ip/white_list_referer/url_param_array), gated on a largest-blocklist threshold of 100 entries (sample had 111,088 IPs / 569 UAs / 87 orgs) to exclude legitimate small allow-lists; cheap raw-text `panel_kee` pre-filter before json_decode so multi-MB files are not parsed speculatively; surfaces the kit's self-version; review-only (deleting config alone leaves the kit live). **BUG CAUGHT BY HARNESS: WP_CONTENT_DIR is inside ABSPATH, so recursive walks over both roots double-report.** `check_doorway_cloak_config` had a `$seen` guard; `check_file_manager_shell` did not - added. Any future check walking both roots recursively needs realpath dedupe (1.3.98's `check_c2_references` is safe: ABSPATH pass is shallow DirectoryIterator). **`INDICATOR_VERSION` -> `1.4.1-1`** - first move since 1.3.81-1, justified: two genuinely new indicator lists.

**1.4.2 - in-admin documentation viewer.** Sequencing: displaced EDR Phase 1 again, which now moves to 1.4.3. Three new files (package 48: 3 files + the vendor/ dir entry): `includes/vendor/class-wps-parsedown.php`, `includes/vendor/parsedown-LICENSE.txt`, `includes/class-admin-docs.php`. Ninth tab `docs` registered in the config band beside Settings. **Vendoring decision: Parsedown 1.8.0 (MIT) is declared as `WPS_Parsedown`, NOT `Parsedown`** - it is among the most commonly bundled PHP libraries and a duplicate global class declaration is a site-wide fatal; the rename is the ONLY upstream modification (verified: the sole code reference is the class declaration, everything else is self::/static::). Licence text shipped verbatim. **Security model for rendering our own docs, given the plugin's own threat model assumes attacker file access:** (1) document selection is a fixed slug=>file WHITELIST, no request path ever reaches the filesystem, resolved path re-checked with realpath + path_is_inside; (2) `setSafeMode(true)` escapes raw HTML and neutralises javascript: URLs; (3) output additionally passed through `wp_kses_post()` as an independent second layer; (4) **`.md` added to the self-integrity baseline extension list** (now php/js/css/md) because rendering docs makes a tampered doc an admin-XSS vector - the viewer contains it, the scanner reports it. Caching: transient keyed on md5(path|size|mtime|WPS_VERSION), needed because changelog.md and upgrading.md are ~300 KB each; `clearstatcache()` before fingerprinting (PHP per-request stat cache would otherwise fingerprint stale values). Degradation: missing mbstring, missing class, or parser throw all fall back to esc_html'd raw source inside `<pre>`, never a fatal. CSS appended to admin.css using EXISTING tokens only (verified: zero raw hex introduced, prior file byte-identical as a prefix). **PROCESS FAILURE RECORDED: the release gate printed GREEN while the docs tab was fatalling**, because the smoke harness was piped to `tail` and its exit code discarded. Gate corrected to assert exit code + absence of FATAL + expected tab count. Rule: a verification step whose result is not asserted is not a verification step. Second defect caught: a class constant initialised from `DAY_IN_SECONDS` fatals when the class loads without WP core constants - use literals for class constants in classes that could load early. `INDICATOR_VERSION` unchanged at `1.4.1-1`.

**1.4.3 - obfuscated packer + index-stub detection (from two live samples).** Sequencing: EDR Phase 1 -> 1.4.4. Samples: `composer_0_1_4` (a legitimate Omega-framework theme - CLEAN, retained conceptually as an FP control) and `vrhpkgc` (Alexis Blondin's GPL "Protect Uploads" plugin in a randomised folder, backdoor in root `index.php`). **CONFIRMED TOTAL BLIND SPOT: all 11 existing detectors returned 0** - the sample carries no matchable string because every function name is rebuilt at runtime. Backdoor metrics: 19,779 bytes, 2 newlines, 7 goto jumps + 7 labels, 28 `$var[N]` concat fragments, one 19,092-char base64 blob (zlib, `eJy` prefix), 1 eval; layer 2 = hex2bin polyfill + 18,320-char blob; layer 3 = encrypted, needs a runtime key, NOT established (stated honestly in the changelog - do not claim payload behaviour). Sibling `admin/`, `includes/`, `languages/` index.php files were 26-byte stubs; that contrast is the highest-signal tell. **New `check_obfuscated_goto_backdoor()`**: technique-first, 5 tells (goto/label flattening >=3 each; `$var[N].` fragments >=10; packed layout >2048B AND <=12 lines; encoded blob >=500 chars; eval/assert/create_function/preg_replace-e). Requires >=3 tells AND at least one of exec-or-blob (layout alone never fires). **Remediation split by POSITION**: `index.php` = stub position = auto_delete (quarantine default, recoverable) because nothing legitimate is packed there; anywhere else = review-only because commercial obfuscators exist and deleting a licensed plugin's main file breaks sites. **New `check_index_stub_anomaly()`**: position-only, technique-agnostic (catches PLAIN backdoors the packer check misses); index.php >=2048B in plugins/mu-plugins/themes/uploads that contains an executable primitive; requires executable logic so comment-only files don't fire; review-only. **Two harness-caught bugs:** (1) sibling-stub glob used `dirname(dirname($path)).'/*/index.php'` = neighbouring PLUGINS, not subdirs of the same plugin - corrected to `dirname($path).'/*/index.php'`; the strongest evidence was silently missing from findings. (2) 1024-byte size floor skipped a complete 976-byte packer - lowered to 600, tell-count decides, not size. **FP controls now in the suite:** ordinary large file, long base64 asset without eval/goto, genuine Silence-is-golden stub, goto-without-payload, comment-only large index.php. `INDICATOR_VERSION` unchanged at `1.4.1-1` (technique regexes live in the check, per the 1.3.97 .htaccess precedent). **Backlog from this analysis:** folder-name vs Text Domain/Plugin Name mismatch detection (trojanised-legit-plugin pattern) - conservative, entropy-gated, review-only; not built this release to keep scope tight.

**1.4.4 - file-operation shells + PHP config overrides (four live samples).** Sequencing: EDR Phase 1 now 1.4.5. Samples: `Plugin-390a770b` (known Plugin-<hex> shape, caught 2x by check_php_signatures + check_obfuscated_js_injector, nothing new), `lobdgok`/`fxkohks`/`cokefga` (SAME campaign as 1.4.3's vrhpkgc: genuine "Protect Uploads" plugin as camouflage, random 7-char folder, payload in root index.php; fxkohks and cokefga are BYTE-IDENTICAL, md5 30ecffc9077dd09e9c00ac086820b029). **All four were already detected pre-release** (test-existing-detectors-first rule again), but fxkohks/cokefga only by POSITION (check_index_stub_anomaly, review-only), not technique. **Gap 1: execution-free web shells.** Their payload ("DarkStealth v3", 283 lines, 10434 bytes) contains ZERO eval/assert/system/shell_exec/base64 - verified by grep. It is a pure file-operation shell: scandir+unlink+mkdir+file_put_contents+move_uploaded_file driven by $_GET(8)/$_POST(3)/$_FILES(3). `check_generic_webshell_patterns` requires user input INSIDE an execution construct and is structurally blind; `check_file_manager_shell` (1.4.1) is brand-specific (FM_ cluster). New `check_file_operation_shell()` matches the CAPABILITY CLUSTER: >=2 raw superglobals (NOT sanitised WP APIs - this is the main FP guard) + a browse primitive + >=3 distinct mutate ops (or upload + >=2), 1 KiB-1 MiB window, all co-occurring in ONE file (real plugins spread this across classes). Position-driven remediation per the 1.4.1/1.4.3 precedent: index.php stub = critical + auto_delete; elsewhere = high + review-only (media/backup plugins legitimately do file work). **Gap 2: PHP config overrides, zero prior coverage.** lobdgok ships `php.ini` with `safe_mode=Off / disable_functions=NONE / open_basedir=OFF / exec=ON / shell_exec=ON` - an attacker re-enabling what the host disabled so a payload elsewhere can shell out. Per-dir php.ini is honoured on CGI/FastCGI, `.user.ini` on PHP-FPM. New `check_php_config_override()` reads php.ini/.user.ini (<=64 KiB) and regex-matches dangerous directives (disable_functions cleared, open_basedir off, safe_mode off, exec-family on, auto_prepend/append_file set, allow_url_include on); case/space insensitive. Severity by location: inside WP_CONTENT_DIR = critical (no legitimate reading), web root = high (host may own it). ALWAYS review-only - deleting host-placed config can down a working site. FP controls verified: ordinary limits-only php.ini not flagged, dangerous directive in a non-php.ini filename ignored, WP-API-based file plugin not flagged, single-mutation file not flagged, sub-floor file ignored. `INDICATOR_VERSION` unchanged at `1.4.1-1` (technique detectors in the scanner, no indicator lists moved - same reasoning as the 1.3.97 .htaccess work).

**1.4.5 - cloaked injector detection + anti-evasion normalisation (live sample `native-image-optimizer-2b8f`).** Sequencing: EDR Phase 1 now 1.4.6. **CONFIRMED BLIND SPOT: all 16 detectors returned 0** - and critically, NOT for want of indicators. All six Polygon RPC endpoints plus `eth_call` were already in `etherhiding_indicators()` since 1.3.78, AND the exact contract address was already recorded in class-blocker.php and class-scanner.php as a confirmed indicator. Indicator coverage was complete; the evasions defeated it entirely. This is the strongest possible argument for matching behaviour over tokens. Two independent evasions defeated them: (1) **encryption at rest** - payload lives in `resources/config.bin` behind magic header `WH68`, byte 4 = campaign id, bytes 5-6 = big-endian key length, then key, then body; decode chain is `strrev` -> XOR(repeating key) -> `gzinflate` -> 4323 chars of JS. No indicator exists in plaintext on disk. (2) **split-literal obfuscation** - EVERY identifier concatenated from fragments; measured ZERO plaintext occurrences of `wp_footer`, `googlebot`, `administrator`, `eth_call` in a file that uses all four. Payload = **EtherHiding on Polygon**: contract `0x08207B08...`, six public RPC endpoints, next stage read on-chain (no takedown-able domain, RPCs are legitimate infrastructure, operator updates by writing to the contract); JS uses clipboard = ClickFix lineage, same campaign as the plugin's origin, evolved. **Cloaking model (why owners never find these):** early-return for logged-in administrator/editor/author; early-return for 18 crawler UAs; defines DONOTCACHEPAGE/OBJECT/DB/MINIFY + no-cache headers so the payload is never cached and leaves no cache artefact. Decoy legitimacy: body_class filter, heartbeat interval, REST `/status` route with a permission callback. **Response: stop depending on intact tokens.** New `WPS_Utils::normalise_split_literals()` (read-side only, <=1 MiB, <=12 passes, both quote styles) + `split_literal_count()`. Normalisation runs BEFORE matching in the new check and is available to retro-fit any existing content check - this is the high-leverage part of the release. New `check_cloaked_injector()`: matches BEHAVIOUR (tokens can be hidden, behaviour cannot) - >=4 crawler UAs from the new `crawler_cloak_user_agents()` list AND an actual `HTTP_USER_AGENT` read (a bot list alone is legitimate: analytics plugins exclude bots from stats) AND privileged-user skip AND output injection. Supporting tells: split density, cache suppression, runtime resource decode. Content-confirmed -> auto_delete of the CONTAINING plugin/theme dir via new `containing_extension_dir()` helper, which returns '' for files sitting directly in plugins/ so the plugins directory itself can never be nominated. New `check_split_literal_obfuscation()`: review-only, needs >=10 absolute AND >=1.0/KB - **threshold measured, not guessed: max 3 splits in any single file across this plugin's own 315 KB; sample had 70 in 4.9 KB (~1000x density)**. **FP controls in the suite (the important half):** bot-excluding analytics plugin (no role skip, no injection) NOT flagged; role-aware footer injector without crawler cloaking NOT flagged; SEO plugin naming crawlers but never reading the request UA NOT flagged; 315 KB legitimate file NOT flagged; sub-floor and rate-diluted files NOT flagged. **Process fix: fixtures141 pinned an exact INDICATOR_VERSION literal and broke on this release's legitimate bump** - rewritten to assert intent (moved off `1.3.81-1`, >= 1.4.1) rather than a literal. RULE: never pin a moving version literal in a regression assertion; assert the invariant. `INDICATOR_VERSION` -> `1.4.5-1` (two new lists: crawler_cloak_user_agents, cache_suppression_constants).

**1.4.6 - tamper protection / must-use guard.** Sequencing: EDR Phase 1 -> 1.4.7. Driven by a live operator report: attackers disabling the plugin on sites where it was dashboard-activated. **Root cause is structural, not a bug: every defence runs inside the plugin, so disabling it first defeats all of them.** Attack surface enumerated: (1) direct `update_option('active_plugins')` - a backdoor running as PHP already holds wp-config DB creds, one query, no files touched, no warning; (2) delete/rename the plugin dir - WP silently drops missing plugins from active_plugins; (3) an mu-plugin that `remove_action`s the hooks - dashboard still shows it active, worst case because it reassures; (4) `auto_prepend_file` via php.ini/.user.ini (the lobdgok sample from 1.4.4 shipped exactly this); (5) unschedule `wps_hourly_scan` - looks healthy, scanning stops. **Loose end found: the deactivation hook carried the comment "Intentionally keep MU file - it must survive plugin deactivation" but NOTHING in the codebase ever wrote an MU file.** Intent predated implementation. New `includes/class-guard.php` (WPS_Guard) + generated `mu-plugins/0-wps-guard.php` (0- prefix sorts first; mu-plugins load before regular plugins, cannot be dashboard-deactivated, never appear in active_plugins). Guard duties: restore to active_plugins + record; report a deleted plugin dir rather than re-adding a broken entry (re-adding would only error); `shutdown` deadman - listed active but `WPS_VERSION` undefined means it never loaded (unhook or swallowed fatal), throttled 15 min. **CONSENT IS THE CRITICAL DESIGN POINT: a guard that blindly re-activates fights the owner and is malware behaviour.** Expected state is explicit - activation writes expected_active=true, `register_deactivation_hook` writes false (that hook ONLY fires for genuine deactivation; a DB write never reaches it). State is **HMAC-signed** with a key in `mu-plugins/.wps-guard-key.php`, so a DB-only attacker cannot forge consent; unsigned/altered state is treated as tampering (restore anyway + record). **Key and guard live in mu-plugins, NOT in WPS_DIR** - deliberate: `WPS_LOG_DIR` is inside the plugin dir, so deleting the plugin would have destroyed its own guard state. Guard cannot use WPS_Event_Log (it runs when the plugin does not) - appends to `.wps-guard-events.php`, imported into the chained store by a throttled (15 min transient) init hook that also re-asserts state and reinstalls a missing/outdated guard. `pre_update_option_active_plugins` watcher records removals with no genuine admin request (is_admin + activate_plugins cap + deactivate action, or WP_CLI); **passes the value through unchanged - blocking could wedge a site in a redirect loop**. Guard status row added to Diagnostics (a guard that failed to install because mu-plugins is unwritable must be visible). Uninstall removes guard + key + events + state. **Verification approach worth reusing: the generated MU file is EXECUTED in a separate process against a simulated WP** rather than merely inspected - 40 checks covering restore, stand-down on genuine deactivation, forged-consent rejection, deleted-dir reporting, and no-op on a healthy site. Also asserted: generated file passes `php -l`, references no plugin classes, silences filesystem calls (fails open - a guard that white-screens a site is worse than the problem), **and the scanner does not flag its own guard**. `INDICATOR_VERSION` unchanged at `1.4.5-1`. **HONEST LIMIT recorded in the changelog: an attacker with mu-plugins write access can delete the guard too. This is tamper-EVIDENCE and cost-raising, not invulnerability; no in-process defence can be.**

**1.4.7 - EDR Phase 1: sessions, incidents, risk.** The roadmap's Phase 1, displaced six times by live-sample releases (1.4.1-1.4.5) and once by tamper protection (1.4.6). New `includes/class-edr.php` (WPS_EDR). **No schema change was needed** - `session_id`/`incident_id`/`correlation_id` have been in CONTENT_FIELDS since 1.3.99 and `build_row()` already passes them through; Phase 1 simply populates them. Hooks registered: `wp_login`, `wp_login_failed`, `clear_auth_cookie` (logout - fires reliably where `wp_logout` timing varies), `user_register`, `set_user_role`, `after_password_reset`, `activated_plugin`, `deactivated_plugin`, `switch_theme`, `upgrader_process_complete`, `add_attachment`, `current_screen`. **Incident grouping:** same user_id, else same ip, within `INCIDENT_WINDOW` 1800s; id shape `inc-<Ymd-His>-<8hex>`. Matching on ip as well as user is deliberate - failed logins have no user_id and the successful login after them belongs to the same story. **Decision 5 RESOLVED: adopt the roadmap's score values as shipped defaults**, in one `SCORES` constant behind `wps_edr_risk_scores` filter (bands behind `wps_edr_risk_bands`); anchors fixed by the roadmap are login_success=10 and admin_account_created=120, everything else placed relative to those AND to the 1.4.0 audit weights so one table governs both. Bands: low 0-50 / medium 51-120 / high 121-220 / critical 221+. **Noise control (essential, the store caps at 10k rows):** `WATCHED_SCREENS` whitelist only (users, user-edit, user, plugin-editor, theme-editor, plugin-install, plugins, options-general, export); `SCREEN_THROTTLE` 600s per session+screen via transient; logged-out screen views ignored; `set_user_role` records ONLY genuine elevation to administrator (sideways changes and already-admin are ignored); non-plugin upgrader events ignored. File editors get their own event type (`file_editor_opened`, 60) because opening one is materially different from viewing a list. **Standing rule upheld: behavioural findings are observations - never auto-remediated.** Gating decisions honoured: no GeoIP (D1 - country/city/asn stay null), no client fingerprinting (D2), no deception (D3); zero third-party network calls. UI: Incidents card at the top of the Events tab (no 10th tab; smoke stays 9/9), tokens only, no inline handlers. **Verification: the fixture harness gained WP hook stubs (`add_action`/`do_action`/`apply_filters`/`has_action` capture) exactly as the roadmap required, so behaviour is tested by FIRING hooks, not by calling methods** - 58 checks. FakeWPDB extended with GROUP BY aggregation and incident-timeline queries. **Two harness defects fixed rather than worked around:** (1) `ARRAY_A` undefined in the harness - a WP core constant the codebase already uses elsewhere; (2) the chain test wiped `$wpdb->rows` directly while the anchor persisted, desynchronising them and producing a failure that looked real - rebuilt on clean state and now proves MORE: chain verifies across behavioural events AND altering a recorded risk_score is detected. `INDICATOR_VERSION` unchanged at `1.4.5-1`. Next: Phase 2 (behaviour baselines, trusted devices; impossible travel still gated on D1).

**1.4.8 - autoloading + documentation refresh (architecture review).** Driven by a full file-architecture review against WordPress standards. **Measured findings:** 29 PHP files / 21,938 LOC; `class-scanner.php` alone is 7,356 LOC / 328 KB with 116 methods and 55 `check_*` detections (a third of the codebase in one class); **865 KB of PHP parsed on EVERY request** with only ~159 KB reachable on a front-end hit (82% deferrable by context, 96% by autoload); docs ship at 658 KB (changelog 343 KB + upgrading 314 KB), larger than all code; **zero i18n** (no Text Domain header, no `load_plugin_textdomain`, 0 translation calls, no `languages/`). Clean and worth recording as such: all 26 classes `WPS_`-prefixed, exactly 1 global function (correctly prefixed `wps_uninstall`), ABSPATH guards 26/26, 37 nonce + 13 capability checks, assets properly split per the standing preference. **Shipped here: autoloading only** (highest leverage, lowest risk). `spl_autoload_register` with an **explicit 26-entry map, not a derived path** - 23 class names map cleanly but `WPS_Utils`, `WPS_Indicators` and `WPS_Parsedown` do not, and silent load failure in a security plugin is not an acceptable trade; the map doubles as the class inventory and a test asserts completeness in BOTH directions (every declared class is mapped; every mapped path exists and declares its class). **Conditional requires were REJECTED and the reason is recorded:** review found `class-blocker.php` calls `WPS_Scanner::run()` unguarded, and `class-dropin-guard.php` (runs on `init` EVERY request) probes `method_exists('WPS_Scanner', ...)` and **falls back to different behaviour when absent** - hand-guessing context would have silently changed plugin behaviour on ordinary page loads. Autoloading cannot, because a class loads exactly when referenced. Both hazards now have tests asserting *why* they are safe. Additionally `WPS_Admin::register_hooks()` + `WPS_Remediation_Controller::register_hooks()` gated on `is_admin()` (they register only `admin_menu`/`admin_enqueue_scripts`/`admin_post_*`/`wp_ajax_*`; admin-ajax.php and admin-post.php both report `is_admin()` true) - a further 93 KB off front-end requests. **Result: 865 KB -> 36.3 KB typical front-end (96% deferred), verified by EXECUTING the real autoloader in a separate process for all 26 classes.** Harness defect fixed: smoke prelude lacked an `is_admin()` stub (same shape as the 1.4.7 `ARRAY_A` gap); `is_admin()` is core, defined long before plugins load, so the call is correct and the harness was incomplete. **Documentation debt cleared:** across 1.4.2-1.4.7 the version line in `doc/readme.md` was bumped each release while CONTENT was never updated - zero mention of tamper protection, EDR, cloaked injectors, normalisation, file-operation shells or php.ini overrides, and its Admin Tabs list omitted the Docs tab from 1.4.2; `readme.txt` still framed the plugin as ClickFix-only. Both refreshed. **PROCESS RULE ADDED (twice-triggered this release): never ship any file whose provenance cannot be established. The working tree twice held content matching neither shipped 1.4.7 nor this release's own edit scripts - first `doc/readme.md`, later `doc/changelog.md` and the version markers. Response: reset EVERY documentation file to the last shipped package as a known-good baseline, reapply all edits in ONE deterministic pass, and verify anchor counts (`s.count(anchor)==1`) rather than mere presence so a partial prior run cannot duplicate an entry. Code was retained only because a passing test suite covered it.** `INDICATOR_VERSION` unchanged at `1.4.5-1`; no detection logic touched. **Deferred from the review, each needing its own release: (1) scanner split** along the existing `check_*` seam into family-grouped classes with the scanner as orchestrator - large blast radius over detection correctness; **(2) i18n retrofit** - a genuine wordpress.org repository-guidelines gap since readme.txt is in repository format, but it touches every user-facing string across ~22k LOC and must not disturb existing escaping.

**1.4.9 - forward-notes refresh (documentation only).** The `## Forward notes` block at the head of `doc/upgrading.md` still described 1.3.98 / 1.3.99 / 1.4.0 / "1.4.1 onwards" as FUTURE work - stale by eight releases, and actively misleading to anyone reading the upgrade guide to find out what was next. Replaced with current forward notes covering: scanner restructuring (7,300 LOC / 55 checks, internal, output equivalence is the thing the release must prove; documented entry points stay), i18n retrofit (no behaviour or English-output change), EDR Phase 2 (behaviour baselines, trusted devices - warns to expect a noisier first week while the baseline has little to compare against), Phase 3 (FIM over the defined set, baseline recorded at upgrade; session summaries; event-time persistence), Phase 4 (classification + MITRE mapping + install-chain and access-log correlation), Phase 5 (timeline, incident replay, executive summary). **Three additions the old block lacked:** (1) the open gating decisions stated in operator terms - GeoIP (D1: bundle vs external API vs ship without; notes the plugin currently makes ZERO third-party calls and an API would reverse that posture), fingerprinting (D2), deception (D3, parked, with the risk-posture reasoning: honeypot accounts write to the users table, decoy endpoints accept uploads by design); (2) an **honest sequencing note** - six consecutive releases 1.4.1-1.4.6 came from live samples and displaced planned work each time, so the roadmap is intent not schedule; (3) a "what will not change" list - behavioural findings never auto-remediated, removals go to recoverable quarantine, no third-party calls without a recorded decision, and **deactivating from the Plugins screen always works** (worth documenting rather than leaving in a changelog, since the 1.4.6 tamper guard is exactly the feature an operator might reasonably fear would fight them). **Version bump for a docs-only change is deliberate and the reasoning is now recorded as a standing rule: since 1.4.2 the plugin's own `.md` files are inside the self-integrity baseline, which is keyed to `WPS_VERSION`. Republishing changed documentation under an unchanged version would make every existing install report its own docs as modified. Any change to shipped documentation therefore requires a version bump.** Full gate re-run despite no code change, on the principle that a release claiming to change nothing should prove it. Tree rebuilt from the shipped 1.4.8 package before editing, all anchors asserted `count == 1` per the 1.4.8 provenance rule. `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.10 - registration/role "account factory" detection (live operator report).** Operator reported new accounts appearing with `users_can_register` OFF. **Confirmed gap: every prior detector is file-based and this attack touches no file.** Three `wp_options` rows: `users_can_register` (open registration), `default_role` (stock = subscriber; set to administrator = every registration becomes an admin, no backdoor file needed - the registration form IS the backdoor), and `wp_user_roles` (the quiet one: leave default_role alone, grant `subscriber` `manage_options`/`activate_plugins`, and the Users screen shows a normal-looking subscriber list). New `check_registration_settings()`: default_role in administrator/super-admin = critical, editor/author = high (occasionally deliberate); `users_can_register` 0->1 reported ONLY against a stored baseline (`wps_registration_baseline`) and the baseline refreshes after reporting so it says its piece once rather than nagging; non-admin roles scanned for 9 dangerous caps, administrators skipped. **ALWAYS review-only - site configuration is not malware, and silently rewriting a membership site's registration settings is a worse failure than the one being detected.** Capability finding explicitly acknowledges legitimate role extension by membership/e-commerce plugins. **Detection at BOTH ends, deliberately:** scan-time finds the bad state; new EDR hooks `update_option_users_can_register` + `update_option_default_role` find the MOMENT and the ACTOR (scan-time cannot tell you who). Scores: `default_role_escalated` = 120 (same weight as `admin_account_created` - identical outcome), `registration_opened` = 70; no-op changes record nothing, lowering a role back records at low risk. Opening registration + escalating default role in one window sums to 190 -> high band with no rule written for that pair. Uninstall clears `wps_registration_baseline`. 37-check suite; the reported scenario tested by name first; stock WordPress confirmed clean in three shapes. `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.11 - cron advice + eval()'d-code verdict + first-party allowlist (found by reading a live scan report).** Operator supplied 7 real findings; two were **plugin faults, not site faults**. **Fault 1 (dangerous advice):** cron findings emitted `wp cron event delete <hook>` for every non-unresolvable verdict regardless of hook. The two live hits were `wp_version_check` and `wp_scheduled_delete` - **WordPress CORE jobs**. Following it would stop update checks AND remove nothing (deleting a schedule does not remove the code registering the handler; it re-registers next request, minus the core job). **Fault 2 (misclassification burying the best signal):** `classify_callback_source()` returned `missing_file` (medium) whenever `realpath()` failed. A closure declared in eval'd code has `getFileName()` = `/real/file.php(123) : eval()'d code`, which realpath can never match - so **a cron handler existing only in evaluated code, with nothing on disk, was filed next to benign resolution failures**. That is the shape of the persistence this plugin exists to find. **Fixes:** new `evald_code` verdict checked BEFORE realpath (also catches `runtime-created function` from `create_function`), mapped to **high**; `missing_file` retains medium for genuine absences. New `WPS_Forensics::core_cron_hooks()` (16 core events) + `cron_finding_action( hook, verdict, file )` - core hooks get an explicit do-NOT-delete with the reason (deletion does not remove the handler), acknowledge that legitimate plugins extend core jobs, and non-core hooks still get the delete instruction. eval advice strips the `(NNN) : eval()'d code` suffix to name the **origin file that performed the eval** and embeds a parameterised `wp eval` probe printing each closure's declaring file. **Third change from the same thread:** operator's own plugin (`hrefmaster`) showed in the "cannot be checksum-verified" report - correct but useless, as wordpress.org has no baseline for unpublished code. New Settings field `first_party_plugins` (reuses `sanitize_slug_list`; accepts `slug` or `slug/file.php`) removes them from that report. **Silence would be the wrong trade** - "my own plugin" is a comfortable place to add an unchecked file - so new `check_first_party_integrity()` keeps a LOCAL baseline: sha256 per PHP file -> single fingerprint, stored against the plugin header version in `wps_first_party_baseline`. **Version change = silent re-baseline (that is an update); fingerprint change with UNCHANGED version = high, review-only** (an edit in place is what both an undeclared hotfix and a planted file look like; only the operator can distinguish them). Baseline updates after reporting so it never nags; a declared-but-absent plugin is not an error. Uninstall clears the option. 47-check suite. `INDICATOR_VERSION` unchanged at `1.4.5-1`. **LESSON RECORDED: both faults were in shipped code that passed its own harness. The harness proves a check RUNS; only an operator reading real output proves it is RIGHT. Scan-report review should be a recurring input, not an incident.**

**1.4.12 - fail-safe bootstrap (operator reported a white screen).** **Could NOT reproduce:** full bootstrap + complete scan + forensics run (15 sections) + every 1.4.11 path + all 9 tabs execute cleanly on PHP 8.3; **no PHP 8-only syntax** (all 103 `match(` hits were `preg_match`, all `never` hits prose; arrow fns are 7.4+ matching declared `Requires PHP: 7.4`). **Shipped anyway because the report exposed a worse property than its cause: a security plugin that can take a site down has already done more damage than most of what it defends against.** Aggravating factor: the 1.4.6 tamper guard means DB-level deactivation is UNDONE on the next request, so the obvious remedy fails (renaming the plugin dir IS safe - guard records `guard_plugin_directory_missing` and does not re-add). Three mechanisms: (1) `register_shutdown_function` records E_ERROR/E_PARSE/E_CORE_ERROR/E_COMPILE_ERROR/E_USER_ERROR **only when the file path is inside WPS_DIR** - never claim another plugin's crash, which matters as much as catching our own; (2) `wps_safe_mode()` gate - if the last request died in our code, load ONLY the passive core (measured: 59 hooks healthy -> 3 in safe mode; hourly scan, CSP, drop-in guard, admin screens and remediation all skipped) and register an admin notice + nonce-protected clear action; (3) `wps_boot( label, callable )` wraps every registration and records a throw with its step label. Autoloader now `is_readable()`-checks before require (interrupted upload / half-finished update is an ordinary cause; a caller's `class_exists()` false beats a white screen). Safe mode self-clears on version change (an upgrade is the usual fix); record stored in `wps_last_fatal`, cleared on uninstall. **Verification approach: real fatals induced in SEPARATE PROCESSES** - a crash handler tested by simulation is not tested. 29 checks covering record/engage/survive/isolate/recover, including that a crash in another plugin's directory is neither recorded nor triggers safe mode, and that deleting a class file outright yields `class_exists()===false` rather than a fatal. **Harness defect fixed: fixtures148 asserted the literal `require_once WPS_DIR`, which broke when the autoloader resolved its path into a variable for the readability check - identical behaviour, different wording. Rewritten to assert intent (exactly one require_once in the bootstrap; it lives inside the autoloader closure). SAME LESSON AS THE 1.4.5 PINNED INDICATOR VERSION - it has now cost two releases. RULE RESTATED: assertions must test intent, never a literal spelling of the implementation.** `INDICATOR_VERSION` unchanged at `1.4.5-1`; no detection logic touched. **Open: the reported white screen is NOT diagnosed.** If it recurs on 1.4.12 the notice will name file+line. Note the same site has an unexplained `wp-config.php` hash change and cron handlers with no file behind them; broken injected code white-screens identically to a plugin fatal.

**1.4.13 - the white screen was OURS: unbounded guard spool (operator-reported outage, root-caused).** MENJ identified mu-plugins as the cause; reproduced and fixed. **ROOT CAUSE, two unbounded halves:** (1) `wps_guard_check()` treats an unverifiable expected-state record as tampering and calls `wps_guard_record()` - correct, but the condition is PERMANENT not momentary, and it was UNTHROTTLED. **Measured: 50 requests -> 50 records.** The trigger is a missing `.wps-guard-key.php`; **dot-files are exactly what backup/migration/sync tools skip by default**, so any restored or migrated site starts the loop. (2) `import_events()` did `file_get_contents()` on the whole spool, `preg_split()` into an array, and called `WPS_Event_Log::audit()` **once per line** - each an HMAC chain link plus a DB insert. ~100k requests = ~17 MB / ~104k lines; importing that exhausts memory_limit or max_execution_time = **WHITE SCREEN**. Not a syntax error - an unbounded loop with a fuse the length of the site's traffic. **FIXES:** MU side - throttle the unsigned record to 900s (same window as the not-loaded check, via `wps_guard_unsigned_at`); refuse to append once the spool exceeds `EVENTS_MAX_BYTES` (256 KB). Plugin side - `import_events()` discards an oversized spool with ONE `guard_events_overflow` summary instead of parsing it, caps any run at `IMPORT_MAX_LINES` (200), and truncates UNCONDITIONALLY so nothing accumulates. **Root cause healed, not endured:** new `WPS_Guard::ensure_key()` regenerates a missing/malformed key and re-signs state, called from the init tick BEFORE `import_events()`. Consent model unaffected - the only code reaching it is this plugin running with mu-plugins write access, which the 1.4.6 honest-limit already concedes. `MU_VERSION` -> `1.4.13-1` so every install regenerates. 28-check suite: failure reproduced THEN re-run against the fix (50->1 records; throttle releases after its window; oversized spool gets no further writes and yields 1 summary not 4000 audits; 200-line cap; healing stops the loop at source - 40 requests, 0 records). Test defect fixed: `filesize()` read without `clearstatcache()` reported a stale size from an earlier test - **same defect as the 1.4.2 stale stat**. **LESSONS: (a) the guard is the one component that CANNOT be dashboard-deactivated, making it the worst possible place for a fault; (b) 1.4.12 safe mode does NOT cover it, because a must-use plugin loads before the plugin that would catch it; (c) anything that writes per-request needs a bound in its ORIGINAL design - throttle, cap, and truncate are not follow-up work. Second time in three releases the plugin was the problem rather than the thing finding problems.**

**1.4.14 - completing the 1.4.13 guard fix (found by measuring, not asserting).** MENJ asked how he could know 1.4.13 would not white-screen again. Rather than assure, built a **worst-case measurement**: guard in its failure state (signing key permanently absent, plugin never runs to heal), 200,000 requests, clock advancing. **Result: 52 MB - WORSE than the 34 MB of the version it fixed.** Two faults, both introduced in 1.4.13: **(1) stat cache** - the size cap read `filesize()` without `clearstatcache()`. Invisible in production-shaped tests because one request = one process = cold cache, which is exactly why it passed; held open across requests every check after the first read a stale size and the ceiling never fired. **(2) throttle in the wrong place** - 1.4.13 throttled ONLY `guard_state_unsigned` (the path behind the reported outage) and left `guard_plugin_directory_missing`, `guard_restored_plugin` and `guard_plugin_not_loaded` writing every request. `guard_plugin_directory_missing` fires when the plugin folder is renamed - **the exact outage-recovery step this project recommends**. **FIX: the bound moved to the choke point.** Every record passes one type-keyed throttle (900s, map in `wps_guard_rec_at`, pruned to 12 entries) inside `wps_guard_record()` itself, plus `clearstatcache()` before the size check. **Re-measured: 262,160 bytes against the 256 KB cap - ceiling holds.** Import side unchanged and already safe (oversized spool discarded with ONE summary; in-bounds runs capped at 200 lines; unconditional truncate). `MU_VERSION` -> `1.4.14-1`. Suite extended to 32 checks: 60 requests with the plugin dir renamed produce <=3 events with no type repeating inside the window (the case 1.4.13 missed), and the GENERATED mu-file is asserted to contain both `clearstatcache` and `wps_guard_rec_at` so neither can be dropped from the template. **The worst-case measurement is retained as a standing test - a bound that is not measured under load is a comment.** Third pinned-literal test defect fixed (fixtures1412 pinned `'1.4.12'`; now reads WPS_VERSION from source) - after 1.4.5 (indicator version) and 1.4.8 (require_once spelling). **PRINCIPLE, now three times paid for: a safety bound belongs at the single point everything passes through, not at each caller; and it must be measured under sustained load, because the failure mode of an unbounded write is invisible in a single-request test.**

**1.4.15 - must-use tamper guard WITHDRAWN (operator decision).** MENJ called it after two outages in a week (1.4.13 root cause, 1.4.14 incomplete first fix). **Removal is ACTIVE, not passive - this is the critical design point: a must-use plugin already on disk keeps loading on every request whether or not the plugin installs it, so merely ceasing to install would have left the fault running on every site that ever had it.** `WPS_Guard` reduced 613 -> 208 lines: mu-file template, HMAC signing apparatus, state records, event spool and import all deleted; what remains is `purge()` / `maybe_purge()` plus no-op methods returning their previous shapes so stale references cannot fatal. `purge()` deletes exactly `0-wps-guard.php`, `.wps-guard-key.php`, `.wps-guard-events.php` from mu-plugins and clears 5 options - **nothing globbed, walked or pattern-matched, so no other plugin's mu-file is at risk** (explicitly tested). Where unlink fails (read-only mu-plugins) the mu-file is overwritten with an inert stub so withdrawn code stops running anyway, and diagnostics reports the leftover with its path. `maybe_purge()` is version-keyed (`wps_guard_removed`), so downgrade-then-upgrade re-cleans; after the first run it is a single option read. Bootstrap detached entirely; `pre_update_option_active_plugins` filter removed; diagnostics card reports "withdrawn" in neutral styling rather than as a failure. **CAPABILITY LOST, stated plainly and not glossed: the plugin no longer detects or reverses its own deactivation, so malware that disables it now succeeds silently.** **PARKED WRITE-UP at the head of `doc/upgrading.md`** with four conditions for return: (1) every write path bounded at the single choke point, proved by sustained-load measurement not single-request tests; (2) its own crash handler independent of the main plugin, since 1.4.12 safe mode CANNOT cover a must-use plugin - it loads first; (3) a documented recovery route needing neither DB nor FTP access; (4) no dependency on anything ordinary hosting tooling silently discards (keying the feature on a dot-file was the original sin, invisible until a restored site proved it). **GOVERNING PRINCIPLE RECORDED: a component with no off switch must be held to a higher standard than the rest of the codebase. Losing a detection capability is a smaller cost than a security tool that can take a site down and cannot be switched off when it does.** 42-check suite; harnesses fixtures146 (guard install) and fixtures1414 (guard bounds) retired with the feature. `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.16 - events tables rebuilt (operator: "this is fugly").** The appearance complaint was real but the substantive fault beneath it: **every row carried identical visual weight, so `wp_config_modified` (critical) rendered exactly like `admin_screen_visited` (routine)**. Three causes: (1) labels inconsistent - curated types read as English, unmapped types printed raw identifiers on the same screen; (2) no severity display on the overview at all, while the Events tab kept **two private hand-written arrays** (`$is_alert`/`$is_ok`) for row colour that had already drifted from the codebase; (3) inline `style=` attributes for column widths/padding/borders, contrary to the standing tokenisation preference. **Fixes:** new shared `WPS_Utils::event_severity()` (5 bands: critical/high/warning/ok/info; exact map then prefix fallback) and `WPS_Utils::event_label()` (curated map wins; unmapped types humanised rather than printed raw). Severity rendered as a left rail on the row PLUS a dot beside the label - **two channels, not colour alone**, so it survives colourblind readers and greyscale print - with the band in `screen-reader-text`. All three tables (overview recent, full events, incidents) consolidated onto one `.wps-events` shell, `data-wps-sev` attribute-driven styling, design tokens throughout, explicit dark-scheme rules (light tints are alpha-composited against white and go muddy on a dark panel, so they are re-stated not derived). Events tab's private arrays deleted - **one source of truth**. **REAL BUG CAUGHT DURING THE WORK: the first draft of the severity map keyed the wp-config event on `wp_config_hash_changed`, which is emitted NOWHERE - the actual type is `wp_config_modified`. The most serious event a site can raise would have stayed classified as routine, i.e. a redesign that changed appearance and fixed nothing.** Found by validating the map against real `log_event()`/`audit()`/`record()` call sites rather than trusting it. **That validation is now a build gate: a severity key naming an event neither emitted nor present in the curated label map fails the suite.** It also caught `guard_events_overflow`, dead since the 1.4.15 guard withdrawal, and removed it. 54-check suite: 95 emitted types enumerated from call sites and all asserted to resolve to a valid band; 10 must-be-critical types asserted by name; informational types asserted to stay quiet; `scan_clean` asserted 'ok' not 'info'; unknown/empty/differently-cased types asserted safe. **Fourth pinned-literal harness defect fixed** (fixtures99 asserted the literal `<th class="wps-strong wps-muted" style="width:110px">User</th>`; now asserts a User column exists) - after 1.4.5, 1.4.8 and 1.4.14. `INDICATOR_VERSION` unchanged at `1.4.5-1`; no detection logic touched; overview inline styles 5 -> 2.

**1.4.17 - login guard (operator request: block bot sign-in attempts, "can we hook Akismet?").** **Akismet 5.7 evaluated FROM SOURCE (operator supplied the zip) rather than from memory - three decisive findings: (a) SEVEN API endpoints exist (`token`, `verify-key`, `deactivate`, `get-stats`, `comment-check`, `submit-spam`, `submit-ham`) and NONE concerns authentication; every "login" string in the codebase is `$current_user->user_login` read for a comment reporter. (b) `Akismet::comment_check()` is a PUBLIC documented method - better than the `http_post()` route first proposed - which auto-fills blog/lang/charset/user_ip/user_agent, merges caller data SECOND (so `user_ip` can be overridden), fires only `apply_filters('akismet_request_args')` and NO `do_action`, so synthetic checks cannot pollute other plugins' comment-spam listeners, and returns `false` cleanly with no key. (c) `http_post()` sets `'timeout' => 15`.** That timeout dictates the architecture: **the local counter decides WHETHER to block (no network on the login path); Akismet decides only HOW LONG, after the block is already recorded.** Otherwise an attacker who can slow Akismet pins every PHP worker via wp-login.php, and an Akismet outage means no protection at all. New `WPS_Login_Guard`: `authenticate` @ priority 1 (rejects before user lookup or password hashing), `wp_login_failed` counter, `wp_login` clears the counter and records the admin IP. Threshold 5 / 900s window -> 3600s default block; Akismet spam -> 86400s, ham -> 900s (a clean IP is likelier a mistyped password than a botnet); verdict cached 24h per IP. Bot-username instant block **gated on `username_exists()` being false - that gate is what makes it near-zero false positive**. XML-RPC: `xmlrpc_enabled` false + `system.multicall`/`pingback.*` stripped, **OFF by default** (Jetpack and the mobile apps authenticate this way). Akismet enrichment **auto-detected via `Akismet::get_api_key()` - no second copy of the key, nothing to configure**; `akismet_status()` surfaces the state on Settings. **ANTI-LOCKOUT (same failure class as the 1.4.15 withdrawn guard, so designed in rather than bolted on): a logged-in `manage_options` IP is never blockable; admin-login IPs are remembered 30 days (capped at 12) and protected; operator allowlist; all blocks expire; `WPS_DISABLE_LOGIN_GUARD` in wp-config is checked BEFORE the DB setting, because recovery must work when wp-admin is unreachable - FTP only, no database.** **BOUNDS ARE STRUCTURAL: counters are transients (cannot outlive their window), and a blocked IP is rejected at the gate before `wp_login_failed` can fire, so a blocked bot stops generating writes entirely** - the 1.4.13/1.4.14 lesson applied at design time rather than after an outage. Reuses the `WPS_Blocker` store via a new general `record_ip_block()` (no second block list); `client_ip()` made public. 58-check suite including Akismet in 5 states (absent / keyless / throwing / malformed / working - all produce a working block), asserted NOT called below the threshold, called once per block rather than per attempt, `user_ip` passed EXPLICITLY (the auto-fill is correct only inline and would score the wrong address if ever deferred), and no password field anywhere in the payload. **Fifth pinned-literal harness defect fixed** (fixtures148 pinned the map size 26; now derived from the classes on disk) - after 1.4.5, 1.4.8, 1.4.14 and 1.4.16. **Honest limits documented in upgrading.md: runs in PHP so WordPress still boots per attempt (an edge/WAF rule beats it for volume); stops guessing, not stolen credentials - 2FA is separate work.** New file -> 51 entries. `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.18 - login guard visibility (operator: "how do I know if it's working?").** Two distinct problems: (1) **correct-by-design silence** - Akismet is consulted only when an IP crosses the threshold, so a site with no attacks since activation has genuinely nothing to report; that one-call-per-block rule is exactly why the login path never waits on the network, and it was explained rather than changed. (2) **a real gap: a working install was indistinguishable from a broken one, and the only way to tell was to be attacked.** Added: four Diagnostics rows (login protection active/disabled; Akismet detected + key state via `akismet_status()`; failed sign-ins today/7d; addresses blocked today/7d) plus a fifth verdict-breakdown row shown only once Akismet has actually been used (known-bad / clean / no answer). New `WPS_Login_Guard::stats()` backed by a single bounded option (`wps_login_stats`, daily rollover keeping 6 days of history); **counting is bounded by the same structural rule as everything else here - an IP can only be counted until it is blocked, after which the gate rejects it before `wp_login_failed` fires.** Blocks now record `duration_source` (`akismet-spam` / `akismet-clean` / `default`) and state the reason in plain words, so an Akismet-informed block is distinguishable from the default - previously the block list showed a length with no explanation. New `verify_akismet()` + `admin_post_wps_verify_akismet` handler (nonce + `manage_options`) driving a **"Test Akismet connection now" button in Settings**, using `Akismet::verify_key()` (verified from source: returns `valid`/`invalid`/`failed`, and short-circuits to `invalid` for any key not exactly 12 chars) and reporting four states. Uninstall clears `wps_login_stats`. Suite extended 58 -> 81 checks: a fresh install asserted to report zero rather than nothing; attempts asserted counted BELOW the threshold with no block and no Akismet call (the state that previously looked like failure); all three duration paths asserted to name themselves in the reason AND record a matching field; verification exercised across 6 states including throwing and unrecognised replies, asserted never to fatal. **Inconsistency found by the suite: reason string said "clean" while the stored field said `akismet-ham` - Akismet's vocabulary leaking into a block list; both now say clean.** `INDICATOR_VERSION` unchanged at `1.4.5-1`; no detection logic touched and no change to when Akismet is called or what decides a block.

**1.4.19 - documentation correction, no code change.** The 1.4.17 Akismet integration was built and documented from reading the Akismet 5.7 plugin source, before the operator supplied Akismet's published API specification (`akismet-api` spec.yml). **The spec confirms the architecture-driving conclusion - the documented endpoints are `verify-key`, `comment-check`, `submit-spam`, `submit-ham`, `key-sites`, `usage-limit`, and NONE concerns authentication - but shows two points were hedged unnecessarily: (1) `comment_type` explicitly accepts "a custom type", so `wps-login-attempt` is documented usage, not a liberty; (2) `comment-check` requires only `api_key`, `blog`, `user_ip`, so an empty `comment_content` is a supported call, not something got away with.** Changelog wording corrected accordingly ("wearing a comment's clothes" removed). **DECISION RECORDED - `usage-limit` panel REJECTED:** the spec documents `/1.2/usage-limit` returning `limit`/`usage`/`percentage`/`throttled`, assessed as a Settings health panel and declined because the monthly figure is dominated by ordinary comment checking (so it reports little about the login guard specifically) and `throttled` only fires for plans consistently over quota; the 1.4.18 connection test already answers the operator's question. Logged so it is not re-proposed. **Also assessed and rejected: the `akismet-sdk-php` package** (clean PSR-18 client exposing `getUsageLimit()`, but requires PHP 8.1 plus three PSR Composer packages to reach one endpoint while the Akismet plugin is already loaded with the key), **and `fuspam 1.3`** (unmaintained third-party helper using PHP short tags and hand-rolled cURL over plain `http://` - unencrypted API calls have no place in a security plugin, and it does nothing `Akismet::comment_check()` does not do better). `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.20 - Akismet usage panel (operator override).** **Previously assessed and RECORDED AS REJECTED in 1.4.19; the operator asked for it anyway and it was built - their plugin, their key.** The reservation is documented on-screen and in the changelog rather than buried: **the monthly figure is dominated by Akismet's ordinary comment checking, so it describes Akismet's day job more than the login guard, which contributes roughly one call per blocked address.** Genuine additions: a `throttled` signal 1.4.18 could not provide, and a concrete number in place of an assurance that login checks cost little. New `WPS_Login_Guard::akismet_usage()` + `akismet_usage_label()`. **Built from the published spec (`akismet-api` spec.yml), not inferred: `GET https://rest.akismet.com/1.2/usage-limit?api_key=…` returning `limit`/`usage`/`percentage`/`throttled`. Requested via `wp_remote_get()` NOT `Akismet::http_post()`, because the latter POSTs to a hardcoded `/1.1/` path and this is a GET on `/1.2/`.** Two spec details easy to get wrong and both tested: **`limit` may be the string `"none"` for an unlimited key** (so not cast to int; rendered "unlimited"), and **the documented error response is the plain string `"invalid"`, not JSON** (so a non-JSON body is a contract, not a parse failure). Cached 1h (a status panel must not consume the allowance it reports on); failed reads retry at 5min; the existing verify button force-refreshes so verdict and figure stay consistent. 8s timeout - admin screen only. Suite 81 -> 103 checks: endpoint/HTTPS/query-key asserted rather than assumed; unlimited key; throttled response incl. usage above limit; four failure modes (transport error, plain-text `invalid`, HTML error page, no key) each asserted to degrade to a reported state not a fatal, with **no request at all when no key**; **the hour cache asserted to hold across repeated reads; and blocking an address asserted to make NO usage-limit request, so this reporting never touches the login path.** Uninstall clears `wps_ak_usage`. `INDICATOR_VERSION` unchanged at `1.4.5-1`; no change to when Akismet is consulted or what decides a block.

**1.4.21 - site-timezone display + events table second pass (operator: UTC unhelpful in Malaysia; "the layout is fugly").** **Timezone: every screen printed the stored UTC string directly - `wp_timezone()`/`gmt_offset` were never consulted anywhere in the codebase.** New `WPS_Utils::local_time()`, `site_timezone()`, `timezone_label()`. **STORAGE REMAINS UTC AND MUST: a log recorded in local time is ambiguous at every DST boundary and cannot be correlated against server/host logs. Storage and presentation are separate problems; only presentation was wrong** - asserted in the suite by checking nothing writes a local timestamp into the log. `timezone_label()` handles named zones (MYT/EDT), offset-only configs (GMT+8, GMT+5:30, GMT-3:30) and UTC; note PHP's `format('T')` returns a short offset like `+08` for offset zones, so the label is derived from `getOffset()` rather than parsed from the abbreviation. Unparseable timestamps are returned as-is, never blanked. **Layout: (1) `.wps-ev-detail` was monospaced wholesale, so prose ("mtime check: suppressed 7 files at...") rendered as terminal output wrapping to 3 lines and tripling row height - now `font-family: inherit` with embedded `code`/`.wps-tok` keeping mono; (2) the incidents ACTIVITY column imploded every step, so a 78-event incident printed "login failed" 78 times across the screen - new `WPS_Utils::collapse_steps()` gives "login failed x78", collapsing runs independently, NOT merging alternating steps, truncating at 6 runs with a remainder count; (3) `.wps-ev-what` was sized for the longest label rather than the common case, leaving an empty channel mid-table.** 33-check suite incl. the operator's exact case (01:39:32 UTC -> 09:39:32 MYT). **Two harness defects fixed: fixtures1421 asserted absence of `date( 'Y-m-d H:i:s' )` and matched `gmdate(...)` by substring (false positive - anchor with a negative lookbehind); fixtures147 did not load `WPS_Utils`, which the autoloader supplies in production (same gap as fixtures99 in 1.4.16).** `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.22 - timezone label matches WordPress vocabulary.** WordPress Settings offers **two storage paths - a named city (`timezone_string`) or a bare offset (`gmt_offset`, presented as "UTC+8")** - and these take different branches through `WPS_Utils::timezone_label()`. **The 1.4.21 suite exercised only the city path; the operator had the offset path set, which is the one the Settings screen presents first.** Conversion was correct on both branches (times matched WordPress's own reported local time exactly); only the label diverged, reading "GMT+8" where the dashboard says "UTC+8" - the same fact in a second vocabulary. Offset labels now render `UTC+8` / `UTC+5:30` / `UTC-3:30`; named zones still return their abbreviation where PHP supplies one; a UTC site still reads `UTC`. **Suite now asserts the operator's exact configuration** (timezone_string empty, gmt_offset 8 -> `01:45:09 UTC` displays `09:45:09`, labelled `UTC+8`). **Lesson recorded: when a setting has more than one storage representation, test every representation, not the one that was convenient to construct.** `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.23 - subnet rotation evasion closed (found in the operator's own event log).** **The 1.4.17 per-IP threshold is the right rule for one attacker at one address and useless against one attacker holding a range. Live evidence: 5 failed sign-ins against ONE username (`cyclehosting`) from FOUR addresses in `185.238.231.0/24` (.59/.224/.58/.209), each stopping before the per-address count of 5. Every address behaved lawfully; the attack was only visible in aggregate. The guard was working correctly and measuring the wrong unit.** Failures now also counted per range: **/24 for IPv4, /64 for IPv6** (the units actually rented). **THREE conditions must ALL hold before a range is touched: >=12 failures in 900s, from >=3 DISTINCT addresses, in a range containing no trusted address.** The distinct-address requirement is the key discriminator - **12 failures from ONE noisy host blocks that host and NOT its range**, asserted in the suite. Range blocks are **1800s (half the per-address 3600s: larger blast radius, shorter hold)** and are **sign-in scope only, not site-wide**. **ANTI-LOCKOUT: `network_is_protected()` checks the allowlist AND every remembered admin IP (30-day list, not just the current session), so an operator on a dynamic address inside their own ISP range is safe.** Separate setting `login_network_guard` (on by default) because **this is the one part of the login guard that can affect people who are not the attacker - a /24 can be a mobile carrier's NAT egress with thousands of users behind it**; the Settings copy says so explicitly and recommends switching off for sites whose visitors share carrier/corporate networks. Bounds structural as before: range counters are transients (cannot outlive the 900s window) with remembered addresses capped at 24 per range. Blocks recorded via `WPS_Blocker::record_ip_block()` keyed by CIDR with `scope=network`; the gate checks exact IP first, then range, returning a distinct error code (`wps_login_blocked_network`). 28-check suite replays the operator's exact attack. `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.24 - server log inspection screen rebuilt (operator: "why is this layout design fugly?").** **Third table in this codebase to draw the same complaint (after 1.4.16 events and 1.4.21 detail/activity columns), and the causes repeated: built for completeness rather than for scanning - every field gets a column, every column gets full-weight chrome, all explanation dumped at the top.** Specific faults: (1) **156 words of caveats above the first control**, including a hosting paragraph only relevant when a log is MISSING; (2) **the `Log` column repeated "Web access log" x4 and "Server log" x2** - a column whose values repeat carries no information the path does not already give; (3) **two full-weight `.button` elements per row x6 rows** - WordPress sizes `.button` as a page's primary action, so 12 of them outweighed the data; (4) **sizes in mixed units, proportional font, left-aligned** so `585 B` and `157 MB` could not be compared - and the 157 MB file is the single most important fact on the screen; (5) `<code>` on every full path making grey slabs; (6) generic `.wps-table` padding. **Fixes:** path leads with kind as an inline qualifier; row actions demoted to `.button-link` while the two PAGE-level buttons deliberately KEEP full weight (the goal was hierarchy, not flattening - asserted both ways); size right-aligned + `tabular-nums`; **files >50MB flagged "slow to scan" - a FUNCTIONAL addition, not cosmetic, since a full IOC scan reads the whole file and can exceed the host's PHP execution limit**; intro cut to one sentence; hosting caveat moved BELOW the table. 29-check suite **asserts the three AJAX hooks are still bound** (restyling controls that drive AJAX silently breaks them), no inline styles introduced, all colour via tokens. **Harness defect fixed - same shape as 1.4.21's: an assertion counted the substring `<th` and matched `<thead`, reporting 4 columns where there are 3. Substring matching without a word boundary has now caused two false failures in four releases; count with an anchored pattern.** `INDICATOR_VERSION` unchanged at `1.4.5-1`.

**1.4.25 - doorway cloaking + control-flow flattening + THE ESCAPE BYPASS (built from a live sample the operator recovered from staging).** **Family: Japanese SEO spam / doorway cloaking.** Root `index.php` hijacks all requests (standard WP `.htaccess` rewrite + `DirectoryIndex`), tests `HTTP_USER_AGENT` against `/(googlebot|baiduspider|bingbot|google|baidu|aol|bing|yahoo|yandex)/si` and `HTTP_REFERER` against `/(google.co.jp|yahoo.co.jp|bing|baidu|google.com)/si`, serves spam `products/[word]/` doorway pages fetched by curl (custom UA `WHR`) from seed `jan06.ofu5563ytu/wowmajp`, cached in a random-hex dir `wp-content/ad630082/` (blob `2cc`, 25KB). `error_reporting(0)` + `ini_set('error_log', NULL)`. **Owner-invisible BY DESIGN; damage lands in SERPs not on the page.** **THE CRITICAL FINDING - A GENERAL BYPASS, NOT A MISSING SIGNATURE: every cloaking string was present but hex-escaped (`\x48\x54\x54\x50_USER_AGENT`), and the scanner matched RAW BYTES, so the ENTIRE content-detection layer walked past it.** Fixed in the shared layer: new `WPS_Utils::deobfuscate_literals()` resolves hex + octal escapes and rejoins split literals (`'bas'.'e64_decode'`), **folding back ONLY printable bytes** (so binary-escape files are not mangled into accidental matches) and **bounded at 1MB** (runs across every PHP file). **New durable signal: `WPS_Utils::goto_density()` - control-flow flattening. Measured ZERO goto/KB across 48 Rodolfo theme PHP files and 31 plugin files; samples ran 16.25 and 18.08/KB. Unlike strings, goto cannot be renamed or escaped without the file ceasing to work - durable against re-obfuscation.** Two checks added: `check_doorway_cloaking()` (**requires TWO independent signals - a crawler list ALONE is ordinary bot/caching behaviour and firing on it would be useless**) and `check_control_flow_flattening()` (floor of 20 statements AND >=5/KB, so a few jumps in a large generated file is not the same thing). **REAL BUG CAUGHT BY THE SUITE: `normalize_relative_path()` returns '' outside wp-content by design, so ROOT-LEVEL findings reported with NO SUBJECT - the single most important finding was unlabelled. New `display_path()` falls back to ABSPATH-relative then basename.** 24-check suite runs against the actual sample; false positives asserted against 29 real theme modules in the same tree, a legitimate crawler-detection file, and self-flagging. **`INDICATOR_VERSION` -> `1.4.25-1`, its FIRST change since 1.4.5, because the indicator set genuinely grew.**

**1.4.26 - punitive escalation + local-evidence-outranks-Akismet + opt-in spam reporting (operator: "something stronger and more punitive", from their own block log).** **BUG FIXED (mine, from 1.4.18): every block in the operator's log showed 15 min including 41.216.188.106 which tried 20+ usernames, because the old logic asked Akismet FIRST and let a 'clean'/ham verdict impose BLOCK_CLEAN even on an address the site had already watched behave like a bot. Akismet not knowing an address != the address being innocent (usually just means the attack is newer than Akismet's crawl).** Reworked `block()`: **local evidence sets the floor, Akismet may only ESCALATE.** Escalation ladder `[900,3600,21600,86400,604800]` (15m/1h/6h/24h/7d) indexed by prior offence count; **offences remembered in bounded option `wps_login_offenders` (14-day TTL, 500 cap) - kept in an OPTION not a transient DELIBERATELY, because the count must outlive the block or a returning attacker resets to the bottom rung.** Multi-username floor: >=5 distinct usernames in-window -> 24h outright regardless of Akismet (tracked as a bounded set on a per-IP transient, expires with the window). Ham may shorten to 15m ONLY on a first offence with <2 usernames (the genuine mistyped-password case); otherwise ham is recorded as 'ignored - local evidence stronger'. Spam still escalates to >=24h. EDR severity rises to 'high' at >=24h. **Opt-in Akismet spam reporting (`login_report_spam`, OFF by default): reports confirmed attackers to submit-spam via `Akismet::http_post(Akismet::build_query($body),'submit-spam')` - both confirmed public in the 5.7 source and matching the API spec's StandardParameters. Fires ONLY on conclusive local evidence (>=5 usernames, repeat offence, or bot-username), NEVER on an Akismet verdict, NEVER on a single-real-username block, once per IP (14-day transient marker), after the block, never on the login path. Rationale: the Akismet corpus is SHARED across all sites, so a wrongful report degrades an address everywhere and submit-ham is slow - only report what the site has already proven.** 20-check suite replays the operator's 10-username case (now 24h not 15m), asserts the ladder climbs across 3 blocks, asserts the mistyped-password path still gets 15m, asserts reporting off-by-default/conclusive-only/once-per-IP/degrades-without-key. **Sixth pinned-literal harness defect fixed (fixtures145 pinned INDICATOR_VERSION '1.4.5-1'; now asserts shape). Also: a test-only `__setBlocked()` helper written into class-blocker.php during development was removed before packaging and the suite reworked to drive the real option - shipping code must never carry test scaffolding.** `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.27 - Akismet reporting default-ON + proxy guard + manual report button (operator override, recommendation recorded).** Operator instructed auto-on-by-default AND a manual path. **Claude recommended AGAINST auto-on (recorded in upgrading.md + changelog): every install then contributes to Akismet's SHARED corpus unasked, and on a proxied site REMOTE_ADDR may be the CDN edge, so blind auto-reporting could flag Cloudflare as a spam source for everyone. Operator chose it anyway - their plugin. The proxy guard was a CONDITION of building it, not an afterthought.** `spam_reporting_enabled()` flipped to default-on (`!== '0'`). New `ip_looks_like_infrastructure()` + `ip_in_cidr()` (packed-bytes CIDR, both families): published Cloudflare v4+v6 plus RFC1918/loopback/link-local; **auto path stands down and LOGS `akismet_spam_report_skipped` on a match**; block itself unaffected (asserted). New `report_ip_manually()` - TRUSTED path, **exempt from BOTH the proxy guard and the conclusive-evidence gate because a human chose the row** - still nonce + `manage_options` + once-per-IP + needs key; returns reported/already/no-key/failed. Shared `submit_spam()` extracted. UI: per-row "Report spam" button (form+nonce) in the Diagnostics blocked-IP table, result banner, already-reported rows show 'reported'; **the blocked-IP table's expiry switched from raw `gmdate(...UTC)` to `WPS_Utils::local_time()`, a lingering 1.4.21 timezone miss.** `admin_post_wps_report_ip` handler. 37-check suite: default-on + overridable; proxy guard across 6 ranges each not-reported + skip-logged; public attacker still reported; CIDR direct on v4+v6 incl. 8.8.8.8 + ordinary IPv6 NOT caught; manual bypasses guard, once-per-IP, degrades without key, nonce+cap; proxy-range attacker still BLOCKED only report withheld. **fixtures1426 TEST 4 updated (default flipped off->on; explicit-off now asserted there, default-on owned by fixtures1427).** `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.28 - Settings screen rebuilt + checkbox default-drift bug (operator: "label alignments, colours, layout... some looks really off").** **Root cause, evidenced: the Settings screen was the ONLY surface with ZERO styling of its own - `grep -c form-table assets/css/admin.css` returned 0, and the dark-scheme block contained 0 rules for `.description`, `form-table`, `textarea`, `select` or `input`. It inherited WordPress core's form-table wholesale, so label column/spacing/description colour came from core, and in dark mode core's light-admin grey was never overridden.** Three concrete faults: (1) **TWO competing row patterns** - 4 rows on the established `.wps-toggle-row` flex (bold title + description beneath), 4 rows (the login-guard settings added 1.4.17-1.4.27, all Claude's) on a plain inline `<label><input> text</label>` + sibling `<p class="description">`; different alignment, wrap behaviour and description indent; (2) **4 SEPARATE `form-table` blocks each sizing its label column independently** - 2 carried an inline `style="width:200px"`, 2 did not, so the column started at a different x-position per section going down the page (this is literally the reported misalignment); (3) no tokenised styling or focus ring anywhere on the form. **REAL BUG CAUGHT by auditing each checkbox's rendered default against its code default: `login_report_spam` code said `?? '1' ) !== '0'` (ON, flipped in 1.4.27) while markup said `?? '0' ) === '1'` (renders UNCHECKED). Display was wrong AND - the worse second-order effect - an unticked box posts nothing, so an operator saving any unrelated setting would have written '0' and SILENTLY DISABLED a running feature.** Fixed; suite now asserts every checkbox renders the default its code path applies, so they cannot drift again. **Also caught: converting the login-protection row wrapped its Akismet status + verify button + usage line (all previously crammed into one `<p class="description">` separated by `<br>`) INSIDE the new `<label>` - an `<a class="button">` inside a label toggles the checkbox on click. Moved to a sibling `.wps-field-actions` container.** Fixes: all 8 checkbox rows unified on `.wps-toggle-row`; label column width set once in scoped CSS (`.wps-app .form-table th`); both inline widths removed (22 -> 20 inline style attrs plugin-wide); descriptions/textareas/selects tokenised with a focus ring; full dark-scheme coverage added for both `[data-wps-scheme="dark"]` and the `auto` media query. **KEY LESSON RECORDED: a bulk find/replace across repeated markup closed the WRONG rows - source-level tag counting passed per-file while 3 individual rows were mis-closed. The suite now RENDERS the page and balances the emitted HTML, because this markup is built through PHP conditionals and a branch can emit an unclosed tag that reading the file never reveals. Assert on rendered output, not source, for anything conditionally built.** 31-check suite. `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.29 - duplicate diagnostic entries were consuming the tamper-evident log (found reviewing the operator's live event log).** **The mtime scanner's mass-event suppression note (`class-forensics.php`, "mtime check: suppressed N files at T UTC") was written UNCONDITIONALLY on every scan. File mtimes do not change, so every scan re-recorded the same facts.** Two details make it a defect rather than untidiness: **(1) `WPS_Logger::write()` routes to `log_event('debug', ...)`, i.e. into the TAMPER-EVIDENT STORE alongside real findings; (2) that store is CAPPED (`array_slice($events, -200)` / `MAX_ROWS`).** Measured from the operator's actual log: **7 distinct buckets (45 files, all at 02:35:08-02:35:21) re-reported across 5 scans = 35 rows = 17.5% of a 200-row window, at 1.4 rows/min - enough to flush EVERY genuine security event out of retention in ~143 minutes.** **A security plugin silently destroying its own evidence is worse than one reporting nothing, because the operator has no reason to distrust what remains.** Fix: new `WPS_Forensics::report_suppressed_batch_once()` - bounded option `wps_mtime_batches_seen`, **keyed on mtime AND batch size so a batch that GROWS is reported again** (a bigger batch on the same second is a new fact), 30-day expiry so a long-lived batch is eventually restated rather than permanently forgotten, capped at 200 entries oldest-first, cleared on uninstall. **Suppression threshold and message wording deliberately UNCHANGED - the goal was to stop repeating the note, not to change which files are reported** (asserted). 18-check suite replays the operator's exact case (7 batches x 5 scans -> 7 rows not 35). `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.30 - Settings label treatment corrected; 1.4.28 was incomplete (operator: "fonts, alignments and layouts look really off", with screenshots).** **ROOT CAUSE: 1.4.28 added `.wps-app .form-table th` styling but did NOT account for an older GLOBAL rule at ~line 695 - `.wps-app th { background: rgba(255,255,255,.52); color: #475467 !important; font-size: 11px; text-transform: uppercase; }` - a DATA-TABLE header treatment that reached EVERY `<th>` in the plugin, form labels included. So settings field labels rendered as column headings in small capitals: the reported "font looks off".** Compounding: **the hardcoded `#475467` carries `!important`, so it BEAT the `var(--wps-ink)` set in 1.4.28 (specificity alone was not enough), and `.wps-app th` appears NOWHERE in either dark-scheme block - confirmed by grep - so those labels stayed mid-grey on near-black. Third instance in this series of a hardcoded colour with no dark coverage.** Fix scoped entirely to the form: `.wps-app .form-table th` now sets `text-transform: none`, `background: transparent`, `letter-spacing: normal !important`, `color: var(--wps-ink) !important` (token AND !important, so it actually wins), plus explicit rules in BOTH dark blocks. **The global rule is deliberately LEFT UNTOUCHED - every data table has its own more specific header rule (`.wps-events th`, `.wps-logs th`, `.wps-table th`, all asserted still present), so nothing on any other screen moves.** Also: inline `<code>` inherited `border-radius: 10px !important` (meant for code blocks) and the `define( 'WPS_DISABLE_LOGIN_GUARD', true );` snippet broke mid-expression across two lines - now `display: inline-block; white-space: nowrap; border-radius: 4px !important`. **DUPLICATE RULE OF CLAUDE'S FOUND AND REMOVED: two `.wps-btn-sm` rules existed - the original defined the control completely (font/height/line-height/padding), while a second added in 1.4.16 changed ONLY font-size, so the control rendered 12px text in a box sized for 11px and neither rule owned it. Consolidated into one rule with enough specificity to beat core's `.wp-core-ui .button` rather than relying on declaration order.** 18-check suite; **two assertions are about restraint not appearance: every new selector asserted scoped to the form/field classes, and the three data-table header rules asserted untouched.** Harness defect fixed: the scoping check sliced the stylesheet from a marker INSIDE a comment, leaving the stripper no opening delimiter, so comment prose was parsed as selectors and reported a phantom failure. **No PHP changed in this release - stylesheet only.** `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.31 - permanent sign-in denylist for non-existent admin usernames (operator request).** **Reporting half ALREADY WORKED and needed no build: `is_bot_username()` has been in the conclusive-evidence list since 1.4.26 and reporting on-by-default since 1.4.27.** Permanent half: **KEY DESIGN DECISION - permanence gets its OWN store, NOT a flag on the existing block list. That list is a fixed 200-entry cache evicting by soonest expiry, so a "permanent" entry in it would either be evicted anyway (not permanent) or crowd out live-attack blocking.** New `wps_permanent_blocks` option + `permanent_blocks()` / `is_permanently_blocked()` / `block_permanently()` / `unblock_permanently()`. Gate checks it FIRST, returns distinct code `wps_login_blocked_permanent`. **TWO PROPERTIES BOUND THE HARM AND ARE WHY PERMANENCE IS DEFENSIBLE HERE, both asserted: (1) the gate is on `authenticate` ONLY, so a permanent block prevents SIGN-IN and nothing else - the site stays readable; (2) `is_bot_username()` requires `username_exists() === false`, so no real account, operator's included, can ever put its own address on the list.** `block_permanently()` additionally refuses any `is_protected_ip()` address (allowlist + 30-day remembered admin IPs). **Bounded at 1000, oldest-first - an unbounded store is how 1.4.13 took a site down.** **Removable from a new Diagnostics table (address / attempted username / date / Remove button, nonce + `manage_options`) - a permanent block with no undo is a trap, since addresses get reassigned.** 35-check suite; the REFUSALS matter more than the blocks: a site that genuinely has an `admin` account asserted never to permanently block for it, allowlisted and remembered-admin addresses asserted refused, gate asserted still rejecting after ALL transients/counters/temporary blocks are cleared. **Two older suites updated to test intent not store: fixtures1417 asserted bot-username blocks appear in the TEMPORARY cache (now the wrong structure - new `blocked_anywhere()` helper asks "is this address blocked"), and fixtures1426's Akismet-spam duration test switched to a non-bot username since a permanent block has no duration to measure.** `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.32 - surfacing counters that were recorded but never displayed; stale posture row removed (operator: "statistics ... shouldn't it be reported here?" and "why the fuck is there a Tamper protection ... withdrawn in 1.4.15 line?").** **DEFECT: the guard records NINE counters via `note()`; `stats()` returned THREE. Four - `spam_reports` (1.4.26), `permanent_blocks` (1.4.31), `network_blocks` (1.4.23), `multiuser_blocks` (1.4.26) - had been written to the DB for up to nine releases and surfaced NOWHERE.** Compounding: **the daily rollover carried only `attempts` and `blocks` into history, so the rest were accumulated all day and DISCARDED at midnight - a 7-day figure for them was impossible however the screen asked.** Fix: counters declared once in a `COUNTERS` const, rollover carries all of them, `stats()` returns `today_*` and `week_*` for each. **Panel gains: permanent-block count (reads the CURRENT list size, not a running tally, so a removal is reflected immediately), Akismet spam reports (today/7d), and a "blocks by rule" breakdown (non-existent account / many usernames / range rotation) that stays HIDDEN until one of those rules has actually fired - a row of zeroes is worse than no row.** **STALE ROW REMOVED: "Tamper protection (must-use guard) - withdrawn in 1.4.15" rendered on EVERY install, for ever, reporting the absence of a feature withdrawn 17 releases earlier and offering nothing actionable - a footnote about the plugin's own history sitting in a status panel. Now a row appears ONLY when `leftover` is true (a must-use file that could not be deleted) and names the file; the two variables it computed (`$wps_guard_ok`, `$wps_guard_neutral`) removed with it rather than left dead.** 32-check suite; **the strongest assertion reads counter names out of the source and fails if any is recorded without also being declared and exposed - the exact failure that produced this release cannot recur silently.** Rollover survival asserted by ageing the stored day. `INDICATOR_VERSION` unchanged at `1.4.25-1`.

**1.4.33 - plain-text web shell detection; THE BLIND SPOT ALL PRIOR CONTENT CHECKS SHARED (two more live samples from the operator).** Sample A = site root `index.php` with an obfuscated payload PREPENDED to the genuine WordPress file (88KB, **4088 goto, 47.2/KB**) - the 1.4.25 flattening check catches it. **Sample B = `ai.php`, 157 lines of CLEAN READABLE PHP: arbitrary upload (`move_uploaded_file`), arbitrary write (`file_put_contents`), delete (`unlink`), rename, directory listing (`opendir`), file read - NO authentication of any kind, NO WordPress bootstrap, Indonesian UI strings. It scored ZERO on EVERY detector in the plugin: goto 0, no base64/gz/rot13, no hex escapes, no eval, no cloaking. Completely invisible.** **THE LESSON, recorded: every content check written so far looked for CONCEALMENT - encodings, escapes, flattened control flow - and concealment is a TACTIC, not a definition. A scanner that only knows how to spot hiding misses every shell that never bothered to hide, and this one did not need to because nothing was looking for code that simply asks to be run.** New `check_unauthenticated_file_manager()` matches the technique itself: **no WP bootstrap (`ABSPATH`/`wp-load`/`wp-config`/`WP_UNINSTALL_PLUGIN` absent) AND no authorisation of ANY kind (`current_user_can`/`is_user_logged_in`/nonce/`password_verify`/`crypt`/`PHP_AUTH_USER`/`$_SESSION`) AND >=2 distinct filesystem MUTATION primitives AND request superglobals present.** **Any ONE of three properties clears a file - loads WordPress, checks auth, or takes no request input - which is what keeps it silent on real code** (uninstall.php has the ABSPATH guard; a maintenance script uses fixed paths; a writer class takes paths from its caller). Severity graded: 2 primitives = 'high' worded for review, 3+ or a browser upload form = 'critical'. **Escapes resolved via `deobfuscate_literals()` first, so hiding the function names does not help.** **DEFECT FOUND WHILE WRITING TESTS: the check as first drafted skipped files <150 bytes, a floor copied from the doorway-cloaking check where bulk is expected. A file manager needs no bulk - 2 primitives fit in ~100 bytes - so the floor would have silently skipped exactly the most suspicious files. Lowered to 40. Lesson: do not carry a threshold across to a check with different physics.** 28-check suite; **the gap is asserted DIRECTLY (sample asserted to have zero goto / no encoding / no hex / no eval) rather than described**; 29 real theme files in the same tree asserted untouched. **`INDICATOR_VERSION` -> `1.4.33-1`.**

**1.4.34 - measured against a REAL attacker toolkit; coverage 4/12 -> 11/11 (operator supplied `qhwvuag.zip`, 309 files).** **KIT STRUCTURE: 12 directories, each a GENUINE GPL plugin (Protect Uploads by alticreation.com; Simple Maintenance Mode) copied under a random 7-letter name, payload dropped as `index.php` - the least remarkable filename in a plugin folder, since WP convention puts a 26-byte "Silence is golden" stub there. Re-drop persistence: removing one achieves nothing.** 3 payload families: X7ROOT "Dark X7ROOT File Manager" (plain, 21584/10434 bytes), layered-decoder loader (19513), 324-byte upload dropper. Plus **6 identical `php.ini` + 7 `.htaccess` artefacts**. **TWO LOGIC ERRORS IN THE 1.4.33 CHECK, both found by measuring: (1) it excluded any file mentioning `wp-load.php` on the reasoning "loads WordPress = subject to WordPress" - BACKWARDS. Requiring wp-load is what a STANDALONE ENTRY POINT does; the legitimacy signal is the opposite, an ABSPATH/WPINC GUARD refusing to run unless WP already loaded it. `tegyqzw` walked straight through. (2) it excluded any file containing `$_SESSION`, counting it as auth - but a shell uses a session for its OWN login; the 507KB `jbwfdih/admin.php` escaped on that clause alone. Auth test now accepts only capability/nonce/HTTP-auth.** **THIRD FIX: `move_uploaded_file` alone is now sufficient (was: 2 primitives). The 324-byte dropper used exactly one, with `error_reporting(0)`, a trigger param and a `'../'` traversal. There is no benign unauthenticated upload endpoint; requiring a second primitive protected only the smallest, most disposable shells.** **NEW `check_encoded_payload_loader()`: `eval(strrev(str_rot13(gzinflate(base64_decode($blob)))))` with decoder names SPLIT across concatenation AND called through variables - two evasions at once. Existing `check_obfuscated_loader()` could not help (JavaScript-specific: atob/charCodeAt/TextDecoder). Matches after `deobfuscate_literals()` and on the NAME not the call, because separating name from bracket IS the technique. Requires sink + >=2 decoders + >=200-char blob.** **NEW `check_hardening_bypass_config()`: THE SCANNER ONLY EVER READ PHP. A third of this kit was configuration - `php.ini` with `disable_functions = NONE`, `open_basedir = OFF`, `exec = ON`, `shell_exec = ON`. On CGI/FastCGI a per-directory php.ini IS honoured, so it re-enables exactly the functions a host disables to contain a break-in. NOT a payload - it is what makes the next payload work. Also checks `.user.ini`.** 21-check suite + 30 from 1.4.33; both 1.4.33 errors asserted from BOTH sides; 53 clean files -> 1 finding (a deliberate near-miss). **`INDICATOR_VERSION` -> `1.4.34-1`.**

**1.4.35 - hidden-identifier detection; the sharpest signal found so far (operator sample `native-image-optimizer-2b8f.zip`, 7 files).** **Sample produced ZERO findings against every check through 1.4.34.** It is a JS injector: **payload NOT in any PHP file** - it lives in `resources/config.bin` behind a custom container (4-byte magic `WH68`, campaign byte, 2-byte key length, body `strrev`'d then XOR'd), read and printed into `wp_footer` at priority 200. **Why everything missed it: no eval, no goto, no encoded blob IN PHP, no filesystem writes, and a genuine correct `ABSPATH` guard that satisfies the standalone-shell test honestly.** **Audience selection is inverted from the 1.4.25 doorway cloak: that one SHOWED spam to crawlers; this one HIDES from them.** Returns early for administrator/editor/author roles, admin/AJAX/cron/REST, and any UA containing crawl/spider/googlebot/yandex/lighthouse/duckduck; then sets `DONOTCACHEPAGE`/`DONOTCACHEOBJECT`/`DONOTCACHEDB`/`DONOTMINIFY` + `Cache-Control: no-store` + `nocache_headers()`. **Net effect: only a logged-out visitor on an uncached page ever receives it - never the owner, never a search engine.** **THE SIGNAL: every meaningful identifier is split across concatenation (`'HTTP'.'_USER_A'.'GENT'`, `'wp'.'_foo'.'ter'`, `'g'.'oogl'.'ebot'`, `'cr'.'aw'.'l'`). New `check_hidden_identifiers()` compares each file against ITSELF with `deobfuscate_literals()` applied and reports sensitive names visible ONLY afterwards. MEASUREMENT: 16 hidden names in the sample vs ZERO across 79 files of real theme + plugin code. There is no reason to write a function name in fragments except to defeat the search someone will run for it.** Severity: 2 hidden = high, 4+ = critical. Files with no splitting at all are skipped before comparison; using those same names OPENLY is explicitly not suspicious. **The names hidden also classify intent - concealing crawler names alongside `is_user_logged_in`/`administrator` IS a description of who must not see the behaviour, and the finding says so.** 22-check suite; **the gap is DEMONSTRATED not described - the sample is asserted to have no goto/eval/visible-base64 and a valid ABSPATH guard, and all four other content checks asserted silent on it.** 1.4.34 toolkit regression confirmed still 11/11. **`INDICATOR_VERSION` -> `1.4.35-1`.**

**1.4.36 - obfuscated JS carried inside PHP (operator sample `Plugin-390a770b.zip`, ONE file, 120,983 bytes).** **Zero findings against everything through 1.4.35.** Header says "simple js plugin"; **the PHP is GENUINELY clean** - correct header, real `ABSPATH` guard, readable functions, no eval, no split literals (so 1.4.35's hidden-identifier check cannot see it), no filesystem writes. **It carries a heredoc holding a SINGLE LINE of 111,478 bytes of obfuscated JS, printed into `wp_footer`. The PHP is not the malware, it is the ENVELOPE - and every content check in this scanner reasons about PHP.** **SIGNAL: javascript-obfuscator renames every identifier to a hex token (`_0x4f78`, `_0x54f1d9`) - 4,911 of them in the sample vs ZERO across 83 files of real theme + plugin code.** **SIGNAL CONSIDERED AND REJECTED - LINE LENGTH: the payload is one 111KB line, which looks damning until you notice Rodolfo ships a LEGITIMATE minified bundle that is one 80KB line. Minification shortens names; obfuscation renames everything to hex. Measuring the wrong one would have flagged every minified asset on every site. This rejection is asserted in the suite, against both a synthetic bundle and the real 80KB vendor file.** New `check_obfuscated_js_payload()`, weighted by location: **inside PHP -> critical (no legitimate version of that exists) and the report notes when the file also prints it; standalone `.js` -> floor 8x higher and severity 'high', since a vendor script protected against copying is unusual but not malicious.** **Threshold lowered while testing: floor was 2048 bytes, but 50 hex identifiers fit in ~700 bytes of script plus a small wrapper, so a compact carrier would have passed underneath. Now 900, chosen against that arithmetic rather than as a round number.** 21-check suite; gap demonstrated not described (sample asserted to have no goto/eval and a valid ABSPATH guard, all four other content checks asserted silent). 1.4.34 toolkit regression still 11/11. **`INDICATOR_VERSION` -> `1.4.36-1`.**

**1.4.37 - self-extracting payloads + comment-evasion repair (operator sample `f4590eb5.zip`, 2 files).** **Zero findings against everything through 1.4.36.** Mechanism: `explode(base64_decode('Pz4='), file_get_contents(__FILE__))` - **`Pz4=` is `?>`, so the file READS ITSELF, splits on its own closing tag, and executes what follows. The payload (16,668 bytes) is NOT in a string, NOT in a variable, NOT inside the PHP block - which is why 1.4.34's decoder-chain check, which requires a long encoded LITERAL, found nothing.** **SECOND EVASION, general and more important: it writes `/**\/@eval/**\/(` - PHP permits a COMMENT between a function name and its bracket, defeating every `\bfunc\s*\(` pattern in the scanner. New `WPS_Utils::strip_php_comments()` (hand-rolled state machine preserving string contents, so a `//` inside quotes survives) applied before call-matching. Kept SEPARATE from `deobfuscate_literals()` deliberately: that method compares a file against its own normalised form, and stripping comments too would make almost every file differ from itself and destroy the comparison.** New `check_self_extracting_payload()` requires all THREE legs - self-read of `__FILE__`, an execution sink, and >=512 bytes past the final closing tag - each asserted individually insufficient. **RATED 'high' NOT 'critical', DELIBERATELY: the sample carries a MONARX copyright header, and Monarx is genuine security software hosts install. Commercial security/licensing products (monarx, ioncube, sourceguardian, zend guard, phpshield, sitelock, imunify) protect their own code with this exact technique for the same reason malware does - the check CANNOT tell them apart. The finding names any recognised vendor, states plainly that both use it, and tells the operator to confirm with their host before deleting. A scanner that removes the host's own security agent has done more harm than the thing it hunted, and misplaced confidence would be the cause.** **The finding WORDING is asserted in the suite - must name a vendor when present, must not invent one when absent, must discourage deleting on this evidence alone.** 21-check suite. **`INDICATOR_VERSION` -> `1.4.37-1`.**

**1.4.38 - unauthenticated write endpoints; 3/63 -> 6 findings (operator sample `syringas_b01e44.zip`, 63 files).** **New family: a full FRAMEWORK, not a dropper - composer `vendor/` (markrogoyski/ipv4-subnet-calculator), `core/filemanager_*.php` (231KB), `core/panel_*.php`, `core/modules/{linkManager,ipManager,settingsManager}.php`, `config/settings.json` (2.8MB), word lists (`words_1.text`, `words_2.text`).** **MISSED COMPONENT: `core/backdor_15bd8c738253.php` - named by its own author - reads `php://input` and writes it to disk. TWO reasons it evaded, both Claude's: (1) the request-input test looked only for SUPERGLOBALS, and this takes data from the raw request body, so it did not look request-driven at all; (2) the check required TWO mutation primitives and this uses one.** Fixes: `php://input` added to the request-input pattern; **mutation floor dropped from 2 to 1.** 1.4.34 had already made an upload exception on the reasoning that no benign unauthenticated upload endpoint exists - **the same argument applies with equal force to ANY single write: putting attacker-controlled bytes at a path with nothing checking who asked IS remote code deployment.** **The two-primitive rule existed for false-positive control and was MEASURED to be buying nothing - across 79 files of real theme+plugin code, relaxing it adds ZERO findings, because legitimate code either loads WordPress, checks something, or takes no request input, and all three exits remain.** **Grading NOT flattened: filemanager (6 primitives) critical, backdor (1) high - asserted.** 1.4.37's comment stripping now applied to this check too, so primitives split from their brackets are found like the sinks were. 11-check suite; **fixtures1433's "a single write primitive alone does not fire" assertion updated rather than worked around - it was true and is now deliberately false, so it asserts the new behaviour AND the fixed-path case that still stays silent.** `INDICATOR_VERSION` -> `1.4.38-1`.

**1.4.39 - character-built identifiers; the first sample to defeat EVERY existing check individually (operator sample `vrhpkgc.zip`, Protect Uploads camouflage, 19,779-byte index.php, hash `e64ae2dff64a2e18` - a NEW payload, close to but distinct from the 19,513 decoder loader).** **TECHNIQUE: `$y = 'I could not have a more welcome visitor 64 group of zain bani'; $f = $y[15].$y[14].$y[13].$y[5].'('.$y[43]...; eval($f . 'eJyt/EnP...');` - the haystack sentence is deliberately innocuous and the function names (`base64_decode`, `gzuncompress`) are SPELLED OUT OF IT BY POSITION, one character at a time, then the call is assembled as text and executed. The names never exist as text anywhere in the file.** **FIVE checks blind to it, each for a DIFFERENT reason, all asserted: (1) nothing split -> `deobfuscate_literals()` finds nothing to rejoin, so `hidden_identifiers` sees no change; (2) no decoder NAME present -> `encoded_payload_loader` counts zero; (3) goto density 7, under the flattening floor of 20; (4) payload concatenated onto a built string, not a literal -> no blob to match; (5) not a file manager.** **SIGNAL: the construction cannot be avoided - spelling a name costs one indexing expression per character, joined in order. 28 chains in the sample; worst case across Rodolfo (production theme), the legitimate GPL Composer theme, and this plugin is THREE, in ordinary string handling.** Floor 8 chains (~3x the worst innocent case); 8 + execution sink = critical; 15 alone = critical without a sink. **THRESHOLD DERIVED NOT GUESSED: the size floor was drafted at 300 bytes, but a minimal carrier costs ~13 chains x ~8 bytes to spell ONE name, plus haystack plus eval - which lands close to 300. A floor set at the size of the thing it should admit excludes it. Set to 200 against that arithmetic. THIRD release in which a copied or guessed threshold would have excluded the smallest samples (cf. 1.4.33 150-byte floor, 1.4.36 2048-byte floor).** 22-check suite. `INDICATOR_VERSION` -> `1.4.39-1`.

**1.4.40 - COORDINATED IMPLANT; location as evidence; the Wordfence false positive that nearly shipped (operator sample `new__2_.zip` - a live wp-content slice, 70 files, Malay language packs + Wordfence installed, so almost certainly the operator's own site).** **TWO FILES, ONE IMPLANT, VERIFIED LINKED: `mu-plugins/db-connection-pool.php` (66KB, fake header "@package Post Format Handler / @author WP Infrastructure" - note the filename and internal name DISAGREE) assembles `d58ce737fcc9690ccfb6` from 295 `chr()` calls; `fonts/role-validator.php` returns immediately unless `$_GET['_cache_token']` starts with `d58ce737fcc9690c`. Same token, confirmed by decoding.** mu-plugins matters on its own - auto-loads, cannot be deactivated from the dashboard, the exact property that caused the 1.4.15 withdrawal. **GAP 1: `chr()` construction. 1.4.39 counts `$var[N]` index chains and scores this file ZERO - same technique, different mechanism. Now both counted (295 vs 0 across 80 clean files); the finding names which was used.** **GAP 2: EVERY check asked what a file CONTAINS; none asked WHERE IT IS. The fonts backdoor is token-gated so it returns nothing to a scanner and rated only 'high' on content - but a PHP file in fonts/ is wrong before it is read.** New `check_php_in_data_directory()`, **two tiers: STRICT (uploads/fonts/upgrade - location alone is the finding) and LOOSE (cache/wflogs/logs/tmp/backup - plugins DO write .php there, so a supporting signal is required).** **THE FALSE POSITIVE THAT NEARLY SHIPPED: the first draft flagged FOUR Wordfence files. Wordfence stores logs as `<?php exit('Access denied'); __halt_compiler(); ?>` + data - the extension prevents direct reading while nothing executes. Excluded now by SHAPE (immediate exit/die/__halt_compiler) rather than by naming vendors, because the principle generalises and a vendor list would not. Reporting a security plugin as an intrusion is the false positive that teaches an operator to ignore the scanner, after which it protects nothing - this is asserted FIRST in the suite.** 17-check suite. `INDICATOR_VERSION` -> `1.4.40-1`.

**1.4.41 - CREDENTIAL EXFILTRATION; the largest gap in the scanner, exposed by an entirely ordinary file (operator sample `new.zip`, a live wp-content slice including the real `wp-config.php`).** **`.wp-config-cache.php` (972 bytes, DOT-PREFIXED to hide from listings, fake header "WordPress Object Cache v2.1"): exits if requested directly; requires ABSPATH/SHORTINIT; `register_shutdown_function` waits for a logged-in ADMINISTRATOR, rate-limits itself to once per admin per 12h via a transient (`ct_{ID}`) to keep traffic low, harvests every `wordpress_*`/`wp-*` cookie and posts them to `https://webanalytics-cdn.sbs/k` with `blocking=false`.** **THIS IS COMPLETE ACCOUNT TAKEOVER WITH NO PASSWORD, and INVISIBLE to a login guard because NO LOGIN EVER FAILS. It explains the operator's long-standing unexplained symptoms: new admin accounts with registration OFF, the tamper-evident log's chain verification failing (they hold admin, so they edit the DB legitimately), and the wp-config.php change.** **WHY EVERY CHECK PASSED IT AND WAS RIGHT TO: short, readable, unobfuscated, correct `ABSPATH` guard, in a directory where PHP belongs, writes NOTHING to disk. Every check here reasons about how code HIDES or what it WRITES. None asked WHAT LEAVES THE SITE.** New `check_credential_exfiltration()`: requires **BOTH** an outbound call (`wp_remote_*`/`curl_exec`/`fsockopen`/`file_get_contents('http...')`) to a **hardcoded host**, AND genuinely sensitive payload (`foreach($_COOKIE)`, `wordpress_logged_in`, `$_POST['pwd'|'log']`, `user_pass`, `AUTH_KEY`/`DB_PASSWORD`). **Both halves asserted separately insufficient - an API call with a version string does not fire; reading cookies without sending does not fire.** Known WordPress hosts grade 'high' not 'critical' but are STILL reported, because no legitimate component sends session cookies anywhere. **THE WORDING IS THE FEATURE: the finding states that DELETING THE FILE IS NOT SUFFICIENT - sessions already taken stay valid until the wp-config.php salts are rotated, and the order matters (remove first, THEN rotate, or the new session is captured too). Asserted in the suite.** 26-check suite. `INDICATOR_VERSION` -> `1.4.41-1`.

**1.4.42 - duplicate-finding grouping (presentation, no detection change).** A re-drop kit plants one payload across a dozen randomly-named directories so that deleting one achieves nothing; the scanner reported a dozen separate identical criticals, burying every other finding and never stating that they were ONE intrusion. New `WPS_Scanner::group_duplicate_findings()`: **groups by `md5( type ) . ':' . md5_file( path )` - by CONTENT not name, because the name is what the attacker regenerates.** Collapsed finding leads with the copy count, carries every path in a new `paths` key, prose truncates at 12 with "and N more" while retaining all paths, worst severity in the group governs, and the check's ORIGINAL action text is preserved rather than replaced. **Three deliberate non-groupings: one file caught by TWO checks stays two findings (they say different things); a finding with no path is never grouped; a finding whose file has vanished passes through rather than being dropped - a scan that quietly loses results is worse than one that repeats.** **THE ORDERING IS THE DESIGN POINT: grouping runs AFTER `auto_remediate()`, not before. If it ran first the remediator would see ONE finding where TWELVE files exist, delete one copy, and report the job done - leaving eleven on disk and the operator believing otherwise. Asserted directly against the source (grouping call position > remediation call position).** 18-check suite. **PROVENANCE NOTE: a `wp-perf-shield-1.4.42.zip` of unknown origin was found in the outputs directory during this session - it lint-checked clean and implemented equivalent grouping, but neither Claude nor the operator placed it there, so it was NOT used. This release was rebuilt from the verified 1.4.41 package instead. On a compromised-site investigation, an unexplained artefact is not something to trust merely because it looks correct.** `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.43 - blocking AND removal for credential exfiltration (operator request following the `.wp-config-cache.php` discovery).** 1.4.41 could FIND a session stealer but not stop one, leaving it working through every page load between the scan and the operator acting - **for most malware that gap is tolerable; for this class each admin page load in it is another stolen session.** **BLOCKING: new `WPS_Blocker::guard_outbound_request()` on `pre_http_request` (priority 1, registered via `wps_boot`). Refuses any wp_remote_* call whose body matches `wordpress_logged_in_*`/`wordpress_sec_*`/`wp-settings-time-*` to a non-allowlisted external host; logs `exfiltration_blocked` naming the host AND stating the salt rotation; raises a critical EDR event. Narrow by design: the site's own host (bare and www), wordpress.org/akismet.com/gravatar.com/w.org, and any body with no session material all pass. Array bodies are FLATTENED first so nesting does not evade. Returns `$pre` untouched if another filter already answered.** **HONEST LIMIT WRITTEN INTO THE CODE, not just the docs: it sees only the WordPress HTTP API - `curl_exec`/`fsockopen`/raw streams bypass it entirely. Closes the common case (implants inside plugins reach for `wp_remote_post`), not the category. Asserted in the suite that this text exists.** **REMOVAL: exfiltration findings now set `auto_delete`/`delete_path`, reusing the EXISTING quarantine-first remediator rather than adding deletion code - already reversible (30-day quarantine), already skips protected theme/core paths, already records redrop hashes. Deliberately WITHHELD when the destination is a WordPress-owned host: those grade 'high' precisely because they might be a misreading, and a misreading must never delete.** **NEITHER HALF UNDOES THE THEFT - sessions already sent stay valid until the wp-config.php salts rotate. The block's log entry says so, because it fires when the operator is most likely to be reading.** 22-check suite. `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.44 - documentation catch-up and TWO STALE CAPABILITY CLAIMS CORRECTED (operator asked whether the docs had kept up; answer was no).** **Nine detections added across 1.4.33-1.4.43 (`unauthenticated_file_manager`, `encoded_payload_loader`, `hardening_bypass_config`, `hidden_identifiers`, `obfuscated_js_payload`, `self_extracting_payload`, `character_built_identifiers`, `php_in_data_directory`, `credential_exfiltration`) appeared in NONE of `doc/readme.md` or the in-plugin Docs tab - version lines were bumped and changelogs written each time, but the reference document was never touched.** **WORSE, AND FOUND WHILE FIXING IT: `readme.txt` lines 20 and 45 (Description + Features, CURRENT TENSE) still claimed "protects itself with a must-use guard that restores it if something disables it" - a feature WITHDRAWN IN 1.4.15 after two production outages, so the claim had been FALSE FOR 28 RELEASES. `doc/readme.md` carried the same claim. Claiming a protection that does not exist is worse than claiming nothing: a reader would conclude the plugin cannot be quietly disabled, which is precisely the assumption the withdrawal removed. All three current-tense claims corrected; CHANGELOG entries describing the guard deliberately LEFT, because those are accurate history rather than a promise.** `doc/readme.md` gains a "What the scanner detects" section organised by INTENT not check name (code that hides what it is / does not bother to hide / changes the ground rules / steals), each entry stating the technique AND what clears a file - a reader deciding whether a finding applies needs the exits as much as the rule. Sign-in protection documented in one place for the first time, having accumulated across seven releases. **The salt-rotation instruction now appears in FOUR places (reference doc, upgrade notes, the finding itself, the runtime block log) because it is the one instruction in this plugin that fails SILENTLY if missed.** Docs tab renders these files directly, so it updates with the same change. No code changed; full gate run to prove it. `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.45 - documentation presentation and branding (operator: "make sure the plugin branding is evident... you are using markdown arent you? then use it!").** The 1.4.44 detection reference was accurate but was a WALL OF PROSE in a renderer supporting tables, fenced code, blockquotes and nested lists, and said "the plugin" throughout documentation shipped INSIDE the plugin. **VERIFIED THE RENDERER BEFORE WRITING rather than assuming: test-rendered each markdown feature through the bundled `WPS_Parsedown` - tables, fenced code, blockquotes, inline code, links, nested lists, bold, italic ALL render. Writing tables into a parser without table support would have shipped a page of pipe characters. (The test initially returned silence because the vendor file correctly `exit`s without `ABSPATH` defined - correct behaviour that briefly looked like failure.)** Reference now: 4 tables (one per technique category), with **"what clears a file" columns for the two most false-positive-prone checks - a reader deciding whether a finding applies needs the exits as much as the rule, and a table puts them side by side**; sign-in rules/thresholds as a table; **the salt-rotation instruction as a blockquote callout with the secret-key URL and the reason order matters - it is the one instruction in this plugin that FAILS SILENTLY when missed, so it now LOOKS different from its surroundings**; `WPS_DISABLE_LOGIN_GUARD` as a fenced PHP block because whoever needs it is locked out. Branding: 13 mentions in the reference, branded titles + purpose lines on upgrading.md and changelog.md. **All four bundled docs re-rendered through the plugin's own parser and output counted, including a check for ACCIDENTAL tables (prose containing a pipe can become one) - none found.** No code changed. `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.46 - SAFE MODE ON A LIVE SITE; the scan's time budget exceeded what PHP allows (operator report: "Maximum execution time of 30 seconds exceeded" at `class-scanner.php:6161`).** **ROOT CAUSE, CLAUDE'S: `SCAN_TIME_BUDGET_SECONDS = 45` while the host's `max_execution_time = 30`. A self-imposed limit ABOVE the limit PHP enforces is not a limit - the scan intended to stop 15s AFTER the point PHP would kill it, so every scan on that host was always going to fatal and the budget mechanism was decorative.** **SECOND HALF: the budget was tested only BETWEEN checks. `compare_plugin_files()` hashes every file in every installed plugin in one pass with a cap on file COUNT and none on TIME - and a count cap does not help when the per-file cost is a hash. Once it started, nothing interrupted it. Eleven detections were added across 1.4.33-1.4.43, each walking the filesystem, and the budget meant to absorb that growth could never fire.** Fix: new `scan_budget_seconds()` derives the budget from `ini_get('max_execution_time')` minus `SCAN_BUDGET_HEADROOM = 8`, floored at 5s, capped by the 45s ceiling; `max_execution_time = 0` (CLI/unlimited) falls back to the ceiling. **30s host -> 22s budget.** New `scan_budget_exceeded()` tested INSIDE the long loops - 11 call sites including `compare_plugin_files()` - not only between checks. 15-check suite; **the load-bearing assertion is that the budget is STRICTLY UNDER `max_execution_time` at 30/45/60/120 - the assertion that would have caught this before it shipped.** **LESSON: a self-imposed resource limit must be DERIVED from the enforced limit, never assumed - and a cap on iteration count is not a cap on time when the per-item cost is unbounded.** No detection logic changed. `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.47 - scan performance; making scans FINISH rather than merely fail safely (operator: "it is making the plugin ineffective and i just got hacked").** 1.4.46 stopped the fatal but left partial scans, which under active attack is the difference between broken and useless. **MEASURED, not guessed: `strip_php_comments()` + `deobfuscate_literals()` on a 398KB file costs ~50ms (both are character-level PHP loops). SIX content checks were doing this to every file INDEPENDENTLY -> 300 such files = 85s, of which 71s was the same work repeated. The cost was never the normalising; it was doing it six times.** Fix: `WPS_Utils::normalised()` / `stripped()` cache the transformed form per scan; all 5 call sites in the scanner rewired; cache released at scan end. **Measured 0.236s -> 0.058s for a full pass over one 398KB file (4x); 71s -> 17s extrapolated over 300.** **BUG CAUGHT BY THE SUITE: the first draft keyed on path+size+mtime, and a test that edited a file got the PREVIOUS answer back - PHP caches `stat()`, so `filemtime()` reported the old value. That is the SAME `clearstatcache` defect recorded in 1.4.2 and 1.4.13. Rather than add another `clearstatcache()` and hope it is remembered next time, the key is now `md5( $raw )` - the content IS the input being transformed, so it is the correct key, and the class of problem disappears rather than being worked around.** Bounded by TOTAL BYTES (8MB) not entry count, because one enormous file can cost more than a thousand small ones - the 1.4.13 distinction; past the ceiling files are still normalised correctly, just uncached (slower, never wrong). 13-check suite asserts cached output byte-identical to uncached, six passes <half the time, an edited file returns new content, the byte ceiling holds under 8x2.5MB, and the cache is released at scan end. `INDICATOR_VERSION` unchanged at `1.4.41-1`.

**1.4.48 - kit identification made rename-resistant; the one-word off switch (operator sample `annealing_1af540`, 126 files, the `syringas` framework from 1.4.38 with a full TDS engine added).** Kit: root stubs `define('IS_INCLUDED')` + include; `core/core.php` reads a GET parameter, geolocates the visitor via `ip-api.com`, filters against whitelists/blacklists of IP, UA, referer, country, org, ISP and ASN, then emits a `window.location.replace` redirect; `core/backdor_<hex>.php` takes POSTed JSON and APPENDS `$data['code']` to an included file (unauthenticated remote code deployment); `config/settings.json` 2.8 MB carrying **111,088 blocked IPs, 87 orgs, 569 UAs**, kit self-versioned `1.10.7`. **1.4.47 DETECTED IT - three ways, zero false positives across the Composer vendor code in the same tree. The release exists because of what happened next: the sample was RENAMED (`core`->`lib`, the hex-suffixed `backdor_`/`panel_`/`filemanager_` files given ordinary names, `panel_kee`->`access_token`) and re-measured. `check_doorway_backdoor_kit` went SILENT, `check_doorway_cloak_config` went SILENT, `check_file_manager_shell` dropped critical->high. The kit still worked.** **ROOT CAUSE: every kit-LEVEL cue was keyed on a name the attacker chooses.** `doorway_kit_cue_score()` scored only filenames (`backdor*` +2, `panel_<hex>` +1, `filemanager_<hex>` +1, the exact module trio +1, directory named `core` +1); score fell 6 -> 1 against a threshold of 3. And `check_doorway_cloak_config()` gated its ENTIRE body on `stripos($head,'panel_kee')` - **a single literal that NOTHING IN THE KIT READS, i.e. a one-word off switch for a whole check**. The per-file shell checks still fired, so the kit was not invisible; **what was lost is the only finding that says THESE FILES ARE ONE KIT AND HERE IS ITS ROOT, which is the sentence remediation depends on.** **FIX - cues the kit cannot rename without breaking itself:** (1) `cloak_config_profile()` extracted as the single shared answer to "is this JSON a cloaking config" (used by both the config check and the kit scorer, so the two cannot drift); pre-filter now requires **3 of the catalogued keys in the 64 KB head** instead of one literal, then 4 on decode, then an evasion list >=100. **The config's key vocabulary is load-bearing because the kit's own PHP reads it as literal subscripts - rename one side and the other breaks.** (2) New `find_cloak_config_reader()`: a file that reads **>=4 cloak switches by name AND emits a redirect** (header Location / `window.location.*` / 30x response code). **BOTH halves required and each asserted separately insufficient - legitimate code redirects constantly, and a firewall plugin legitimately holds deny lists.** (3) Re-drop fingerprint fell back from the hardcoded `core.php` to the identified redirector, so renaming that file no longer costs the re-drop history. New indicator list `WPS_Indicators::doorway_cloak_flag_keys()`. **TWO FALSE POSITIVES IN CLAUDE'S OWN FIRST DRAFT, both caught by MEASURING not reading: (a) it named the SITE'S WEB ROOT as a doorway kit** - the config cue searched a candidate's parent, so a kit one level down implied the directory above it; fixed by requiring a located config to imply the SAME kit root as the candidate that found it. **Reporting a document root as an intrusion is the false positive that teaches an operator to ignore the scanner - the same principle recorded in 1.4.40 over the Wordfence near-miss.** **(b) one intrusion reported THREE times, nested** (`config/` scored, `include/` scored, each naming its own parent); fixed with an outermost-root collapse, since deleting that takes the rest. **COST MEASURED BEFORE SHIPPING, because 1.4.46 and 1.4.47 were both about scan time and this is the same multiply-across-the-tree shape: 720 candidate directories went 0.041s -> 0.178s (4.3x). Bounded two ways - the config profile is memoised for one scan pass (path-keyed, bounded at 512, DELIBERATELY not content-keyed unlike the 1.4.47 cache, because here a stale answer moves a cue by one scan rather than corrupting a finding, and hashing multi-MB blocklists would cost more than the problem), and the expensive reader cue runs ONLY on directories that already scored, since its +2 against a threshold of 3 cannot change an outcome alone. Re-measured 0.091s: about 50ms of real added cost across 720 directories, stated rather than hidden.** 30-check suite; **the refusals carry as much weight as the detections** - web root and `wp-content` each asserted never reported as a kit root, a fixture firewall plugin with a 600-address deny list and real redirects asserted silent, the Composer vendor tree inside the malware asserted silent, self-flagging asserted absent, and a 3.3 MB unrelated JSON asserted rejected on its head without being decoded. **Harness defect fixed during the work (the recurring shape): the redirector fixtures shared a parent with the JSON fixtures, so a cloak config from an earlier test was in scope and scored a cue the test asserted absent - harness wrong, code right, but the same mistake would hide a real regression.** **STALE CLAIM CORRECTED: `doc/upgrading.md` still promised in the present tense that "the tamper guard restores it" - withdrawn in 1.4.15. 1.4.44 corrected exactly this claim in `readme.txt` and `doc/readme.md` and MISSED THIS COPY, so it stood four further releases. A capability claim must be hunted across EVERY file that makes it, not the files that came to mind.** `doc/readme.md`'s "rename-proof matching" line was likewise overstated (1.4.1 relaxed the directory name only, leaving filenames and `panel_kee` load-bearing) and now describes what is actually rename-resistant. **HONESTY NOTE, RECORDED DELIBERATELY: the build container had been reset, so the ~40 harnesses from earlier releases did not exist and were NOT re-run. The WordPress stub and reflection harness were rebuilt from scratch this session. The changelog says so; prior-suite regression is NOT claimed for this release.** **PRINCIPLE: a detection keyed on a name the attacker chooses is a detection with an off switch, and a single-literal pre-filter is the smallest possible one. Match what the malware cannot change without ceasing to work.**

**1.4.49 - the two 1.3.79 variants were CATALOGUED BUT NEVER BLOCKED (operator sample `site-security-toolkit-1f30.zip`, one file, 9,674 bytes, MD5 `608576a9322aab3585fe7e7eb109f368`, SHA-256 `3bb3738a...`, byte-identical to the 1.3.79 catalogue entry - Plugin Name "Site Security Toolkit", Author "Cache Solutions", class `Core_Loader_c8fc`, option `wp_1f20bc3f7f_cfg`, placeholder `Plugin URI: https://developer.wordpress.org/plugins/`).** **DETECTION WAS FINE - six checks fired (`check_clickfix_shape_heuristic` 6/6 traits, `check_plugin_metadata_camouflage`, `check_character_built_identifiers`, `check_hidden_identifiers`, `check_cloaked_injector`, `check_split_literal_obfuscation`), four critical, two with auto-delete. The gap was on the BLOCKING side and was found by measuring it rather than assuming it: the slug was not on `WPS_Blocker::get_blocked_slugs()`.** **WHAT 1.3.79 ACTUALLY DID: it recorded these two variants in exactly two places - the option key in `malware_option_keys()`, and the shell-company author strings ('Cache Solutions' / 'WP Solutions') as ONE TRAIT in the scanner's scored heuristic, written as trailing comments. It reached NONE of the five places 1.3.69 wired `total-database-optimizer` into: the default slug list, the two suffix regexes, the MD5 list, the SHA-256 list, and the ZIP content-signature list. So for thirty releases both variants were findable and not preventable.** **THE SUFFIX PATTERNS ARE THE OPERATIVE PART: the folder in the wild is `site-security-toolkit-1f30`, so a bare-slug entry alone would have matched nothing - the same reason every prior family in the list carries `slug[-_][a-z0-9]{3,8}` patterns for both the directory and the `.php` form.** **FIX: both slugs across all five layers, matching the 1.3.69 precedent. Hashes ONLY for the sample in hand (hashed this session, not read back from a catalogue). NO HASH ADDED FOR `auto-asset-helper` - that file was not available, and a hash written from memory silently matches nothing while looking like coverage; its slug/patterns/signature are structural and safe without the file. DECISION RECORDED AND ASSERTED IN THE SUITE: the shell-company author strings were deliberately NOT added to `zip_content_signature()`, because that list blocks on a SINGLE match and "WP Solutions" is a plausible genuine vendor name; they stay as weighted traits in the scanner heuristic where other evidence must agree.** **SEVERITY IMPROVED AS A SIDE EFFECT, and the mechanism is worth knowing: `check_clickfix_shape_heuristic` deliberately SKIPS slugs already on the blocklist (exact match covers them), so the sample moved from `check_clickfix_shape_heuristic` at HIGH to `check_plugin_folders` at CRITICAL - six findings before, six after, better classified.** **SIXTH PINNED-ASSERTION HARNESS DEFECT: the suite asserted the shape heuristic by NAME and failed when it correctly stood down; rewritten to assert that a folder-level check reports the folder at critical, whichever check does it - after 1.4.5, 1.4.8, 1.4.14, 1.4.16, 1.4.26. The recurrence rate of this one defect shape is itself the finding.** 22-check suite; blocking asserted in the hex-suffixed form AND with an underscore separator, refusals asserted for a differently-named security plugin, a plugin sharing one word, a repository plugin, and WP Perf Shield itself; detection asserted UNCHANGED at six checks so blocking has not quietly displaced finding. 1.4.48 suite re-run as regression, 30/30. **PRINCIPLE: cataloguing a variant is not the same as covering it. When a family is added, it must be wired through EVERY layer that family can arrive by - activation, upload, hash, signature, option - and the check that it was is a measurement, not a memory.** `INDICATOR_VERSION` -> `1.4.49-1`.

**1.4.50 - blocking a SHAPE rather than a name; the theme-loader JS injector family (operator sample `Plugin-7e4eb3ff.zip`, one file, 130,672 bytes, MD5 `73f07f1438b9a710b5bf1893186d1e67`, SHA-256 `eb45ec5c...`).** Family: PHP wrapper, correct header (`Description: simple js plugin`), valid ABSPATH guard, two heredocs (`ENDPLUGINJS`, `ENDPLUGINFN`), 121,167-byte single line of javascript-obfuscator output, `file_put_contents` into `get_template_directory()` as `css.js`, script tag injected via `get_template_directory_uri()`, hooks `init` + `admin_notices`. **DETECTION WAS ALREADY STRONG - 4 checks, ALL critical, 3 auto-delete (`check_php_signatures`, `check_obfuscated_js_payload`, `check_theme_loader_plugin`, `check_obfuscated_js_injector`). BLOCKING WAS ABSENT ON EVERY LAYER: not activation-blocked, not upload-blocked by entry name, no hash catalogued, no content signature. Confirmed END TO END through the real `block_zip_upload()` handler with the strict gate disabled so only the malware nets decided - the ZIP was ACCEPTED.** **THIRD CONSECUTIVE SAMPLE EXPOSING THE SAME 1.3.79 SEAM (after `site-security-toolkit` in 1.4.49): a family catalogued for DETECTION and never wired for BLOCKING. `ENDPLUGINJS` was already sitting in the scanner's marker list and in `WPS_Indicators`, and had simply never been handed to `WPS_Blocker::zip_content_signature()`.** **THE STRUCTURAL DIFFERENCE FROM EVERY PRIOR ENTRY: this family has NO FIXED SLUG. It names itself `Plugin-<8 hex>`, regenerated per drop - `Plugin-7e4eb3ff` here, `Plugin-390a770b` in the 1.4.36 sample. The slug list does substring matching on literals and cannot express that, so the entry lives in `get_patterns()` and this is the project's FIRST blocking rule that matches a FORM rather than a STRING.** Added: `/(^|\/)Plugin-[0-9a-f]{8}\//i` and the `.php$` companion (both `is_blocked()` and `zip_entry_name_matches()` read the same pattern list, so activation and upload are covered by one change), MD5 + SHA-256 from the sample in hand, and `ENDPLUGINJS`/`ENDPLUGINFN` as upload content signatures. **DECISION RECORDED AND ASSERTED: `Description: simple js plugin` was NOT added to the single-match upload signature list - it is a plausible description for a genuine plugin, and the indicator catalogue already notes it may vary between variants; it stays a scanner signal where other evidence must agree. Same reasoning as the 'WP Solutions' refusal in 1.4.49.** No hash invented for `Plugin-390a770b` (file unavailable). **A SHAPE PATTERN IS A WIDER NET THAN A NAME, so the suite spends most of its weight on the edges: seven hex digits, nine hex digits, eight non-hex characters, `plugin-directory`, `my-plugin-helper`, a repository plugin, and WP Perf Shield itself are each asserted NOT blocked; exactly eight hex digits is the whole rule.** Upload blocking asserted END TO END rather than by list inspection - sample rejected, same payload in an innocuous folder rejected, modified build with BOTH hashes missing rejected (caught by the heredoc signature), and two genuine plugins accepted including one that legitimately serves JS from the theme directory. **Detection improved 4 -> 5 checks as a side effect (exact folder identification joined the structural ones), and the structural detections are asserted still firing so blocking has not displaced finding.** 24-check suite; 1.4.48 (30/30) and 1.4.49 (22/22) re-run as regressions; family audit still reports 0 of 24 declared families unstoppable in randomised form. **PRINCIPLE EXTENDED FROM 1.4.49: when a family is catalogued, wire it through every layer it can ARRIVE by - and when the family has no stable name, block the shape it cannot vary, because the randomised part is exactly the part the attacker controls.** `INDICATOR_VERSION` -> `1.4.50-1`.

**1.4.51 - the variant catalogue; completeness made checkable (operator request: "there should be a document tracking all these malware variants and their details... they must all be documented without fail").** New bundled `doc/variants.md`, registered in `WPS_Admin_Docs::documents()` as the fifth document: 15 family records in a fixed schema (classification / delivery / mechanism / indicators / detection / blocking / remediation), a cross-family technique table mapping technique -> families -> checks, and six appendices (C2 hosts, option keys, filenames, hardcoded accounts, cron fragments, complete hash reference). **The hash appendix is GENERATED FROM `class-blocker.php` rather than transcribed, so it cannot drift from the list it documents.** **ATTRIBUTION IS MARKED THROUGHOUT - Verified (sample examined, hashes computed from the file: `total-database-optimizer-9a95`, `site-security-toolkit-1f30`, `Plugin-7e4eb3ff`, `annealing_1af540`), Catalogued (recorded from earlier analysis, not re-examined), Structural (no fixed indicator exists). A catalogue that blurs those three will eventually mislead an analyst, and most of this one is Catalogued rather than Verified.** **THE LOAD-BEARING DECISION: completeness is ASSERTED, NOT CLAIMED. The suite reads every value out of every array-returning `WPS_Indicators` accessor plus `get_blocked_slugs()` and `get_blocked_hashes()` - 247 values - and FAILS if any does not appear in the document. "Without fail" is only meaningful if it is mechanically checkable, and a hand-written catalogue rots silently otherwise.** **WHAT THE CHECK FOUND THAT READING WOULD NOT: 58 gaps on the first pass - 14 indicator values (4 doorway flag keys, 10 crawler UAs, the 4 cache-suppression constants, a payload marker regex), 5 ClickFix slugs with no entry, and 33 absent hashes. It also surfaced TWO FAMILIES that existed ONLY as comments beside a hash entry and had never been written up anywhere: the TDS drive-by injector (`tji-site-js.php`, mu-plugin disguised as "Theme JS Injector", routes through `ntdnewtds.shop/jsrepo` + `dnsnewtds.shop/jsrepo`, MD5 `47ff560f...`) and the user-hiding filter installer (`wp-security-helper.php`, five filters hiding every user except the logged-in admin plus self-concealment from the Plugins page unless `?sp`, fake author "WordPress Security Team" - same as `wp-security-cache.php`, confirming one operator chain, MD5 `50c02424...`). Both now have full records; the credential-exfil staging file `Stained_Heart_Red-600x500.png` gained one too.** **MISATTRIBUTION OF CLAUDE'S CORRECTED BEFORE SHIPPING: `ntdnewtds.shop`/`dnsnewtds.shop` had been written into the doorway/TDS kit record because both concern traffic distribution. They belong to the drive-by injector - a DIFFERENT operator chain found co-resident on one victim site. The document now states this explicitly, because co-residence is not evidence of a shared operator (the same caution recorded in 1.3.83 about the annealing/resweep family).** **DEAD BLOCKLIST ENTRY FOUND BY COUNTING CHARACTERS: one hash was 65 characters, labelled "SHA-256, XOR 84". SHA-256 is 64, so the value could never equal any hash and had been INERT SINCE IT WAS ADDED - an entry that looked like coverage and provided none. REMOVED RATHER THAN CORRECTED: the XOR-84 sample is not held and guessing which character was spurious would be INVENTING A FINGERPRINT; that build remains covered by its MD5 and the structural ClickFix checks, neither of which ever depended on it. Hash hygiene now asserted - every entry exactly 32 or 64 hex characters, no duplicates, no non-hex.** 15-check suite: completeness, hash hygiene, Docs-tab registration, slug resolution, **the traversal refusal asserted STILL working after adding a document** (a new registry entry is exactly when a path check gets loosened by accident), rendering through the bundled `WPS_Parsedown` as 19 tables / 203 rows with no leaked delimiters, and record-shape assertions so a future family cannot be added without Detection and Blocking lines. 1.4.48 / 1.4.49 / 1.4.50 suites re-run as regressions. **NO DETECTION OR BLOCKING BEHAVIOUR CHANGED** beyond removing the dead hash. **PRINCIPLE: documentation that describes a list must be verified against the list by a machine, or it becomes fiction at the speed the list changes - and the verification is worth more than the document, because it is what found the two undocumented families and the dead entry.** `INDICATOR_VERSION` -> `1.4.51-1`.

**1.4.52 - opt-in public identification marker (operator asked how to get WP Perf Shield to appear in Wappalyzer).** **ROOT ANSWER, ESTABLISHED BY READING THE CODE NOT BY GUESSING: it could not appear, because the plugin's front-end footprint is ZERO BYTES. `assets/css/admin.css` and `assets/js/admin.js` enqueue only when `$hook_suffix === 'tools_page_wp-perf-shield'`; the sole front-end hook is `send_headers` for the OPT-IN CSP header, which carries no identity. Technology profilers match what an anonymous visitor receives - HTML, script paths, headers, cookies - so there was no pattern any of them could have matched. Site Kit appears in those directories because it DELIBERATELY emits `<meta name="generator" content="Site Kit by Google <version>" />` (their own docs call it the only front-end code the plugin adds); Akismet appears incidentally via comment-form assets. WP Perf Shield does neither.** New `WPS_Public_Marker` (`includes/class-public-marker.php`), registered in the autoload map and hooked to `wp_head`; new setting `public_marker` with its own "Public identification" card on the Settings tab, built on the shared `.wps-toggle-row` pattern with zero inline styles. **THREE DESIGN DECISIONS, EACH ASSERTED RATHER THAN INTENDED: (1) OFF BY DEFAULT and fails closed - a missing key, an empty array, a non-array option, or any value other than exactly `'1'` all yield silence. Announcing which security software a site runs is reconnaissance; that trade is the operator's to make, and for this category silence is the correct default. (2) NEVER THE VERSION - THE LOAD-BEARING ONE. 1.4.48-1.4.51 each closed a specific evasion, so a version string in page source is a lookup table telling an attacker which bypasses still work against that install. Asserted three ways: the version appears neither in the tag nor anywhere on the rendered settings page, no version-shaped `\d+\.\d+\.\d+` appears in the tag, and the class contains NO reference to `WPS_VERSION` at all. There is deliberately no option to include it. (3) REMOVABLE - a NAMED METHOD not a closure, so `remove_action( 'wp_head', [ 'WPS_Public_Marker', 'render' ] )` works, plus a `wps_public_marker` filter; a non-string filter return degrades to silence rather than fataling. Site Kit used an anonymous closure and collected years of support requests about it - a documented failure mode, avoided by reading someone else's.** **FAILURE MODE DESIGNED OUT: `WPS_Admin::save_settings()` REBUILDS the entire option array from `$_POST` rather than merging, so a key absent from that handler is not merely unsaveable - it is WIPED on every save of anything else. The suite asserts the key is handled there, because the symptom is otherwise a setting that appears to work and silently reverts. Related and why the check exists: the 1.4.28 checkbox default-drift bug, where a rendered default disagreeing with a code default silently disabled a running feature on the next unrelated save; here the rendered default is asserted against the code default in BOTH states.** 32-check suite. **RENDERED OUTPUT ASSERTED, NOT SOURCE - the settings page is rendered through `WPS_Admin_Settings::render()` and its emitted HTML tag-balanced (5 tables / 14 rows / 7 divs / 19 labels), per the 1.4.28 rule that conditionally-built markup can emit an unclosed tag that reading the file never reveals.** New `doc/wappalyzer-submission.md` carrying the fingerprint JSON, category identifiers, and both routes (**Wappalyzer closed its repository in 2023 so the product takes suggestions only via a web form; the OSS forks - tunetheweb/wappalyzer, enthec/webappanalyzer, dochne/wappalyzer - still accept PRs and feed much of the tooling researchers use**). **SEVENTH PINNED-LITERAL HARNESS DEFECT FIXED: `wp-stubs.php` pinned `WPS_VERSION` to `'1.4.47'`, so "the version never appears" was asserting against a string that was never going to appear. The stub now derives it from the plugin source by regex. After 1.4.5, 1.4.8, 1.4.14, 1.4.16, 1.4.26 and 1.4.49 - and this one was in Claude's own harness rather than inherited.** **NO DETECTION OR BLOCKING LOGIC CHANGED.** **PRINCIPLE: a security tool may advertise its presence if its operator chooses, but never its version - presence is a deterrent, version is a targeting aid, and the two are not the same disclosure.** `INDICATOR_VERSION` -> `1.4.52-1`.

**1.4.53 - the hash 1.4.49 refused to invent (operator supplied `auto-asset-helper-2763.zip`, one file, 10,739 bytes).** **1.4.49 wired both 1.3.79 variants through the blocking layers and stopped one short DELIBERATELY, recording why: `auto-asset-helper` got slug, patterns and upload signature but NO HASH, because the sample was not in hand and "a hash written from memory silently matches nothing while looking like protection". The operator has now supplied the file, so the refusal resolves the only way it legitimately can - by measurement.** **VERIFIED FIELD BY FIELD AGAINST THE 1.3.79 CATALOGUE ENTRY RATHER THAN ASSUMED: MD5 `7bbf81ab731b59b3c0fed628c1f3cf3d` (matching what was recorded), SHA-256 `de3bc67f...a451` (new), 10,739 bytes, class `Res_Helper_ad74`, option `wp_8447aa87d2_cfg`, author "WP Solutions", placeholder `Plugin URI: https://developer.wordpress.org/plugins/`.** Coverage measured BEFORE any edit: activation blocked, upload blocked (matched on the `Auto Asset Helper` plugin-name signature), option key catalogued, 6 checks detecting at 4 criticals - **only the hash layer open, exactly where 1.4.49 left it.** Both hashes added; `auto-asset-helper` now appears 6 times in `class-blocker.php`, at parity with `total-database-optimizer` and `site-security-toolkit`. Catalogue entry moves **Catalogued -> Verified**, which is the point of marking confidence at all: the distinction must be able to CHANGE when evidence arrives, or it is decoration. **ASSERTION RETIRED PROPERLY (the 1.4.38 precedent): fixtures1449 asserted "no hash was invented for the variant not in hand" by counting mentions - true when written, now deliberately false. Updated rather than deleted or worked around, and updated to test the PRINCIPLE rather than its proxy: the stored MD5 and SHA-256 are now asserted EQUAL to `md5_file()` / `hash_file()` of the sample itself, so "a hash must be computed from a file, never remembered" is tested directly.** **THE 1.4.51 COMPLETENESS GATE FIRED IMMEDIATELY AND CORRECTLY - adding two hashes broke `fixtures1451` before a word of documentation was written, which is precisely why it exists; `doc/variants.md` Appendix F was regenerated from source (41 MD5 / 31 SHA-256).** Detection asserted UNCHANGED at 6 checks, so exact identification has not displaced the structural detections that catch unseen builds. 1.4.48-1.4.52 suites re-run as regressions. **PRINCIPLE CONFIRMED IN PRACTICE: refusing to record an unverifiable indicator costs nothing but a later release, and leaves the catalogue honest in the meantime. The alternative - a plausible-looking hash that matches no file - would have been indistinguishable from coverage until the day it mattered.** `INDICATOR_VERSION` -> `1.4.53-1`.

**1.4.54 - the brand mark (operator asked "what about the plugin icon for Wappalyzer" after the 1.4.52 submission draft required one).** **THERE WAS NO ICON. Fifty-odd releases and zero image assets - no SVG, no PNG, no ico; the UI borrows WordPress dashicons for buttons and nothing else. The 1.4.52 submission document had a placeholder line saying an icon was required, which is where this surfaced.** **DESIGN REASONING, GROUNDED IN THE SUBJECT RATHER THAN THE CATEGORY: the default mark for a WordPress security plugin is a blue shield, optionally with a padlock - Wordfence, Sucuri, MalCare and Solid Security all sit there, so a plain shield is INVISIBLE in a directory listing beside them. The mark is a shield silhouette (category read, and the product is literally named Shield) containing THREE ASCENDING BARS knocked out in white (identity: this plugin exists BECAUSE malware impersonates performance plugins - the whole ClickFix family wears names like `wp-perf-analytics`, `site-speed-insights`, `pro-cache-scanner` - and the name still carries the lineage). Silhouette gives the category, contents give the identity.** **DRAWN FOR 16px AND VERIFIED THERE, NOT AT DRAWING SIZE: first draft used 7-unit bars with 1.9-unit gaps = 1.75px bars separated by 0.47px at 16px, and the gaps MERGE INTO A SOLID BLOCK. Rasterised via rsvg-convert at 16/24/32px, magnified 12x, and inspected on light AND dark backgrounds before acceptance. Shipped version: 8-unit bars, 4-unit gaps (half a bar width), shield enlarged to fill the canvas so the interior has room. No text (illegible at 16px and needs font support), no gradient, no filter (both muddy at small size).** Fill is `#1e6ba8` = the EXISTING `--wps-blue` token, not a new blue; **the suite asserts the SVG fill equals the CSS token so a rebrand cannot silently desynchronise the mark from the interface.** Placed in the page header at 28px, styled from `admin.css` (zero inline attributes, `flex: 0 0 auto` so a long title cannot squash it), `alt=""` because the adjacent text already reads "WP Perf Shield" and double-announcing is noise not access. Assets now `assets/img/` beside `assets/css/` and `assets/js/`. **SVG ASSERTED INERT - no `<script>`, no event handler, no `xlink:href`, no `<foreignObject>`, no embedded raster, no external `url()`. This file is served to browsers FROM `wp-content`, so anything executable in it runs in the SITE'S OWN ORIGIN; the same rules that apply to an uploaded SVG apply to one the plugin ships, and a security plugin shipping a live SVG would be an embarrassing way to learn that.** 25-check suite. **TWO OF CLAUDE'S OWN MISTAKES CAUGHT WHILE WRITING IT: (1) an assertion matched `[^>]*` across an inline PHP expression whose closing delimiter contains `>`, failing for a reason unrelated to the markup - fixed by collapsing PHP blocks before testing the emitted attribute structure. (2) THE COMMENT WRITTEN TO EXPLAIN THAT GOTCHA USED `//`, AND A CLOSING PHP DELIMITER INSIDE A `//` COMMENT ENDS PHP MODE AND DUMPS THE REST OF THE FILE TO OUTPUT AS HTML - while `php -l` STILL REPORTS THE FILE VALID, because it is. The comment describing the pitfall was destroyed by the pitfall it described. Recorded because a lint pass that cannot see a defect is exactly the sort of thing worth writing down; block comments do not have this behaviour.** 1.4.48-1.4.53 suites re-run as regressions. **NO DETECTION OR BLOCKING LOGIC CHANGED.** `INDICATOR_VERSION` -> `1.4.54-1`.

**1.4.55 - two P0 fixes from an external review (operator supplied a 17-item prioritised improvement plan as YAML).** **BOTH P0 CLAIMS VERIFIED AGAINST THE CODE BEFORE ANY EDIT - an audit can be stale, and these touch privileged file operations. Both were real; one was WORSE than reported.** **P0-001 - THE HARDENING ROUTINE WAS LEAKING THE DATABASE CREDENTIALS. All three wp-config.php edit paths (`add_constants`, malware clean, salt rotation) called `next_backup_path( $config . '.wps.bak' )` then `@copy()`, producing `wp-config.php.wps.bak` IN THE WEB ROOT. That file is NOT PHP: wp-config.php's entire protection is that it is EXECUTED rather than SERVED, so a non-.php copy of it is handed to anyone who requests the name - DB_NAME, DB_USER, DB_PASSWORD, table prefix and every salt. The hardening routine was creating a total compromise and reporting success.** **THE FIX WAS ALREADY IN THE CODEBASE AND UNAPPLIED: 1.3.97 moved remediation backups into the quarantine store for EXACTLY this reason, with a comment explaining it; `class-hardening.php` was never migrated. New `backup_wpconfig()` choke point (bounds at the single point everything passes through, per the standing principle); `next_backup_path()` deleted as dead code.** **CRITICAL ORDERING DETAIL: quarantine availability is tested BEFORE the copy, not after, because `WPS_Utils::create_timestamped_backup()` FALLS BACK to `$path . '.wps-backup-' . $stamp` beside the original when the store is unavailable - which for wp-config.php is the very exposure being fixed. If a fallback copy is produced anyway it is `@unlink`ed and the edit REFUSED. Only a `quarantine:` return is accepted. A wp-config edit without a safe recovery point is not worth what it costs.** **WHAT THE REVIEW DID NOT COVER AND CLAUDE ADDED: upgrading stops NEW copies but CANNOT REMOVE THE ONES ALREADY IN THE WEB ROOT OF EVERY INSTALL THAT EVER USED THE HARDENING TAB. New `check_exposed_config_backup()` (registry now 70) reports them at CRITICAL, names WP Perf Shield as the author of its own `.wps.bak`/`.wps-clean.bak` files and states that deleting them is safe - and also finds everyone else's (`wp-config.php.bak`, `~`, `.save`, `.old`, `.orig`), which is the commoner real-world case and a well-worn route into WordPress sites. GATED ON CONTENT NOT NAME: >=2 credential constants must be present, because a name is not evidence and crying wolf over an empty `.bak` teaches operators to ignore findings. Remediation text says ROTATE SALTS AND CHANGE THE DB PASSWORD, not merely delete - a file exposed for an unknown period must be assumed read.** **P0-002 - SAVING SETTINGS SILENTLY SWITCHED OFF CSP. `save_settings()` passed `update_option()` a LITERAL ARRAY, destroying every key the Settings tab does not own. `csp_mode`/`csp_policy` live in the same `WPS_OPTION` and are edited on a DIFFERENT TAB, so enabling CSP then saving any unrelated setting reverted `csp_mode` to 'off' - A SECURITY CONTROL DISABLED BY AN UNRELATED ACTION, with no message and no trace. Now reads first and `array_merge`s, so future components' keys survive BY DEFAULT rather than by somebody remembering to add them here. Note the asymmetry that hid it: `class-csp.php` already did read-modify-write correctly; only the Settings handler was wrong. (Claude had already observed the rebuild-from-POST behaviour in 1.4.52 while adding `public_marker` and treated it purely as a save-your-own-key requirement, NOT noticing it destroyed another component's keys - the review caught what Claude had looked directly at and under-read.)** 26-check suite; **the new check exercised against REAL FILES ON DISK (the plugin's own shape, five third-party shapes) with FOUR REFUSALS (config-shaped file with no secrets, `wp-config-sample.php`, a `.php` include the server still executes, and `wp-config.php` itself); the settings fix carries an assertion that WOULD HAVE FAILED BEFORE IT, because a regression test that would also have passed against the bug is not a regression test.** 1.4.48-1.4.54 suites re-run. **SCOPE HELD DELIBERATELY: the review has 17 items across 5 priorities; this release contains the two P0s AND NOTHING ELSE. The remaining items - remediation registry, capability tightening, mandatory backups, scanner split, finding schema, resumable scans - are safety-critical refactors of privileged file operations, and the review's own constraint says "avoid behaviour-changing scanner refactors until regression tests exist". That sequencing is correct and is being followed.** **PRINCIPLE: a security tool's own maintenance actions are attack surface. This one hardened a site by writing its credentials into the web root, and did it while displaying a success message - the failure was invisible precisely because the feature worked.** `INDICATOR_VERSION` -> `1.4.55-1`.

**1.4.56 - ErrTraffic attribution, and a live false positive against every legitimate web3 plugin (operator forwarded Sekoia TDR's ErrTraffic report and asked whether it strengthens the plugin).** **THE ATTRIBUTION: Sekoia names `0x08207B087F61d7e95E441E15fd6d40BEfd6eD308` as the Polygon contract of ErrTraffic's "Analytics" cluster. THAT EXACT ADDRESS HAS BEEN IN `class-scanner.php`'S CLASS DOCBLOCK SINCE THE ORIGINAL wp-perf-analytics WORK, described only as "Confirmed contract". WP Perf Shield's founding target is ErrTraffic - a commercially sold MaaS (seller "LenAI", Exploit.IN since Dec 2025, $380/mo, $4,500 source with lifetime support), not an anonymous campaign. CORROBORATION, INDEPENDENT OF THE CONTRACT: Sekoia lists the Analytics cluster TLDs as .cfd/.club/.click/.cyou/.lat/.sbs/.shop/.xyz; the C2 hosts catalogued here from captured samples are `biletors.cfd`, `comicstar.lat`, `webanalytics-cdn.sbs` - three for three. The claim is bounded: same contract = same cluster infrastructure. It does NOT make every sample in this catalogue ErrTraffic (the doorway/TDS kits are unrelated).** **FALSE POSITIVE FOUND BY WRITING THE REFUSAL FIXTURE FIRST: a genuine NFT-gallery plugin (own directory, real header, plain readable code, `eth_call` to `polygon.drpc.org` because that is what such a plugin does) was reported CRITICAL as ClickFix malware. `SIGNATURES_PERF` is SINGLE-MATCH and contained SEVEN public RPC hostnames plus `eth_call` ITSELF - a string in every web3 library in existence. ANY legitimate NFT/wallet/web3 plugin on ANY site would have been flagged as malware and offered for deletion. Entries removed from the single-match list; they remain in `etherhiding_indicators()` where every consumer requires corroboration. ALL SIX SAMPLE TREES REPORT IDENTICAL COUNTS AFTER REMOVAL (11/11/6/7/6/0) - real samples carry a dozen other signature entries. THE 1.4.40 WORDFENCE LESSON IN A DIFFERENT COAT: a scanner that deletes legitimate software teaches its operator to ignore it, and then it protects nobody.** **STRUCTURAL GAP CLOSED: detection depended on a LIST OF RPC HOSTNAMES, which cannot be completed - the "Beer" cluster resolves through Quicknode (PER-CUSTOMER SUBDOMAINS) and assigns EACH AFFILIATE ITS OWN CONTRACT, so neither value is knowable in advance and an unseen affiliate defeated the match entirely. New `check_etherhiding_resolver()` matches the SHAPE: a JSON-RPC read primitive + a `0x`+40-hex address + A THIRD INDEPENDENT SIGNAL (obfuscation, clipboard-execution lure, known ErrTraffic request path, or a location no self-declaring plugin would choose). Both of the first two are required because either alone is ordinary; the third is required because the first two together describe a legitimate web3 plugin EXACTLY.** Added `quiknode.pro`/`quicknode.pro` (registrable domain, since subdomains are per-customer) and `errtraffic_endpoint_markers()` = `/cf.js`, `/api/css.js`, `a=ctx`, `src=cloudflare` - **durable because both clusters rotate C2 daily via the contract, so the PATHS outlive the DOMAINS.** 23-check suite; **the legitimate web3 plugin is asserted clean against EVERY CHECK IN THE SCANNER, not merely the new one.** **CONCURRENCY INCIDENT, RECORDED: a parallel session shipped its own 1.4.55 (two P0 fixes from an external review - a `.wps.bak` of wp-config.php written to a web-reachable path, and `save_settings()` rebuilding the option array so saving one setting wiped others) into the SAME container while this work was in flight. Claude nearly overwrote `fixtures1455.php`; `create_file` refusing on an existing path is what surfaced it. Their suite was run WITH these edits applied (26/26) before proceeding, this work was renumbered 1.4.55 -> 1.4.56, and the suite renamed to `fixtures1456.php`. NOTE: their save_settings fix is INDEPENDENTLY the same defect flagged in 1.4.52's changelog - found twice, from two directions, which is corroboration that it was real.** `INDICATOR_VERSION` -> `1.4.56-1`.

**1.4.57 - the catalogue was wrong about its own contents (operator sample `Plugin-b45b652c.zip`, one file, 129,503 bytes, MD5 `ab86726bb8ed4527cb6ea787f9a12c1a`, SHA-256 `ee72a3a0...8d31e`).** Theme-loader JS injector family: `ENDPLUGINJS`/`ENDPLUGINFN` heredocs, 5,248 `_0x` hex identifiers in a single 119,998-byte line, constant `IB45B652C_PLUGIN_VERSION` confirming the `IB<8HEX-UPPER>_PLUGIN_VERSION` convention holds across members. **THE 1.4.50 SHAPE PATTERN WORKED ON A MEMBER IT HAD NEVER SEEN: blocked at activation AND rejected at upload (matched on `ENDPLUGINJS`) by a build with no knowledge of this file; 5 checks detected it, all critical. First empirical confirmation that blocking a FORM rather than a STRING generalises - which was the whole argument of 1.4.50 and until now was untested against an unseen member.** Only the hash layer was open; both hashes added from the file in hand. **DOCUMENTATION DEFECT, AND THE INTERESTING PART: `doc/variants.md` listed TWO members of this family. THE SOURCE KNEW FOUR. `Plugin-b45b652c` and `Plugin-45e0930c` were confirmed samples in the ORIGINAL 1.3.79 analysis, recorded in comments in `class-scanner.php` (lines 9160-9161) and `class-wps-indicators.php` (607-608) INCLUDING THE EXACT BYTE COUNTS - 129,503 and 127,542 - and the 129,503 matches this upload precisely. They never reached the catalogue built in 1.4.51. Blocking was never affected (the shape pattern covers any member); what was wrong is the document that tells an analyst what is known.** **WHY THE 1.4.51 COMPLETENESS GATE MISSED IT - A REAL LIMIT IN A MECHANISM BUILT FOR EXACTLY THIS: the gate reads VALUES out of array-returning indicator accessors and the blocking lists. A SAMPLE NAMED IN A COMMENT IS NOT A VALUE IN A LIST, so it was invisible to the very check designed to prevent undocumented knowledge. Extended: the gate now extracts sample identifiers FROM THE SOURCE in the shapes this project uses (`Plugin-<8hex>`, `<word>_<6hex>` kit directories, hex-suffixed plugin slugs) and holds them to the same standard. Swept across the codebase it found EXACTLY the two gaps; the other 13 identifiers were already documented.** **THE NEW GATE WAS VERIFIED BY INJECTING THE DEFECT, and Claude's FIRST ATTEMPT AT THAT PROOF WAS ITSELF FAULTY: removing the catalogue table row left `Plugin-b45b652c` present in the Appendix F hash attribution, so the gate PASSED and appeared not to work. Re-proved by injecting a synthetic `Plugin-deadbeef` into a source comment and confirming the gate FAILS and names it. A REGRESSION TEST THAT WOULD HAVE PASSED AGAINST THE BUG PROVES NOTHING - and verifying the verification is the step that catches this.** No hashes invented for `Plugin-45e0930c` or `Plugin-390a770b`; files not in hand. 1.4.48-1.4.56 suites re-run as regressions including the parallel session's 1.4.55. **PRINCIPLE: a completeness check is only as complete as its notion of what counts as knowledge. Values in lists were the easy half; the project's real memory is also in its comments, and a catalogue that cannot see them will drift exactly where the analysis was richest.** `INDICATOR_VERSION` -> `1.4.57-1`.

**1.4.58 - the fourth member, and the comment that was right all along (operator sample `Plugin-45e0930c.zip`, one file, 127,542 bytes, MD5 `748f6d05c328364ebf6a0cec1aec350d`, SHA-256 `dfe33210...da66`).** Second of the two members 1.4.57 identified as recorded in 1.3.79's code comments but absent from the catalogue. **THE BYTE COUNT MATCHES THE COMMENT EXACTLY - `class-scanner.php:9161` and `class-wps-indicators.php:608` both say 127,542, written during the original analysis, and the file supplied today is 127,542. The source note was accurate for however many releases nobody checked it, which is worth recording as the counterweight to 1.4.57's finding: the comments were not wrong, they were merely unread by the catalogue.** Both hashes added from the file in hand; entry promoted **Catalogued -> Verified**. **THREE OF FOUR MEMBERS NOW VERIFIED; `Plugin-390a770b` is the only one still carried without a fingerprint.** Coverage before this release was already complete apart from hashes: blocked at activation by the 1.4.50 shape pattern, rejected at upload on `ENDPLUGINJS`, 5 checks at critical. **FAMILY PROFILE NOW FIRM WITH FOUR MEMBERS: folder `Plugin-<8hex>`, description `simple js plugin`, constant `I<8HEX-UPPER>_PLUGIN_VERSION` (I7E4EB3FF / IB45B652C / I45E0930C - consistent across all). Payloads cluster 118,037-121,167 bytes on a single line carrying 4,911-5,248 `_0x` identifiers. THAT SPREAD IS NARROW ENOUGH TO INDICATE ONE BUILDER EMITTING PER-VICTIM COPIES rather than four separately authored variants - the same conclusion the naming convention pointed at, now with sizes behind it. Stated as an inference from four samples, not a finding.** 1.4.48-1.4.57 suites re-run as regressions including the parallel session's 1.4.55; the 1.4.57 source-identifier gate passes against the updated catalogue. **PRINCIPLE: 1.4.57 showed a catalogue can be blind to what the source knows. This release shows the converse is also true - the source knowledge was CORRECT, precisely recorded, and simply never promoted. The defect was in the promotion path, not the analysis.** `INDICATOR_VERSION` -> `1.4.58-1`.

**1.4.59 - the sample that needed nothing, and the audit it prompted (operator sample `site-speed-insights-d6e7__2_.zip`, one file, 9,656 bytes).** **NOTHING WAS MISSING: MD5 `6f6b4854cb0d71f81796ead56132c89a` AND SHA-256 `9b5cc2de...caa8` were BOTH already catalogued and BOTH matched the file exactly. All five layers held - activation blocked, upload rejected (matched on the option key `wp_204acd2d43_cfg`) INCLUDING renamed AND rebuilt so both fingerprints missed, option key catalogued, 7 checks at 5 criticals. A verification that confirms the catalogue is not a release, and was not treated as one.** **TWO DOCUMENTATION CORRECTIONS CAME OUT OF IT. (1) UNDERSTATEMENT: the entry was marked `Catalogued` (recorded from analysis, not re-examined). It has now been re-examined with hashes computed from the file, which is this catalogue's definition of `Verified`. Promoted; 4 of 17 ClickFix members now verified directly. Same principle as 1.4.53 - the confidence marker must move when evidence arrives or it is decoration. (2) OVERSTATEMENT BY OMISSION, found by auditing the hash list to check that entry: **17 OF 34 FINGERPRINTED SAMPLES CARRY AN MD5 WITH NO SHA-256 COMPANION**, and one catalogue row carries no fingerprint at all (`Plugin-390a770b`). A reader of a catalogue that only shows what IS known will reasonably assume the rest is covered.** New **Appendix G**, GENERATED FROM `class-blocker.php` and ASSERTED AGAINST IT by `fixtures1451`, stating exactly where coverage is thin. **THE GAP ASSESSED HONESTLY RATHER THAN DRAMATISED: it is NOT an evasion hole - altering a file changes BOTH hashes, so an MD5-only entry is no easier to slip past than a pair. What it costs is CORROBORATION: a single MD5 is weaker evidence when confirming an identification, and MD5 is no longer collision-resistant so a crafted collision could provoke a FALSE match (note the matcher accepts EITHER hash, so a SHA-256 companion does not mitigate that - only re-examination does). Neither is urgent. Appendix G doubles as the list of samples worth supplying.** **EIGHTH PINNED-LITERAL ASSERTION DEFECT: the new appendix check looked for "never written from memory" while the document says "was ever written from memory" - failing on WORD ORDER rather than substance. Rewritten to assert the claim via two stable substrings. After 1.4.5, 1.4.8, 1.4.14, 1.4.16, 1.4.26, 1.4.49, 1.4.52. The recurrence rate remains the finding; this instance was in a documentation assertion, which is a new place for it.** NO CODE CHANGED - detection, blocking and settings identical to 1.4.58. 1.4.48-1.4.58 suites re-run as regressions including the parallel session's 1.4.55. **PRINCIPLE: publish the shape of your ignorance. A catalogue that lists only what it knows silently invites the reader to assume the remainder is covered, and the cheapest way to stop that is to generate the gap list from the same source as the coverage list.** `INDICATOR_VERSION` -> `1.4.59-1`.

**1.4.60 - phase 1 of an external security review of 1.4.59 (5 critical, 5 high, 7 medium).** **ARCHIVE IDENTITY VERIFIED FIRST: the review's `archive_sha256` 38ee657b...ef595 matches the packaged 1.4.59 EXACTLY, so findings map to the source in hand. All five criticals were reproduced against source before any edit, per the review's own `before_editing` requirement.** **CRIT-001 - THE INTEGRITY SCANNER NEVER RAN. `compare_plugin_files()` built `$iter` and iterated `$iterator`. REPRODUCED at runtime against a plugin carrying an injected `eval()` in a legitimate file AND a planted backdoor: two PHP warnings, `foreach` receives null, loop body never executes, returns two empty arrays, caller emits zero findings - BOTH REPORTED CLEAN. Warnings are not exceptions so the enclosing `try` caught nothing. The rename is one character; WHAT LET IT SURVIVE IS THAT A SILENT FAILURE AND A CLEAN RESULT WERE INDISTINGUISHABLE FROM OUTSIDE. Every exit now carries explicit `status` (complete/incomplete/failed) + reason: unresolvable directory = failed, budget/cap exhaustion = incomplete. Handler widened `\Exception` -> `\Throwable`, and that was NOT cosmetic - the first repaired run surfaced a `TypeError`, an Error the old handler would have let escape as a fatal.** **CRIT-002 - FAILED QUARANTINE AUTHORISED IRREVERSIBLE DELETION. The fallback ran whenever quarantine did not succeed, FOR ANY REASON; the comment stated the intent openly. THE CONFLATION: quarantine DISABLED is a policy choice (deletion is what the operator asked for); quarantine FAILING is an accident (no disk, permissions, unwritable store) and is NOT evidence that destroying the file is safe. The old behaviour turned a recoverable incident into permanent data loss PRECISELY WHEN THE MACHINE WAS ALREADY MISBEHAVING, and most eagerly on the findings least likely to be correct. A live threat left one more scan is recoverable; a legitimate file deleted because the quarantine dir was full is not. Now: failed attempt short-circuits, target untouched, finding stays OPEN and unremediated, `auto_remediation_withheld` event recorded, operator told which directory to check. Deletion still reachable when quarantine is deliberately off.** **CRIT-004 - NO SINGLE-FLIGHT LOCK. New `WPS_Scan_Lock`: ATOMIC acquisition via `INSERT` into the options table (either creates the row or fails, NO read-then-write window - the review explicitly prohibits a non-atomic transient), random owner token so a worker can only release its OWN lock, TTL so a crashed worker cannot block permanently, stale recovery, and `describe()` that OMITS the token because the token is the credential that authorises release. Release in `finally` so a fatal cannot strand it.** **AKISMET - behaviour untouched and ASSERTED untouched (default-enabled reporting is a non-negotiable product decision). Comment saying "if the operator opted in" corrected. ADDITIONAL DEFECT FOUND DURING VERIFICATION, NOT IN THE REVIEW: `akismet_enrichment` (whether Akismet is CONSULTED for block duration) was READ at runtime but had NO settings control and NO save handler, so it could never be set - and its docblock said "there is nothing to switch on" directly above the line reading the switch. Control made real: settings row + save key, still default-enabled, described accurately as the opposite data flow to reporting (enrichment ASKS, reporting SENDS).** **32-check suite run against BOTH builds: 32/32 pass on 1.4.60, 27/32 FAIL on 1.4.59. THE PROOF TOOK TWO ATTEMPTS AND BOTH FAILURES ARE INSTRUCTIVE: (1) the suite first PASSED against 1.4.59 because a hardcoded `putenv()` overrode the environment - it was testing the PATCHED tree while claiming to test the unpatched one, the exact failure mode flagged in 1.4.57; (2) it then FATALLED because a missing class file was a hard `require`. Fixed: environment honoured, includes tolerant so an absent file is REPORTED by the assertion that cares rather than crashing the run.** All 8 earlier suites green; 9 sample trees unchanged. **STILL OUTSTANDING - PHASE 1 OF 7. CRIT-003 (fixed-order scan starvation) and CRIT-005 (event-chain concurrency) NOT fixed here, plus all high and medium findings. CRIT-003 IS NOT NEWS: Claude identified it at the start of this session's work, described it accurately, and then shipped twelve other releases without fixing it; the review found it independently. CRIT-005 is the probable cause of the operator's long-standing unexplained chain-verification failure on the live site - concurrent writes fork the chain and verification reports a fork as tampering.** **PRINCIPLE: a check that cannot report its own failure will eventually report a failure as a pass, and nobody will know. Explicit execution status is not bookkeeping - it is the difference between a scanner and a green light.** `INDICATOR_VERSION` -> `1.4.60-1`.

**1.4.61 - the handover document, asserted against the code (operator: "update documentation and indicate the next phases so that any AI tool can continue from the last work done").** New `doc/remediation-roadmap.md`, registered in `WPS_Admin_Docs::documents()` as the FIRST entry so a resuming session lands on it. Carries: the 20-finding ledger with status (3 fixed, 1 partial, 16 open), what 1.4.60 repaired and how it is proven, the next 4 tasks with file locations + acceptance criteria, later phases in the review's order, product-decisions-that-are-not-defects, the prohibited-actions list, house conventions, and - critically for a container that does not persist - the environment rebuild commands, the derive-don't-pin WPS_VERSION rule, the stale-staged-copy trap, and the external-WPS_TEST_SITE rule. Four recurring defect shapes recorded (pinned-literal assertions, verify-the-verification, comments-are-not-evidence, `?>` in `//` comments). **THE DOCUMENT IS MACHINE-CHECKED BY `fixtures1461.php` (28 checks) AND THE CHECK BITES BOTH WAYS: every finding ID must appear; every **Fixed** claim must be genuinely present in source; every **Open** claim must be genuinely ABSENT from source. THE OPEN-CLAIM ASSERTIONS INVERT ON PURPOSE - the moment CRIT-003 continuation state or CRIT-005 transactions land, "genuinely still open" FAILS, forcing the ledger to be updated in the SAME COMMIT as the code. A status doc that drifts is worse than none because it is believed; this one cannot drift silently.** **TWO 1.4.60 OVERSTATEMENTS CORRECTED IN THE RECORD: (1) Phase-1 item 4 (explicit failed/incomplete statuses) was done in `compare_plugin_files()` ONLY - the 71-check registry still runs `array_merge($findings,(array)call_user_func($cb))` with no try/catch and no per-check status, so a throwing detector still escapes and empty-is-indistinguishable-from-clean still holds at the registry level. Task 1 of the next phase, not finished work. (2) The false "skipped checks run then" sentence STILL SHIPS - named as a task and asserted to still exist so the suite notices when it goes. MED-003 recorded as WORSENED by this programme, not quietly listed.** NO CODE CHANGED beyond the docs-registry entry and version constants; `class-scanner.php`, `class-blocker.php`, `class-event-log.php` byte-identical to 1.4.60. 28-check suite + 9 earlier suites green; fixtures1460 still 32/32 on patched, 27/32 on 1.4.59. **PRINCIPLE: a handover is only trustworthy if it is executable. Prose describing what is fixed can lie by omission or by staleness; an assertion that fails when the claim stops being true cannot. The roadmap earns belief by being wired to the thing it describes.** `INDICATOR_VERSION` -> `1.4.61-1`.

**1.4.68 - permanent range ban + report-every-blocked-address (operator request, one override on record).** Two operator asks during a live low-and-slow /24 rotation attack. (1) **Permanent range ban**: Diagnostics field to permanently deny an address or CIDR; the permanent store, address-only since 1.4.31, now matches CIDR entries at the gate (exact addresses O(1), only range entries walked, list capped). Safety is a real containment test, not the /24-only `network_is_protected()` compare: a range holding an allowlisted or 30-day-remembered admin address is refused, and anything broader than /16 (v4) or /32 (v6) is refused outright. Tested 11/11. (2) **Report every blocked address to Akismet** - `akismet_report_all_blocks`, default on. **OVERRIDE RECORDED: the recommendation was to keep reporting conservative; the operator chose the aggressive posture. Their key, their call, same pattern as 1.4.20 and the 1.4.27 default-on decision.** The broadening reports the first-offence single-username block that was previously held back as a possible mistyped password - a real false-positive risk to the SHARED Akismet corpus, since a wrongly-reported address is degraded for every site that queries Akismet. **Two safeguards were kept as non-negotiable because they protect third parties who are neither the attacker nor the operator, and are therefore not the operator's to trade: (a) a CDN/proxy/private address is never auto-reported - `ip_looks_like_infrastructure()` still stands - so a shared edge is not flagged for every site behind it; (b) a CIDR is NEVER submitted to Akismet - submit-spam is per-address, so reporting a /24 would flag its innocent neighbours; instead the individual attacking members of a blocked range are reported, capped and deduplicated.** Each address reported at most once; both new behaviours have off switches. Refactor: the infra-guard + dedup + submit core extracted into `report_single_attacker()`, shared by the per-address, range-member, and manual paths. Tested 6/6 including report-off, report-all-off-holds-back, report-all-on-reports, once-per-address, infra-guard-still-skips, and submission-always-carries-an-address-never-a-CIDR. New event `range_blocked_permanently` (classified `high` by the existing `blocked` prefix rule, same as `ip_blocked_permanently`). Also: the posture panel's "blocks by rule" line now attributes single-address blocks beside range rotation, so 624-blocked-next-to-4-range reconciles instead of reading as idle. `INDICATOR_VERSION` unchanged. 

**1.4.69 - documentation sync (recurring drift caught again).** Operator asked "did you update the documentation". Audit found the answer was *partly*: changelog, upgrading, readme.txt changelog and SSOT were current through 1.4.68, but doc/readme.md CONTENT and the readme.txt Description had not moved since 1.4.61 - only readme.md version line was bumped each release. This is the identical failure the 1.4.8 entry recorded ("the version line in doc/readme.md was bumped each release while CONTENT was never updated"), recurred across seven releases. Fixed by bringing readme.md Features/Sign-in/admin sections and the readme.txt Description forward to cover banned-plugins denylist, event-chain self-test, subnet-rotation trigger + range escalation ladder, XML-RPC multicall stripping, permanent range ban, and report-every-blocked-address. No code changed. **PROCESS NOTE: readme.md content is not covered by any assertion, which is why it drifts silently; the version-marker bump gives false confidence that the file was reviewed. A doc-freshness check belongs in the release routine.**

## Product Roadmap - EDR Programme (adopted 2026-07-03)

This section is the authoritative sequencing for everything queued after 1.3.97. It merges three inputs: the Tier-2 detection backlog, the security-hardening audit (triaged 2026-07-02), and the EDR product roadmap (adopted 2026-07-03). The positioning shift is deliberate and approved: WP Perf Shield grows from a purpose-built anti-malware scanner into a WordPress EDR platform that detects attacker behaviour, correlates events into incidents, and explains what happened. Signature scanning stays; behavioural detection joins it.

### The one-schema rule

The hardening audit's immutable HMAC-chained log and the roadmap's enriched event record are the same subsystem, and it gets built once. The current logger (`includes/class-logger.php`) writes a four-field event - time, type, subject, ip - capped at 200 entries in a JSON file. Its replacement is designed to the full unified schema from day one, even though early releases populate only part of it:

```text
timestamp, event_type, username, user_role, user_id, session_id,
incident_id, correlation_id, ip, country, city, asn, user_agent,
referer, request_uri, object_type, object_name, action, severity,
risk_score, notes, previous_hash, current_hash, hmac_signature
```

Fields the plugin can't populate yet (country/city/asn before the GeoIP decision; incident_id before correlation ships) are stored null rather than omitted, so the record shape never changes underneath consumers. `WPS_Logger::log_event()` stays as a shim that writes a schema-shaped record with the fields it has, so all ~70 existing call sites keep working unmodified.

### Release train

**1.3.98 - Tier-2 detection.** Verify-then-rescan after removal, the unified C2 content sweep, functions.php redirect-injection detection, and the per-scan wall-clock budget. Small, self-contained, keeps detection momentum while the event-subsystem design settles. No architectural risk.

**1.3.99 - Hardening I: the event subsystem.** New `WPS_Event_Log` implementing the unified schema with an HMAC-chained record (previous_hash + current_hash + hmac_signature per the audit), tamper-evident verification, secure storage (randomised filename, deny-PHP/deny-listing rules, rotation), and migration of the existing 200-entry log. Events tab re-rendered from the new store. The old `log_event()` API survives as the shim described above. Storage engine (custom table versus append-only secured file) is Decision 4 below and gets settled before this release starts.

**1.4.0 - Hardening II: complete the audit's high-priority list.** Rate limiting on destructive actions (delete, quarantine restore/purge, cleanup, salt regeneration; 30s cooldown, per-user), per-action audit enrichment (initiating user, original value, new value, reason, risk level - the quarantine manifest's `stored_by`/`reason` fields from 1.3.97 are the pattern, generalised), plugin self-baseline integrity (sha256 baseline of the plugin's own files at install/upgrade, verified on admin load and scheduled scans - distinct from the 1.3.89 wordpress.org check, which covers other plugins), and signed diagnostic exports. The 1.4.0 number marks the platform foundation as complete. Deferred from the audit as low-leverage: the AuthorizationManager formalisation (guard_request already centralises nonce + capability), the Request/Validator/Controller split, and REST hardening (the plugin registers no REST routes; re-evaluate if that changes).

**1.4.1 - EDR Phase 1: sessions, incidents, risk.** The plugin registers its first behavioural hooks: `wp_login`, `wp_logout`, `user_register`, `current_screen`, plus upload/activation/editor interceptors. Login timeline reconstruction per administrator session; incident grouping by session, IP, user, and a configurable time window; cumulative risk scoring against the adopted score table (successful login 10 -> new admin account 120, severity bands low 0-50 / medium 51-120 / high 121-220 / critical 221+, Decision 5 below); a timeline card in the admin. GeoIP-dependent scores (first-seen country, impossible travel) stay dormant until Decision 1.

**1.4.2 - EDR Phase 2: behaviour baselines.** Login history with trusted devices (mark/revoke), administrator behaviour baseline (pages, hours, IPs, browsers) with anomaly alerts, impossible travel (gated on Decision 1), session fingerprinting against cookie theft (gated on Decision 2).

**1.4.3 - EDR Phase 3: integrity and session summaries.** Proper file-integrity monitoring over the defined set (wp-config.php, .htaccess, mu-plugins, active theme, plugins, functions.php; sha256; triggered on login/logout/scheduled scan; reports modified/added/deleted) - this generalises the existing wp-config baseline and redrop hashing into one FIM. Admin session summary on logout. Event-time persistence detection (new-admin-account alerts via `user_register`, option-change monitoring) complementing the seven scan-time persistence checks that already ship (cron tampering, mu-plugins, wp-config, auto-prepend, dropins, dropper persistence, base64 options).

**1.4.4 - EDR Phase 4: classification.** Attack categorisation (brute force, credential stuffing, session hijacking, privilege escalation, persistence, reconnaissance, malware execution), the plugin-installation-chain correlator, admin-recon classification, and MITRE ATT&CK mapping as a static technique table over existing and new detections (T1110 brute force, T1505.003 web shell, T1078 valid accounts, extended as detections map). The Apache/Nginx log correlation from the optional server features slots here: `WPS_Log_Reader` (1.3.92) already greps readable logs in PHP; correlation joins those lines to events by timestamp + IP.

**1.4.5 - EDR Phase 5: visualisation.** Interactive filterable timeline, incident replay as a chronological narrative, one-page executive summary per critical incident. The incident graph ships only if it earns its complexity over the timeline view.

**Phase 6 (deception) - parked.** Honeypot admin accounts write to the users table and the honey-plugin endpoint accepts uploads by design; both change the plugin's risk posture and neither proceeds without explicit item-level approval (Decision 3).

### Decisions requiring explicit approval before their release

1. **GeoIP source** (blocks impossible travel, first-seen-country scoring, country/city/asn enrichment). Options: bundle MaxMind GeoLite2 (licence terms, update mechanism, tens of MB on disk), call an external lookup API (the plugin currently makes no third-party calls and works on restricted hosting - this reverses that posture), or ship without (fields stay null, dependent detections stay dormant). Default until decided: ship without.
2. **Client-side fingerprinting** requires JS collection on wp-admin pages beyond the plugin's own screens (admin.js currently loads only there) plus storage of per-session fingerprints. Default until decided: dormant.
3. **Deception features** (users-table writes, decoy upload endpoint). Default: parked.
4. **Event storage engine** - RESOLVED 1.3.99: custom DB table (`{$prefix}wps_events`). Phase-1 incident grouping is query-shaped work, the HMAC chain supplies tamper evidence either way, and a table survives filesystem resets; wholesale truncation is covered by the dual chain anchors. (Original options: table versus append-only secured file.)
5. **Risk score table**: adopt the roadmap's values as shipped defaults or recalibrate after 1.4.1's timeline data exists. Scores ship as filterable constants either way, in one place, per the one-source-of-truth rule.

### Standing rules extended to the programme

Detection remains technique-first; behavioural alerts follow the same review-only versus auto-remediation discipline as file detections (an anomaly is never auto-remediated; only content-confirmed malware is). Every list - scores, MITRE mappings, tracked screens, FIM file sets - lives in exactly one constant with accessors. UI additions follow the token system (PHP chooses classes, CSS owns colours, value-preserving) and the tabbed layout. The verification protocol extends to hook-driven features: the fixture harness gains WordPress hook stubs (`add_action`/`do_action` capture) so timelines and scoring are testable the way 1.3.97's detections are, and changelog claims stay grounded in what the harness actually exercised. `INDICATOR_VERSION` is unaffected by any of this planning; it moves only when the indicator lists change.

---
